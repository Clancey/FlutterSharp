// <auto-generated>
// This file was automatically generated on {{ generated_date }}
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi' hide Size;
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/utils.dart' hide parseBoxConstraints, parseEdgeInsetsGeometry, parseColor;
import 'package:flutter_module/maui_flutter.dart';
{{~ struct_import = string.downcase (string.replace struct_name "Struct" "") ~}}
import '../structs/{{ struct_import }}_struct.dart';

{{~ if documentation ~}}
/// {{ documentation }}
{{~ end ~}}
class {{ parser_name }} extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<{{ struct_name }}>.fromAddress(fos.handle.address).ref;
    return {{ widget_name }}(
{{~ for prop in properties ~}}
{{~ if prop.is_callback ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }}.address != 0 ? _create{{ string.capitalize prop.name }}Callback(map.{{ prop.struct_property_name }}.toDartString()) : null{{ if !for.last || has_children }},{{ end }}
{{~ else if prop.is_pointer_void && prop.is_required ~}}
      // TODO: {{ prop.property_name }} is required but type '{{ prop.dart_type }}' cannot be marshaled through FFI
      // This widget needs a manual wrapper to properly handle this parameter
      {{ prop.property_name }}: _get{{ string.capitalize prop.name }}Placeholder(){{ if !for.last || has_children }},{{ end }}
{{~ else if !prop.is_pointer_void ~}}
{{~ # Handle different property types with proper null handling ~}}
{{~ base_type = string.replace prop.dart_type "?" "" ~}}
{{~ if prop.is_color_primitive ~}}
{{~ if prop.is_nullable ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }} == 0 ? null : Color(map.{{ prop.struct_property_name }}){{ if !for.last || has_children }},{{ end }}
{{~ else ~}}
      {{ prop.property_name }}: Color(map.{{ prop.struct_property_name }}){{ if !for.last || has_children }},{{ end }}
{{~ end ~}}
{{~ else if prop.is_enum_type && !prop.parser_function ~}}
{{~ if prop.is_nullable ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }} == -1 ? null : {{ base_type }}.values[map.{{ prop.struct_property_name }}]{{ if !for.last || has_children }},{{ end }}
{{~ else ~}}
      {{ prop.property_name }}: {{ base_type }}.values[map.{{ prop.struct_property_name }}]{{ if !for.last || has_children }},{{ end }}
{{~ end ~}}
{{~ else if prop.is_string ~}}
{{~ if prop.is_nullable ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }}.address != 0 ? map.{{ prop.struct_property_name }}.toDartString() : null{{ if !for.last || has_children }},{{ end }}
{{~ else ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }}.address != 0 ? map.{{ prop.struct_property_name }}.toDartString() : ''{{ if !for.last || has_children }},{{ end }}
{{~ end ~}}
{{~ else if prop.is_pointer_type ~}}
{{~ # For pointer types, check for known types with sensible defaults FIRST (before nullable check) ~}}
{{~ # These types have common Flutter defaults that we should use when not set ~}}
{{~ if base_type == "AlignmentGeometry" || base_type == "Alignment" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : Alignment.center{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "Curve" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : Curves.linear{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "EdgeInsetsGeometry" || base_type == "EdgeInsets" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : EdgeInsets.zero{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "BorderRadiusGeometry" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : BorderRadius.zero{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "BorderRadius" ~}}
      {{ prop.property_name }}: (map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : BorderRadius.zero) as BorderRadius?{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "BoxConstraints" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : const BoxConstraints(){{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "Offset" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : Offset.zero{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "Size" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : Size.zero{{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "Decoration" || base_type == "BoxDecoration" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : const BoxDecoration(){{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "TextStyle" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : const TextStyle(){{ if !for.last || has_children }},{{ end }}
{{~ else if base_type == "Matrix4" ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : Matrix4.identity(){{ if !for.last || has_children }},{{ end }}
{{~ else if prop.is_nullable ~}}
{{~ # Unknown nullable type - return null when not set ~}}
      {{ prop.property_name }}: map.has{{ string.capitalize prop.name }} == 1 ? {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref) : null{{ if !for.last || has_children }},{{ end }}
{{~ else ~}}
{{~ # Unknown non-nullable type - try to call parser anyway (will fail at runtime if pointer is null) ~}}
      {{ prop.property_name }}: {{ prop.parser_function }}(map.{{ prop.struct_property_name }}.ref){{ if !for.last || has_children }},{{ end }}
{{~ end ~}}
{{~ else if prop.parser_function ~}}
      {{ prop.property_name }}: {{ prop.parser_function }}(map.{{ prop.struct_property_name }}){{ if !for.last || has_children }},{{ end }}
{{~ else if prop.is_bool && prop.is_nullable ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }} == 1 ? true : (map.{{ prop.struct_property_name }} == 0 ? false : null){{ if !for.last || has_children }},{{ end }}
{{~ else if prop.is_bool ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }} == 1{{ if !for.last || has_children }},{{ end }}
{{~ else ~}}
      {{ prop.property_name }}: map.{{ prop.struct_property_name }}{{ if !for.last || has_children }},{{ end }}
{{~ end ~}}
{{~ end ~}}
{{~ end ~}}
{{~ if has_children ~}}
{{~ if child_property_name ~}}
{{~ if child_is_nullable ~}}
      {{ child_property_name }}: DynamicWidgetBuilder.buildFromPointer(map.{{ child_property_name }}, buildContext)
{{~ else ~}}
      {{ child_property_name }}: DynamicWidgetBuilder.buildFromPointerNotNull(map.{{ child_property_name }}, buildContext)
{{~ end ~}}
{{~ else if children_property_name ~}}
      {{ children_property_name }}: DynamicWidgetBuilder.buildWidgets(map.{{ children_property_name }}.cast<ChildrenStruct>(), buildContext)
{{~ end ~}}
{{~ end ~}}
    );
  }

  @override
  String get widgetName => "{{ widget_name }}";

  /// Handles action dispatch to C# via method channel
  void _handleAction(String? actionId) {
    if (actionId == null || actionId.isEmpty) return;

    // Dispatch action to C# via method channel
    methodChannel.invokeMethod('HandleAction', {
      'actionId': actionId,
      'widgetType': widgetName,
    }).catchError((error) {
      print('Error dispatching action $actionId: $error');
    });
  }
{{~
  # Collect all required Pointer<Void> properties that need placeholders
  required_void_props = []
  for prop in properties
    if prop.is_pointer_void && prop.is_required
      required_void_props = array.add required_void_props prop
    end
  end

  # Collect all callback properties
  callback_props = []
  for prop in properties
    if prop.is_callback
      callback_props = array.add callback_props prop
    end
  end
~}}
{{~ if callback_props.size > 0 ~}}

  // Callback wrapper methods that dispatch to C# via method channel
{{~ for prop in callback_props ~}}
  dynamic _create{{ string.capitalize prop.name }}Callback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
{{~ end ~}}
{{~ end ~}}
{{~ if required_void_props.size > 0 ~}}

  // Placeholder methods for required parameters that cannot be marshaled through FFI
  // TODO: Implement proper handling for these parameters or create a manual wrapper
{{~ for prop in required_void_props ~}}
  dynamic _get{{ string.capitalize prop.name }}Placeholder() {
    // TODO: Provide proper default value for {{ prop.dart_type }}
    throw UnimplementedError('{{ widget_name }}.{{ prop.property_name }} requires manual implementation - type {{ prop.dart_type }} cannot be auto-generated');
  }
{{~ end ~}}
{{~ end ~}}
}
