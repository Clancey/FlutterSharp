// <auto-generated>
// This file was automatically generated on 2026-01-07 12:06:29 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi' hide Size;
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/utils.dart' hide parseBoxConstraints, parseEdgeInsetsGeometry, parseColor;
import 'package:flutter_module/maui_flutter.dart';
import '../structs/semantics_struct.dart';

/// A widget that annotates the widget tree with a description of the meaning of
  /// the widgets.
  /// 
  /// Used by assistive technologies, search engines, and other semantic analysis
  /// software to determine the meaning of the application.
  /// 
  /// {@youtube 560 315 https://www.youtube.com/watch?v=NvtMt_DtFrQ}
  /// 
  /// See also:
  /// 
  /// * [SemanticsProperties], which contains a complete documentation for each
  /// of the constructor parameters that belongs to semantics properties.
  /// * [MergeSemantics], which marks a subtree as being a single node for
  /// accessibility purposes.
  /// * [ExcludeSemantics], which excludes a subtree from the semantics tree
  /// (which might be useful if it is, e.g., totally decorative and not
  /// important to the user).
  /// * [RenderObject.describeSemanticsConfiguration], the rendering library API
  /// through which the [Semantics] widget is actually implemented.
  /// * [SemanticsNode], the object used by the rendering library to represent
  /// semantics in the semantics tree.
  /// * [SemanticsDebugger], an overlay to help visualize the semantics tree. Can
  /// be enabled using [WidgetsApp.showSemanticsDebugger],
  /// [MaterialApp.showSemanticsDebugger], or [CupertinoApp.showSemanticsDebugger].
class SemanticsParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<SemanticsStruct>.fromAddress(fos.handle.address).ref;
    return Semantics(
      container: map.container == 1,
      explicitChildNodes: map.explicitChildNodes == 1,
      excludeSemantics: map.excludeSemantics == 1,
      blockUserActions: map.blockUserActions == 1,
      enabled: map.enabled == 1 ? true : (map.enabled == 0 ? false : null),
      checked: map.checked == 1 ? true : (map.checked == 0 ? false : null),
      mixed: map.mixed == 1 ? true : (map.mixed == 0 ? false : null),
      selected: map.selected == 1 ? true : (map.selected == 0 ? false : null),
      toggled: map.toggled == 1 ? true : (map.toggled == 0 ? false : null),
      button: map.button == 1 ? true : (map.button == 0 ? false : null),
      slider: map.slider == 1 ? true : (map.slider == 0 ? false : null),
      keyboardKey: map.keyboardKey == 1 ? true : (map.keyboardKey == 0 ? false : null),
      link: map.link == 1 ? true : (map.link == 0 ? false : null),
      header: map.header == 1 ? true : (map.header == 0 ? false : null),
      headingLevel: map.headingLevel,
      textField: map.textField == 1 ? true : (map.textField == 0 ? false : null),
      readOnly: map.readOnly == 1 ? true : (map.readOnly == 0 ? false : null),
      focusable: map.focusable == 1 ? true : (map.focusable == 0 ? false : null),
      focused: map.focused == 1 ? true : (map.focused == 0 ? false : null),
      inMutuallyExclusiveGroup: map.inMutuallyExclusiveGroup == 1 ? true : (map.inMutuallyExclusiveGroup == 0 ? false : null),
      obscured: map.obscured == 1 ? true : (map.obscured == 0 ? false : null),
      multiline: map.multiline == 1 ? true : (map.multiline == 0 ? false : null),
      scopesRoute: map.scopesRoute == 1 ? true : (map.scopesRoute == 0 ? false : null),
      namesRoute: map.namesRoute == 1 ? true : (map.namesRoute == 0 ? false : null),
      hidden: map.hidden == 1 ? true : (map.hidden == 0 ? false : null),
      image: map.image == 1 ? true : (map.image == 0 ? false : null),
      liveRegion: map.liveRegion == 1 ? true : (map.liveRegion == 0 ? false : null),
      expanded: map.expanded == 1 ? true : (map.expanded == 0 ? false : null),
      isRequired: map.isRequired == 1 ? true : (map.isRequired == 0 ? false : null),
      maxValueLength: map.maxValueLength,
      currentValueLength: map.currentValueLength,
      identifier: map.identifier.address != 0 ? map.identifier.toDartString() : null,
      label: map.label.address != 0 ? map.label.toDartString() : null,
      value: map.value.address != 0 ? map.value.toDartString() : null,
      increasedValue: map.increasedValue.address != 0 ? map.increasedValue.toDartString() : null,
      decreasedValue: map.decreasedValue.address != 0 ? map.decreasedValue.toDartString() : null,
      hint: map.hint.address != 0 ? map.hint.toDartString() : null,
      tooltip: map.tooltip.address != 0 ? map.tooltip.toDartString() : null,
      onTapHint: map.onTapHint.address != 0 ? map.onTapHint.toDartString() : null,
      onLongPressHint: map.onLongPressHint.address != 0 ? map.onLongPressHint.toDartString() : null,
      textDirection: TextDirection.values[map.textDirection],
      child: DynamicWidgetBuilder.buildFromPointer(map.child, buildContext)
    );
  }

  @override
  String get widgetName => "Semantics";

  /// Handles action dispatch to C# via method channel
  void _handleAction(String? actionId) {
    if (actionId == null || actionId.isEmpty) return;

    // Dispatch action to C# via method channel
    methodChannel.invokeMethod('HandleAction', {
      'actionId': actionId,
      'widgetType': widgetName,
    }).catchError((error) {
      print('Error dispatching action $actionId: $error');
    });
  }
}
