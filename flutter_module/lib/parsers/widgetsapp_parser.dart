// <auto-generated>
// This file was automatically generated on 2026-01-07 08:54:20 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi';
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/maui_flutter.dart';
import '../structs/widgetsapp_struct.dart';

/// A convenience widget that wraps a number of widgets that are commonly
  /// required for an application.
  /// 
  /// One of the primary roles that [WidgetsApp] provides is binding the system
  /// back button to popping the [Navigator] or quitting the application.
  /// 
  /// It is used by both [MaterialApp] and [CupertinoApp] to implement base
  /// functionality for an app.
  /// 
  /// Find references to many of the widgets that [WidgetsApp] wraps in the "See
  /// also" section.
  /// 
  /// See also:
  /// 
  /// * [CheckedModeBanner], which displays a [Banner] saying "DEBUG" when
  /// running in debug mode.
  /// * [DefaultTextStyle], the text style to apply to descendant [Text] widgets
  /// without an explicit style.
  /// * [MediaQuery], which establishes a subtree in which media queries resolve
  /// to a [MediaQueryData].
  /// * [Localizations], which defines the [Locale] for its `child`.
  /// * [Title], a widget that describes this app in the operating system.
  /// * [Navigator], a widget that manages a set of child widgets with a stack
  /// discipline.
  /// * [Overlay], a widget that manages a [Stack] of entries that can be managed
  /// independently.
  /// * [SemanticsDebugger], a widget that visualizes the semantics for the child.
class WidgetsAppParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<WidgetsAppStruct>.fromAddress(fos.handle.address).ref;
    return WidgetsApp(
      navigatorKey: map.hasNavigatorKey == 1 ? parseGlobalKey<NavigatorState>(map.navigatorKey.ref) : null,
      onGenerateRouteAction: map.onGenerateRouteAction.address != 0 ? _createOnGenerateRouteCallback(map.onGenerateRouteAction.toDartString()) : null,
      onGenerateInitialRoutesAction: map.onGenerateInitialRoutesAction.address != 0 ? _createOnGenerateInitialRoutesCallback(map.onGenerateInitialRoutesAction.toDartString()) : null,
      pageRouteBuilderAction: map.pageRouteBuilderAction.address != 0 ? _createPageRouteBuilderCallback(map.pageRouteBuilderAction.toDartString()) : null,
      home: map.hasHome == 1 ? parseWidget(map.home.ref) : null,
      onUnknownRouteAction: map.onUnknownRouteAction.address != 0 ? _createOnUnknownRouteCallback(map.onUnknownRouteAction.toDartString()) : null,
      onNavigationNotificationAction: map.onNavigationNotificationAction.address != 0 ? _createOnNavigationNotificationCallback(map.onNavigationNotificationAction.toDartString()) : null,
      initialRoute: map.initialRoute.address != 0 ? map.initialRoute.toDartString() : null,
      builderAction: map.builderAction.address != 0 ? _createBuilderCallback(map.builderAction.toDartString()) : null,
      title: map.title.address != 0 ? map.title.toDartString() : null,
      onGenerateTitleAction: map.onGenerateTitleAction.address != 0 ? _createOnGenerateTitleCallback(map.onGenerateTitleAction.toDartString()) : null,
      textStyle: map.hasTextStyle == 1 ? parseTextStyle(map.textStyle.ref) : null,
      color: Color(map.color),
      localeListResolutionCallbackAction: map.localeListResolutionCallbackAction.address != 0 ? _createLocaleListResolutionCallbackCallback(map.localeListResolutionCallbackAction.toDartString()) : null,
      localeResolutionCallbackAction: map.localeResolutionCallbackAction.address != 0 ? _createLocaleResolutionCallbackCallback(map.localeResolutionCallbackAction.toDartString()) : null,
      showPerformanceOverlay: map.showPerformanceOverlay == 1,
      showSemanticsDebugger: map.showSemanticsDebugger == 1,
      debugShowWidgetInspector: map.debugShowWidgetInspector == 1,
      exitWidgetSelectionButtonBuilderAction: map.exitWidgetSelectionButtonBuilderAction.address != 0 ? _createExitWidgetSelectionButtonBuilderCallback(map.exitWidgetSelectionButtonBuilderAction.toDartString()) : null,
      moveExitWidgetSelectionButtonBuilderAction: map.moveExitWidgetSelectionButtonBuilderAction.address != 0 ? _createMoveExitWidgetSelectionButtonBuilderCallback(map.moveExitWidgetSelectionButtonBuilderAction.toDartString()) : null,
      tapBehaviorButtonBuilderAction: map.tapBehaviorButtonBuilderAction.address != 0 ? _createTapBehaviorButtonBuilderCallback(map.tapBehaviorButtonBuilderAction.toDartString()) : null,
      debugShowCheckedModeBanner: map.debugShowCheckedModeBanner == 1,
      restorationScopeId: map.restorationScopeId.address != 0 ? map.restorationScopeId.toDartString() : null,
      useInheritedMediaQuery: map.useInheritedMediaQuery == 1
    );
  }

  @override
  String get widgetName => "WidgetsApp";

  /// Handles action dispatch to C# via method channel
  void _handleAction(String? actionId) {
    if (actionId == null || actionId.isEmpty) return;

    // Dispatch action to C# via method channel
    methodChannel.invokeMethod('HandleAction', {
      'actionId': actionId,
      'widgetType': widgetName,
    }).catchError((error) {
      print('Error dispatching action $actionId: $error');
    });
  }

  // Callback wrapper methods that dispatch to C# via method channel
  dynamic _createOnGenerateRouteCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnGenerateInitialRoutesCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createPageRouteBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnUnknownRouteCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnNavigationNotificationCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnGenerateTitleCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createLocaleListResolutionCallbackCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createLocaleResolutionCallbackCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createExitWidgetSelectionButtonBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createMoveExitWidgetSelectionButtonBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createTapBehaviorButtonBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
}
