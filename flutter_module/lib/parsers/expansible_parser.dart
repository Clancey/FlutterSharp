// <auto-generated>
// This file was automatically generated on 2026-01-07 08:01:56 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi';
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/maui_flutter.dart';
import '../structs/expansible_struct.dart';

/// A [StatefulWidget] that expands and collapses.
  /// 
  /// An [Expansible] consists of a header, which is always shown, and a
  /// body, which is hidden in its collapsed state and shown in its expanded
  /// state.
  /// 
  /// The [Expansible] is expanded or collapsed with an animation driven by an
  /// [AnimationController]. When the widget is expanded, the height of its body
  /// animates from 0 to its fully expanded height.
  /// 
  /// This widget is typically used with [ListView] to create an "expand /
  /// collapse" list entry. When used with scrolling widgets like [ListView], a
  /// unique [PageStorageKey] must be specified as the [key], to enable the
  /// [Expansible] to save and restore its expanded state when it is scrolled
  /// in and out of view.
  /// 
  /// Provide [headerBuilder] and [bodyBuilder] callbacks to
  /// build the header and body widgets. An additional [expansibleBuilder]
  /// callback can be provided to further customize the layout of the widget.
  /// 
  /// The [Expansible] does not inherently toggle the expansion state. To toggle
  /// the expansion state, call [ExpansibleController.expand] and
  /// [ExpansibleController.collapse] as needed, most typically when the header
  /// returned in [headerBuilder] is tapped.
  /// 
  /// See also:
  /// 
  /// * [ExpansionTile], a Material-styled widget that expands and collapses.
class ExpansibleParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<ExpansibleStruct>.fromAddress(fos.handle.address).ref;
    return Expansible(
      controller: parseExpansibleController(map.controller),
      headerBuilderAction: map.headerBuilderActionAction.address != 0 ? _createHeaderBuilderCallback(map.headerBuilderActionAction.toDartString()) : null,
      bodyBuilderAction: map.bodyBuilderActionAction.address != 0 ? _createBodyBuilderCallback(map.bodyBuilderActionAction.toDartString()) : null,
      duration: parseDuration(map.duration),
      curve: parseCurve(map.curve),
      reverseCurve: parseInvalidType(map.reverseCurve),
      maintainState: map.maintainState,
      expansibleBuilderAction: map.expansibleBuilderActionAction.address != 0 ? _createExpansibleBuilderCallback(map.expansibleBuilderActionAction.toDartString()) : null
    );
  }

  @override
  String get widgetName => "Expansible";

  /// Handles action dispatch to C# via method channel
  void _handleAction(String? actionId) {
    if (actionId == null || actionId.isEmpty) return;

    // Dispatch action to C# via method channel
    methodChannel.invokeMethod('HandleAction', {
      'actionId': actionId,
      'widgetType': widgetName,
    }).catchError((error) {
      print('Error dispatching action $actionId: $error');
    });
  }

  // Callback wrapper methods that dispatch to C# via method channel
  dynamic _createHeaderBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createBodyBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createExpansibleBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
}
