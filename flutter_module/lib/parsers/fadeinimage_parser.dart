// <auto-generated>
// This file was automatically generated on 2026-01-07 08:01:56 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi';
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/maui_flutter.dart';
import '../structs/fadeinimage_struct.dart';

/// An image that shows a [placeholder] image while the target [image] is
  /// loading, then fades in the new image when it loads.
  /// 
  /// Use this class to display long-loading images, such as [NetworkImage.new],
  /// so that the image appears on screen with a graceful animation rather than
  /// abruptly popping onto the screen.
  /// 
  /// {@youtube 560 315 https://www.youtube.com/watch?v=pK738Pg9cxc}
  /// 
  /// If the [image] emits an [ImageInfo] synchronously, such as when the image
  /// has been loaded and cached, the [image] is displayed immediately, and the
  /// [placeholder] is never displayed.
  /// 
  /// The [fadeOutDuration] and [fadeOutCurve] properties control the fade-out
  /// animation of the [placeholder].
  /// 
  /// The [fadeInDuration] and [fadeInCurve] properties control the fade-in
  /// animation of the target [image].
  /// 
  /// Prefer a [placeholder] that's already cached so that it is displayed
  /// immediately. This prevents it from popping onto the screen.
  /// 
  /// When [image] changes, it is resolved to a new [ImageStream]. If the new
  /// [ImageStream.key] is different, this widget subscribes to the new stream and
  /// replaces the displayed image with images emitted by the new stream.
  /// 
  /// When [placeholder] changes and the [image] has not yet emitted an
  /// [ImageInfo], then [placeholder] is resolved to a new [ImageStream]. If the
  /// new [ImageStream.key] is different, this widget subscribes to the new stream
  /// and replaces the displayed image to images emitted by the new stream.
  /// 
  /// When either [placeholder] or [image] changes, this widget continues showing
  /// the previously loaded image (if any) until the new image provider provides a
  /// different image. This is known as "gapless playback" (see also
  /// [Image.gaplessPlayback]).
  /// 
  /// {@tool snippet}
  /// 
  /// ```dart
  /// FadeInImage(
  /// // here `bytes` is a Uint8List containing the bytes for the in-memory image
  /// placeholder: MemoryImage(bytes),
  /// image: const NetworkImage('https://backend.example.com/image.png'),
  /// )
  /// ```
  /// {@end-tool}
class FadeInImageParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<FadeInImageStruct>.fromAddress(fos.handle.address).ref;
    return FadeInImage(
      placeholder: parseInvalidType(map.placeholder),
      placeholderErrorBuilderAction: map.placeholderErrorBuilderActionAction.address != 0 ? _createPlaceholderErrorBuilderCallback(map.placeholderErrorBuilderActionAction.toDartString()) : null,
      image: parseInvalidType(map.image),
      imageErrorBuilderAction: map.imageErrorBuilderActionAction.address != 0 ? _createImageErrorBuilderCallback(map.imageErrorBuilderActionAction.toDartString()) : null,
      fadeOutDuration: parseDuration(map.fadeOutDuration),
      fadeOutCurve: parseInvalidType(map.fadeOutCurve),
      fadeInDuration: parseDuration(map.fadeInDuration),
      fadeInCurve: parseInvalidType(map.fadeInCurve),
      width: map.width,
      color: Color(map.color),
      colorBlendMode: parseInvalidType(map.colorBlendMode),
      placeholderColor: parseInvalidType(map.placeholderColor),
      placeholderColorBlendMode: parseInvalidType(map.placeholderColorBlendMode),
      height: map.height,
      fit: parseInvalidType(map.fit),
      placeholderFit: parseInvalidType(map.placeholderFit),
      filterQuality: parseInvalidType(map.filterQuality),
      placeholderFilterQuality: parseInvalidType(map.placeholderFilterQuality),
      alignment: parseAlignmentGeometry(map.alignment),
      repeat: parseInvalidType(map.repeat),
      matchTextDirection: map.matchTextDirection,
      excludeFromSemantics: map.excludeFromSemantics,
      imageSemanticLabel: map.imageSemanticLabel
    );
  }

  @override
  String get widgetName => "FadeInImage";

  /// Handles action dispatch to C# via method channel
  void _handleAction(String? actionId) {
    if (actionId == null || actionId.isEmpty) return;

    // Dispatch action to C# via method channel
    methodChannel.invokeMethod('HandleAction', {
      'actionId': actionId,
      'widgetType': widgetName,
    }).catchError((error) {
      print('Error dispatching action $actionId: $error');
    });
  }

  // Callback wrapper methods that dispatch to C# via method channel
  dynamic _createPlaceholderErrorBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createImageErrorBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
}
