// <auto-generated>
// This file was automatically generated on 2026-01-07 08:01:56 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi';
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/maui_flutter.dart';
import '../structs/reorderablelist_struct.dart';

/// A scrolling container that allows the user to interactively reorder the
  /// list items.
  /// 
  /// This widget is similar to one created by [ListView.builder], and uses
  /// an [IndexedWidgetBuilder] to create each item.
  /// 
  /// It is up to the application to wrap each child (or an internal part of the
  /// child such as a drag handle) with a drag listener that will recognize
  /// the start of an item drag and then start the reorder by calling
  /// [ReorderableListState.startItemDragReorder]. This is most easily achieved
  /// by wrapping each child in a [ReorderableDragStartListener] or a
  /// [ReorderableDelayedDragStartListener]. These will take care of recognizing
  /// the start of a drag gesture and call the list state's
  /// [ReorderableListState.startItemDragReorder] method.
  /// 
  /// This widget's [ReorderableListState] can be used to manually start an item
  /// reorder, or cancel a current drag. To refer to the
  /// [ReorderableListState] either provide a [GlobalKey] or use the static
  /// [ReorderableList.of] method from an item's build method.
  /// 
  /// See also:
  /// 
  /// * [SliverReorderableList], a sliver list that allows the user to reorder
  /// its items.
  /// * [ReorderableListView], a Material Design list that allows the user to
  /// reorder its items.
class ReorderableListParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<ReorderableListStruct>.fromAddress(fos.handle.address).ref;
    return ReorderableList(
      itemBuilderAction: map.itemBuilderActionAction.address != 0 ? _createItemBuilderCallback(map.itemBuilderActionAction.toDartString()) : null,
      itemCount: map.itemCount,
      onReorderAction: map.onReorderActionAction.address != 0 ? _createOnReorderCallback(map.onReorderActionAction.toDartString()) : null,
      onReorderStartAction: map.onReorderStartActionAction.address != 0 ? _createOnReorderStartCallback(map.onReorderStartActionAction.toDartString()) : null,
      onReorderEndAction: map.onReorderEndActionAction.address != 0 ? _createOnReorderEndCallback(map.onReorderEndActionAction.toDartString()) : null,
      proxyDecoratorAction: map.proxyDecoratorActionAction.address != 0 ? _createProxyDecoratorCallback(map.proxyDecoratorActionAction.toDartString()) : null,
      padding: parseEdgeInsetsGeometry(map.padding),
      scrollDirection: parseInvalidType(map.scrollDirection),
      reverse: map.reverse,
      controller: parseScrollController(map.controller),
      primary: map.primary,
      physics: parseScrollPhysics(map.physics),
      shrinkWrap: map.shrinkWrap,
      anchor: map.anchor,
      cacheExtent: map.cacheExtent,
      dragStartBehavior: parseInvalidType(map.dragStartBehavior),
      keyboardDismissBehavior: map.keyboardDismissBehavior,
      restorationId: map.restorationId,
      clipBehavior: map.clipBehavior,
      itemExtent: map.itemExtent,
      itemExtentBuilder: parseInvalidType(map.itemExtentBuilder),
      prototypeItem: parseWidget(map.prototypeItem),
      autoScrollerVelocityScalar: map.autoScrollerVelocityScalar,
      dragBoundaryProviderAction: map.dragBoundaryProviderActionAction.address != 0 ? _createDragBoundaryProviderCallback(map.dragBoundaryProviderActionAction.toDartString()) : null
    );
  }

  @override
  String get widgetName => "ReorderableList";

  /// Handles action dispatch to C# via method channel
  void _handleAction(String? actionId) {
    if (actionId == null || actionId.isEmpty) return;

    // Dispatch action to C# via method channel
    methodChannel.invokeMethod('HandleAction', {
      'actionId': actionId,
      'widgetType': widgetName,
    }).catchError((error) {
      print('Error dispatching action $actionId: $error');
    });
  }

  // Callback wrapper methods that dispatch to C# via method channel
  dynamic _createItemBuilderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnReorderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnReorderStartCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createOnReorderEndCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createProxyDecoratorCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
  dynamic _createDragBoundaryProviderCallback(String actionId) {
    // Generic callback wrapper that handles various callback signatures
    // Returns a closure that dispatches the action and returns a placeholder widget if needed
    return (context, [child]) {
      _handleAction(actionId);
      // For builder callbacks, return the child or an empty Container
      return child ?? Container();
    };
  }
}
