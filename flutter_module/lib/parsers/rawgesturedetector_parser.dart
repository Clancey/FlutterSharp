// <auto-generated>
// This file was automatically generated on 2026-04-09 02:46:15 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi' hide Size;
import 'package:flutter/widgets.dart';
import 'package:flutter_module/flutter_sharp_structs.dart';
import 'package:flutter_module/maui_flutter.dart';
import '../generated/structs/rawgesturedetector_struct.dart';

/// A widget that detects gestures described by the given gesture
/// factories.
///
/// For common gestures, use a [GestureDetector].
/// [RawGestureDetector] is useful primarily when developing your
/// own gesture recognizers.
///
/// Configuring the gesture recognizers requires a carefully constructed map, as
/// described in [gestures] and as shown in the example below.
///
/// {@tool snippet}
///
/// This example shows how to hook up a [TapGestureRecognizer]. It assumes that
/// the code is being used inside a [State] object with a `_last` field that is
/// then displayed as the child of the gesture detector.
///
/// ```dart
/// RawGestureDetector(
/// gestures: <Type, GestureRecognizerFactory>{
/// TapGestureRecognizer: GestureRecognizerFactoryWithHandlers<TapGestureRecognizer>(
/// () => TapGestureRecognizer(),
/// (TapGestureRecognizer instance) {
/// instance
/// ..onTapDown = (TapDownDetails details) { setState(() { _last = 'down'; }); }
/// ..onTapUp = (TapUpDetails details) { setState(() { _last = 'up'; }); }
/// ..onTap = () { setState(() { _last = 'tap'; }); }
/// ..onTapCancel = () { setState(() { _last = 'cancel'; }); };
/// },
/// ),
/// },
/// child: Container(width: 300.0, height: 300.0, color: Colors.yellow, child: Text(_last)),
/// )
/// ```
/// {@end-tool}
///
/// See also:
///
/// * [GestureDetector], a less flexible but much simpler widget that does the same thing.
/// * [Listener], a widget that reports raw pointer events.
/// * [GestureRecognizer], the class that you extend to create a custom gesture recognizer.
class RawGestureDetectorParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map =
        Pointer<RawGestureDetectorStruct>.fromAddress(fos.handle.address).ref;
    return RawGestureDetector(
        // TODO: gestures uses Pointer<Void> and cannot be marshaled through FFI.
        // We only pass a placeholder when a non-null pointer is provided.
        gestures: map.gestures.address != 0
            ? _getGesturesPlaceholder()
            : (null as dynamic),
        behavior:
            map.hasBehavior != 0 ? HitTestBehavior.values[map.behavior] : null,
        excludeFromSemantics: map.excludeFromSemantics == 1,
        // TODO: semantics uses Pointer<Void> and cannot be marshaled through FFI.
        // We only pass a placeholder when a non-null pointer is provided.
        semantics: map.hasSemantics != 0 && map.semantics.address != 0
            ? _getSemanticsPlaceholder()
            : (null as dynamic),
        child: map.hasChild != 0
            ? DynamicWidgetBuilder.buildFromPointer(map.child, buildContext)
            : null);
  }

  @override
  String get widgetName => "RawGestureDetector";

  // Placeholder methods for parameters that cannot be marshaled through FFI.
  // TODO: Implement proper handling for these parameters or create a manual wrapper
  dynamic _getGesturesPlaceholder() {
    // TODO: Provide proper default value for Map<Type, GestureRecognizerFactory<GestureRecognizer>>
    throw UnimplementedError(
        'RawGestureDetector.gestures requires manual implementation - type Map<Type, GestureRecognizerFactory<GestureRecognizer>> cannot be auto-generated');
  }

  dynamic _getSemanticsPlaceholder() {
    // TODO: Provide proper default value for SemanticsGestureDelegate?
    throw UnimplementedError(
        'RawGestureDetector.semantics requires manual implementation - type SemanticsGestureDelegate? cannot be auto-generated');
  }
}
