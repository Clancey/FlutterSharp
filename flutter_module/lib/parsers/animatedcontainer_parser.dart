// <auto-generated>
// This file was automatically generated on 2026-01-08 02:00:39 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi' hide Size;
import 'package:ffi/ffi.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter/gestures.dart' show DragStartBehavior;
import 'package:flutter/rendering.dart' show PlatformViewHitTestBehavior, PointerSignalEventListener;
import 'package:flutter/services.dart' show PlatformViewCreatedCallback, PointerEnterEventListener, PointerExitEventListener, PointerHoverEventListener;
import 'package:flutter_module/flutter_sharp_structs.dart';
import '../generated_utility_parsers.dart';
import 'package:flutter_module/utils.dart' hide parseBoxConstraints, parseEdgeInsetsGeometry, parseColor, parseCurve, parseBoxBorder;
import 'package:flutter_module/maui_flutter.dart';
import '../generated/structs/animatedcontainer_struct.dart';

/// Animated version of [Container] that gradually changes its values over a period of time.
  /// 
  /// The [AnimatedContainer] will automatically animate between the old and
  /// new values of properties when they change using the provided curve and
  /// duration. Properties that are null are not animated. Its child and
  /// descendants are not animated.
  /// 
  /// This class is useful for generating simple implicit transitions between
  /// different parameters to [Container] with its internal [AnimationController].
  /// For more complex animations, you'll likely want to use a subclass of
  /// [AnimatedWidget] such as the [DecoratedBoxTransition] or use your own
  /// [AnimationController].
  /// 
  /// {@youtube 560 315 https://www.youtube.com/watch?v=yI-8QHpGIP4}
  /// 
  /// {@tool dartpad}
  /// The following example (depicted above) transitions an AnimatedContainer
  /// between two states. It adjusts the `height`, `width`, `color`, and
  /// [alignment] properties when tapped.
  /// 
  /// ** See code in examples/api/lib/widgets/implicit_animations/animated_container.0.dart **
  /// {@end-tool}
  /// 
  /// See also:
  /// 
  /// * [AnimatedPadding], which is a subset of this widget that only
  /// supports animating the [padding].
  /// * The [catalog of layout widgets](https://flutter.dev/widgets/layout/).
  /// * [AnimatedPositioned], which, as a child of a [Stack], automatically
  /// transitions its child's position over a given duration whenever the given
  /// position changes.
  /// * [AnimatedAlign], which automatically transitions its child's
  /// position over a given duration whenever the given [AnimatedAlign.alignment] changes.
  /// * [AnimatedSwitcher], which switches out a child for a new one with a customizable transition.
  /// * [AnimatedCrossFade], which fades between two children and interpolates their sizes.
class AnimatedContainerParser extends WidgetParser {
  @override
  Widget parse(IFlutterObjectStruct fos, BuildContext buildContext) {
    var map = Pointer<AnimatedContainerStruct>.fromAddress(fos.handle.address).ref;
    return AnimatedContainer(
      // TODO: alignment is required but type 'AlignmentGeometry' cannot be marshaled through FFI
      // This widget needs a manual wrapper to properly handle this parameter
      alignment: _getAlignmentPlaceholder(),
      padding: map.padding.address != 0 ? parseEdgeInsetsGeometry(map.padding.ref) : EdgeInsets.zero,
      decoration: map.decoration.address != 0 ? parseDecoration(map.decoration.ref) : const BoxDecoration(),
      foregroundDecoration: map.foregroundDecoration.address != 0 ? parseDecoration(map.foregroundDecoration.ref) : const BoxDecoration(),
      constraints: map.constraints.address != 0 ? parseBoxConstraints(map.constraints.ref) : const BoxConstraints(),
      margin: map.margin.address != 0 ? parseEdgeInsetsGeometry(map.margin.ref) : EdgeInsets.zero,
      transform: map.transform.address != 0 ? parseMatrix4(map.transform.ref) : Matrix4.identity(),
      transformAlignment: map.transformAlignment.address != 0 ? (parseAlignmentGeometry(map.transformAlignment.ref) as Alignment) : Alignment.center,
      clipBehavior: Clip.values[map.clipBehavior],
      color: Color(map.color),
      width: map.width,
      height: map.height,
      curve: map.curve.address != 0 ? parseCurve(map.curve.ref) : Curves.linear,
      duration: parseDurationMicroseconds(map.duration),
      onEnd: createVoidCallback(map.onEndAction.address != 0 ? map.onEndAction.toDartString() : null),
      child: DynamicWidgetBuilder.buildFromPointerNotNull(map.child, buildContext)
    );
  }

  @override
  String get widgetName => "AnimatedContainer";

  // Placeholder methods for required parameters that cannot be marshaled through FFI
  // TODO: Implement proper handling for these parameters or create a manual wrapper
  dynamic _getAlignmentPlaceholder() {
    // TODO: Provide proper default value for AlignmentGeometry
    throw UnimplementedError('AnimatedContainer.alignment requires manual implementation - type AlignmentGeometry cannot be auto-generated');
  }
}
