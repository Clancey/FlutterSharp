// <auto-generated>
// This file was manually created for FlutterSharp Phase 4 - Input Widgets
// </auto-generated>

import 'dart:ffi';
import 'package:ffi/ffi.dart';

/// FFI struct representation of the Material Design Switch widget.
///
/// A Material Design switch.
///
/// Used to toggle the on/off state of a single setting. The switch itself does not
/// maintain any state. Instead, when the state of the switch changes, the widget calls
/// the onChanged callback. Most widgets that use a switch will listen for the onChanged
/// callback and rebuild the switch with a new value to update the visual appearance.
final class SwitchStruct extends Struct {
  // FlutterObject Struct base fields
  external Pointer handle;
  external Pointer managedHandle;
  external Pointer<Utf8> widgetType;

  // WidgetStruct base field
  external Pointer<Utf8> id;

  /// Whether this switch is on (1) or off (0).
  @Int8()
  external int value;

  // Has flag for nullable callback: onChanged
  @Int8()
  external int hasOnChanged;

  /// Action identifier for onChanged callback.
  external Pointer<Utf8> onChangedAction;

  // Has flag for nullable property: activeColor
  @Int8()
  external int hasActiveColor;

  /// The color to use when this switch is on (ARGB int).
  @Int32()
  external int activeColor;

  // Has flag for nullable property: activeTrackColor
  @Int8()
  external int hasActiveTrackColor;

  /// The color to use on the track when this switch is on (ARGB int).
  @Int32()
  external int activeTrackColor;

  // Has flag for nullable property: inactiveThumbColor
  @Int8()
  external int hasInactiveThumbColor;

  /// The color to use on the thumb when this switch is off (ARGB int).
  @Int32()
  external int inactiveThumbColor;

  // Has flag for nullable property: inactiveTrackColor
  @Int8()
  external int hasInactiveTrackColor;

  /// The color to use on the track when this switch is off (ARGB int).
  @Int32()
  external int inactiveTrackColor;

  // Has flag for nullable property: focusColor
  @Int8()
  external int hasFocusColor;

  /// The color when it has the input focus (ARGB int).
  @Int32()
  external int focusColor;

  // Has flag for nullable property: hoverColor
  @Int8()
  external int hasHoverColor;

  /// The color when a pointer is hovering over it (ARGB int).
  @Int32()
  external int hoverColor;

  // Has flag for nullable property: splashRadius
  @Int8()
  external int hasSplashRadius;

  /// The splash radius of the circular Material ink response.
  @Double()
  external double splashRadius;

  // Has flag for nullable property: materialTapTargetSize
  @Int8()
  external int hasMaterialTapTargetSize;

  /// Configures the minimum size of the tap target (enum value as int).
  @Int32()
  external int materialTapTargetSize;

  /// True (1) if this widget will be selected as the initial focus.
  @Int8()
  external int autofocus;
}
