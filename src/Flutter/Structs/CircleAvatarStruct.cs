// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A circle that represents a user.
/// 
/// Typically used with a user's profile image, or, in the absence of
/// such an image, the user's initials. A given user's initials should
/// always be paired with the same background color, for consistency.
/// 
/// If [foregroundImage] fails then [backgroundImage] is used. If
/// [backgroundImage] fails too, [backgroundColor] is used.
/// 
/// The [onBackgroundImageError] parameter must be null if the [backgroundImage]
/// is null.
/// The [onForegroundImageError] parameter must be null if the [foregroundImage]
/// is null.
/// 
/// {@tool snippet}
/// 
/// If the avatar is to have an image, the image should be specified in the
/// [backgroundImage] property:
/// 
/// ```dart
/// CircleAvatar(
/// backgroundImage: NetworkImage(userAvatarUrl),
/// )
/// ```
/// {@end-tool}
/// 
/// The image will be cropped to have a circle shape.
/// 
/// {@tool snippet}
/// 
/// If the avatar is to just have the user's initials, they are typically
/// provided using a [Text] widget as the [child] and a [backgroundColor]:
/// 
/// ```dart
/// CircleAvatar(
/// backgroundColor: Colors.brown.shade800,
/// child: const Text('AH'),
/// )
/// ```
/// {@end-tool}
/// 
/// See also:
/// 
/// * [Chip], for representing users or concepts in long form.
/// * [ListTile], which can combine an icon (such as a [CircleAvatar]) with
/// some text for a fixed height list entry.
/// * <https://material.io/design/components/chips.html#input-chips>
	[StructLayout(LayoutKind.Sequential)]
	internal class CircleAvatarStruct : SingleChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Nullable field: backgroundColor
		private IntPtr _backgroundColor;

/// The color with which to fill the circle. Changing the background
/// color will cause the avatar to animate to the new color.
/// 
/// If a [backgroundColor] is not specified and [ThemeData.useMaterial3] is true,
/// [ColorScheme.primaryContainer] will be used, otherwise the theme's
/// [ThemeData.primaryColorLight] is used with dark foreground colors, and
/// [ThemeData.primaryColorDark] with light foreground colors.
		public IntPtr backgroundColor
		{
			get => _backgroundColor;
			set { _backgroundColor = value; HasbackgroundColor = 1; }
		}

		// Has flag for nullable property: foregroundColor
		public byte HasforegroundColor { get; set; }

		// Nullable field: foregroundColor
		private IntPtr _foregroundColor;

/// The default text color for text in the circle.
/// 
/// Defaults to the primary text theme color if no [backgroundColor] is
/// specified.
/// 
/// If a [foregroundColor] is not specified and [ThemeData.useMaterial3] is true,
/// [ColorScheme.onPrimaryContainer] will be used, otherwise the theme's
/// [ThemeData.primaryColorLight] for dark background colors, and
/// [ThemeData.primaryColorDark] for light background colors.
		public IntPtr foregroundColor
		{
			get => _foregroundColor;
			set { _foregroundColor = value; HasforegroundColor = 1; }
		}

		// Has flag for nullable property: backgroundImage
		public byte HasbackgroundImage { get; set; }

		// Nullable field: backgroundImage
		private IntPtr _backgroundImage;

/// The background image of the circle. Changing the background
/// image will cause the avatar to animate to the new image.
/// 
/// Typically used as a fallback image for [foregroundImage].
/// 
/// If the [CircleAvatar] is to have the user's initials, use [child] instead.
		public IntPtr backgroundImage
		{
			get => _backgroundImage;
			set { _backgroundImage = value; HasbackgroundImage = 1; }
		}

		// Has flag for nullable property: foregroundImage
		public byte HasforegroundImage { get; set; }

		// Nullable field: foregroundImage
		private IntPtr _foregroundImage;

/// The foreground image of the circle.
/// 
/// Typically used as profile image. For fallback use [backgroundImage].
		public IntPtr foregroundImage
		{
			get => _foregroundImage;
			set { _foregroundImage = value; HasforegroundImage = 1; }
		}

		// Has flag for nullable property: onBackgroundImageError
		public byte HasonBackgroundImageError { get; set; }

		// Callback field: onBackgroundImageError
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onBackgroundImageError;

/// An optional error callback for errors emitted when loading
/// [backgroundImage].
		/// <summary>
		/// Action identifier for onBackgroundImageError callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onBackgroundImageErrorAction
		{
			get => GetString(_onBackgroundImageError);
			set { SetString(ref _onBackgroundImageError, value); HasonBackgroundImageError = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onForegroundImageError
		public byte HasonForegroundImageError { get; set; }

		// Callback field: onForegroundImageError
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onForegroundImageError;

/// An optional error callback for errors emitted when loading
/// [foregroundImage].
		/// <summary>
		/// Action identifier for onForegroundImageError callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onForegroundImageErrorAction
		{
			get => GetString(_onForegroundImageError);
			set { SetString(ref _onForegroundImageError, value); HasonForegroundImageError = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: radius
		public byte Hasradius { get; set; }

		// Nullable field: radius
		private double _radius;

/// The size of the avatar, expressed as the radius (half the diameter).
/// 
/// If [radius] is specified, then neither [minRadius] nor [maxRadius] may be
/// specified. Specifying [radius] is equivalent to specifying a [minRadius]
/// and [maxRadius], both with the value of [radius].
/// 
/// If neither [minRadius] nor [maxRadius] are specified, defaults to 20
/// logical pixels. This is the appropriate size for use with
/// [ListTile.leading].
/// 
/// Changes to the [radius] are animated (including changing from an explicit
/// [radius] to a [minRadius]/[maxRadius] pair or vice versa).
		public double radius
		{
			get => _radius;
			set { _radius = value; Hasradius = 1; }
		}

		// Has flag for nullable property: minRadius
		public byte HasminRadius { get; set; }

		// Nullable field: minRadius
		private double _minRadius;

/// The minimum size of the avatar, expressed as the radius (half the
/// diameter).
/// 
/// If [minRadius] is specified, then [radius] must not also be specified.
/// 
/// Defaults to zero.
/// 
/// Constraint changes are animated, but size changes due to the environment
/// itself changing are not. For example, changing the [minRadius] from 10 to
/// 20 when the [CircleAvatar] is in an unconstrained environment will cause
/// the avatar to animate from a 20 pixel diameter to a 40 pixel diameter.
/// However, if the [minRadius] is 40 and the [CircleAvatar] has a parent
/// [SizedBox] whose size changes instantaneously from 20 pixels to 40 pixels,
/// the size will snap to 40 pixels instantly.
		public double minRadius
		{
			get => _minRadius;
			set { _minRadius = value; HasminRadius = 1; }
		}

		// Has flag for nullable property: maxRadius
		public byte HasmaxRadius { get; set; }

		// Nullable field: maxRadius
		private double _maxRadius;

/// The maximum size of the avatar, expressed as the radius (half the
/// diameter).
/// 
/// If [maxRadius] is specified, then [radius] must not also be specified.
/// 
/// Defaults to [double.infinity].
/// 
/// Constraint changes are animated, but size changes due to the environment
/// itself changing are not. For example, changing the [maxRadius] from 10 to
/// 20 when the [CircleAvatar] is in an unconstrained environment will cause
/// the avatar to animate from a 20 pixel diameter to a 40 pixel diameter.
/// However, if the [maxRadius] is 40 and the [CircleAvatar] has a parent
/// [SizedBox] whose size changes instantaneously from 20 pixels to 40 pixels,
/// the size will snap to 40 pixels instantly.
		public double maxRadius
		{
			get => _maxRadius;
			set { _maxRadius = value; HasmaxRadius = 1; }
		}

	}
}
