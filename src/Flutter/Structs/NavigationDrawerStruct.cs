// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// Material Design Navigation Drawer component.
/// 
/// On top of [Drawer]s, Navigation drawers offer a persistent and convenient way to switch
/// between primary destinations in an app.
/// 
/// The style for the icons and text are not affected by parent
/// [DefaultTextStyle]s or [IconTheme]s but rather controlled by parameters or
/// the [NavigationDrawerThemeData].
/// 
/// The [children] are a list of widgets to be displayed in the drawer. These can be a
/// mixture of any widgets, but there is special handling for [NavigationDrawerDestination]s.
/// They are treated as a group and when one is selected, the [onDestinationSelected]
/// is called with the index into the group that corresponds to the selected destination.
/// 
/// {@tool dartpad}
/// This example shows a [NavigationDrawer] used within a [Scaffold]
/// widget. The [NavigationDrawer] has headline widget, divider widget and three
/// [NavigationDrawerDestination] widgets. The initial [selectedIndex] is 0.
/// The [onDestinationSelected] callback changes the selected item's index and displays
/// a corresponding widget in the body of the [Scaffold].
/// 
/// ** See code in examples/api/lib/material/navigation_drawer/navigation_drawer.0.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [Scaffold.drawer], where one specifies a [Drawer] so that it can be
/// shown.
/// * [Scaffold.of], to obtain the current [ScaffoldState], which manages the
/// display and animation of the drawer.
/// * [ScaffoldState.openDrawer], which displays its [Drawer], if any.
/// * <https://material.io/design/components/navigation-drawer.html>
	[StructLayout(LayoutKind.Sequential)]
	internal class NavigationDrawerStruct : MultiChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Nullable field: backgroundColor
		private IntPtr _backgroundColor;

/// The background color of the [Material] that holds the [NavigationDrawer]'s
/// contents.
/// 
/// If this is null, then [NavigationDrawerThemeData.backgroundColor] is used.
/// If that is also null, then it falls back to [ColorScheme.surfaceContainerLow].
		public IntPtr backgroundColor
		{
			get => _backgroundColor;
			set { _backgroundColor = value; HasbackgroundColor = 1; }
		}

		// Has flag for nullable property: shadowColor
		public byte HasshadowColor { get; set; }

		// Nullable field: shadowColor
		private IntPtr _shadowColor;

/// The color used for the drop shadow to indicate elevation.
/// 
/// If null, [NavigationDrawerThemeData.shadowColor] is used. If that
/// is also null, the default value is [Colors.transparent] which
/// indicates that no drop shadow will be displayed.
/// 
/// See [Material.shadowColor] for more details on drop shadows.
		public IntPtr shadowColor
		{
			get => _shadowColor;
			set { _shadowColor = value; HasshadowColor = 1; }
		}

		// Has flag for nullable property: surfaceTintColor
		public byte HassurfaceTintColor { get; set; }

		// Nullable field: surfaceTintColor
		private IntPtr _surfaceTintColor;

/// The surface tint of the [Material] that holds the [NavigationDrawer]'s
/// contents.
/// 
/// This is not recommended for use. [Material 3 spec](https://m3.material.io/styles/color/the-color-system/color-roles)
/// introduced a set of tone-based surfaces and surface containers in its [ColorScheme],
/// which provide more flexibility. The intention is to eventually remove surface tint color from
/// the framework.
/// 
/// If this is null, then [NavigationDrawerThemeData.surfaceTintColor] is used.
/// If that is also null, the default value is [Colors.transparent].
		public IntPtr surfaceTintColor
		{
			get => _surfaceTintColor;
			set { _surfaceTintColor = value; HassurfaceTintColor = 1; }
		}

		// Has flag for nullable property: elevation
		public byte Haselevation { get; set; }

		// Nullable field: elevation
		private double _elevation;

/// The elevation of the [NavigationDrawer] itself.
/// 
/// If null, [NavigationDrawerThemeData.elevation] is used. If that
/// is also null, it will be 1.0.
		public double elevation
		{
			get => _elevation;
			set { _elevation = value; Haselevation = 1; }
		}

		// Has flag for nullable property: indicatorColor
		public byte HasindicatorColor { get; set; }

		// Nullable field: indicatorColor
		private IntPtr _indicatorColor;

/// The color of the [indicatorShape] when this destination is selected.
/// 
/// If this is null, [NavigationDrawerThemeData.indicatorColor] is used.
/// If that is also null, defaults to [ColorScheme.secondaryContainer].
		public IntPtr indicatorColor
		{
			get => _indicatorColor;
			set { _indicatorColor = value; HasindicatorColor = 1; }
		}

		// Has flag for nullable property: indicatorShape
		public byte HasindicatorShape { get; set; }

		// Nullable field: indicatorShape
		private IntPtr _indicatorShape;

/// The shape of the selected indicator.
/// 
/// If this is null, [NavigationDrawerThemeData.indicatorShape] is used.
/// If that is also null, defaults to [StadiumBorder].
		public IntPtr indicatorShape
		{
			get => _indicatorShape;
			set { _indicatorShape = value; HasindicatorShape = 1; }
		}

		// Has flag for nullable property: header
		public byte Hasheader { get; set; }

		// Widget field: header
		private IntPtr _header;

/// A widget to display at the top of the layout.
/// 
/// Typically used for titles, navigation bars, or other header content.
		public IntPtr? header
		{
			get => _header != IntPtr.Zero ? (IntPtr)_header : null;
			set { SetIntPtr(ref _header, value); Hasheader = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: footer
		public byte Hasfooter { get; set; }

		// Widget field: footer
		private IntPtr _footer;

/// A widget to display at the bottom of the layout.
/// 
/// Typically used for actions, navigation controls, or other footer content.
		public IntPtr? footer
		{
			get => _footer != IntPtr.Zero ? (IntPtr)_footer : null;
			set { SetIntPtr(ref _footer, value); Hasfooter = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: selectedIndex
		public byte HasselectedIndex { get; set; }

		// Nullable field: selectedIndex
		private int _selectedIndex;

/// The index into destinations for the current selected
/// [NavigationDrawerDestination] or null if no destination is selected.
/// 
/// A valid [selectedIndex] satisfies 0 <= [selectedIndex] < number of [NavigationDrawerDestination].
/// For an invalid [selectedIndex] like `-1`, all destinations will appear unselected.
		public int selectedIndex
		{
			get => _selectedIndex;
			set { _selectedIndex = value; HasselectedIndex = 1; }
		}

		// Has flag for nullable property: onDestinationSelected
		public byte HasonDestinationSelected { get; set; }

		// Callback field: onDestinationSelected
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onDestinationSelected;

/// Called when one of the [NavigationDrawerDestination] children is selected.
/// 
/// This callback usually updates the int passed to [selectedIndex].
/// 
/// Upon updating [selectedIndex], the [NavigationDrawer] will be rebuilt.
		/// <summary>
		/// Action identifier for onDestinationSelected callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onDestinationSelectedAction
		{
			get => GetString(_onDestinationSelected);
			set { SetString(ref _onDestinationSelected, value); HasonDestinationSelected = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: tilePadding
/// Defines the padding for [NavigationDrawerDestination] widgets (Drawer items).
/// 
/// Defaults to `EdgeInsets.symmetric(horizontal: 12.0)`.
		public IntPtr tilePadding { get; set; }

	}
}
