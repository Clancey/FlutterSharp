// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A menu button that displays a cascading menu.
/// 
/// It can be used as part of a [MenuBar], or as a standalone widget.
/// 
/// This widget represents a menu item that has a submenu. Like the leaf
/// [MenuItemButton], it shows a label with an optional leading or trailing
/// icon, but additionally shows an arrow icon showing that it has a submenu.
/// 
/// By default the submenu will appear to the side of the controlling button.
/// The alignment and offset of the submenu can be controlled by setting
/// [MenuStyle.alignment] on the [style] and the [alignmentOffset] argument,
/// respectively.
/// 
/// When activated (by being clicked, through keyboard navigation, or via
/// hovering with a mouse), it will open a submenu containing the
/// [menuChildren].
/// 
/// If [menuChildren] is empty, then this menu item will appear disabled.
/// 
/// See also:
/// 
/// * [MenuItemButton], a widget that represents a leaf menu item that does not
/// host a submenu.
/// * [MenuBar], a widget that renders menu items in a row in a Material Design
/// style.
/// * [MenuAnchor], a widget that creates a region with a submenu and shows it
/// when requested.
/// * [PlatformMenuBar], a widget that renders similar menu bar items from a
/// [PlatformMenuItem] using platform-native APIs instead of Flutter.
	[StructLayout(LayoutKind.Sequential)]
	internal class SubmenuButtonStruct : SingleChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: onHover
		public byte HasonHover { get; set; }

		// Callback field: onHover
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onHover;

/// Called when a pointer enters or exits the button response area.
/// 
/// The value passed to the callback is true if a pointer has entered this
/// part of the button and false if a pointer has exited.
		/// <summary>
		/// Action identifier for onHover callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onHoverAction
		{
			get => GetString(_onHover);
			set { SetString(ref _onHover, value); HasonHover = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onFocusChange
		public byte HasonFocusChange { get; set; }

		// Callback field: onFocusChange
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onFocusChange;

/// Handler called when the focus changes.
/// 
/// Called with true if this widget's [focusNode] gains focus, and false if it
/// loses focus.
		/// <summary>
		/// Action identifier for onFocusChange callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onFocusChangeAction
		{
			get => GetString(_onFocusChange);
			set { SetString(ref _onFocusChange, value); HasonFocusChange = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onOpen
		public byte HasonOpen { get; set; }

		// Callback field: onOpen
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onOpen;

/// A callback that is invoked when the menu is opened.
		/// <summary>
		/// Action identifier for onOpen callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onOpenAction
		{
			get => GetString(_onOpen);
			set { SetString(ref _onOpen, value); HasonOpen = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onClose
		public byte HasonClose { get; set; }

		// Callback field: onClose
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onClose;

/// A callback that is invoked when the menu is closed.
		/// <summary>
		/// Action identifier for onClose callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onCloseAction
		{
			get => GetString(_onClose);
			set { SetString(ref _onClose, value); HasonClose = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: controller
		public byte Hascontroller { get; set; }

		// Nullable field: controller
		private IntPtr _controller;

/// An optional [MenuController] for this submenu.
		public IntPtr controller
		{
			get => _controller;
			set { _controller = value; Hascontroller = 1; }
		}

		// Has flag for nullable property: style
		public byte Hasstyle { get; set; }

		// Nullable field: style
		private IntPtr _style;

/// Customizes this button's appearance.
/// 
/// Non-null properties of this style override the corresponding properties in
/// [themeStyleOf] and [defaultStyleOf]. [WidgetStateProperty]s that resolve
/// to non-null values will similarly override the corresponding
/// [WidgetStateProperty]s in [themeStyleOf] and [defaultStyleOf].
/// 
/// Null by default.
		public IntPtr style
		{
			get => _style;
			set { _style = value; Hasstyle = 1; }
		}

		// Has flag for nullable property: menuStyle
		public byte HasmenuStyle { get; set; }

		// Nullable field: menuStyle
		private IntPtr _menuStyle;

/// The [MenuStyle] of the menu specified by [menuChildren].
/// 
/// Defaults to the value of [MenuThemeData.style] of the ambient [MenuTheme].
		public IntPtr menuStyle
		{
			get => _menuStyle;
			set { _menuStyle = value; HasmenuStyle = 1; }
		}

		// Has flag for nullable property: alignmentOffset
		public byte HasalignmentOffset { get; set; }

		// Nullable field: alignmentOffset
		private IntPtr _alignmentOffset;

/// The offset of the menu relative to the alignment origin determined by
/// [MenuStyle.alignment] on the [style] attribute.
/// 
/// Use this for fine adjustments of the menu placement.
/// 
/// Defaults to an offset that takes into account the padding of the menu so
/// that the top starting corner of the first menu item is aligned with the
/// top of the [MenuAnchor] region.
		public IntPtr alignmentOffset
		{
			get => _alignmentOffset;
			set { _alignmentOffset = value; HasalignmentOffset = 1; }
		}

		// Simple field: clipBehavior
/// {@macro flutter.material.Material.clipBehavior}
/// 
/// Defaults to [Clip.hardEdge].
		public Clip clipBehavior { get; set; }

		// Has flag for nullable property: focusNode
		public byte HasfocusNode { get; set; }

		// Nullable field: focusNode
		private IntPtr _focusNode;

/// {@macro flutter.widgets.Focus.focusNode}
		public IntPtr focusNode
		{
			get => _focusNode;
			set { _focusNode = value; HasfocusNode = 1; }
		}

		// Has flag for nullable property: statesController
		public byte HasstatesController { get; set; }

		// Nullable field: statesController
		private IntPtr _statesController;

/// {@macro flutter.material.inkwell.statesController}
		public IntPtr statesController
		{
			get => _statesController;
			set { _statesController = value; HasstatesController = 1; }
		}

		// Has flag for nullable property: leadingIcon
		public byte HasleadingIcon { get; set; }

		// Widget field: leadingIcon
		private IntPtr _leadingIcon;

/// An optional icon to display before the [child].
		public IntPtr? leadingIcon
		{
			get => _leadingIcon != IntPtr.Zero ? (IntPtr)_leadingIcon : null;
			set { SetIntPtr(ref _leadingIcon, value); HasleadingIcon = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: submenuIcon
		public byte HassubmenuIcon { get; set; }

		// Nullable field: submenuIcon
		private IntPtr _submenuIcon;

/// If provided, the widget replaces the default [SubmenuButton] arrow icon.
/// 
/// Resolves in the following states:
/// * [WidgetState.disabled].
/// * [WidgetState.hovered].
/// * [WidgetState.focused].
/// 
/// If this is null, then the value of [MenuThemeData.submenuIcon] is used.
/// If that is also null, then defaults to a right arrow icon with the size
/// of 24 pixels.
		public IntPtr submenuIcon
		{
			get => _submenuIcon;
			set { _submenuIcon = value; HassubmenuIcon = 1; }
		}

		// Has flag for nullable property: trailingIcon
		public byte HastrailingIcon { get; set; }

		// Widget field: trailingIcon
		private IntPtr _trailingIcon;

/// An optional icon to display after the [child].
		public IntPtr? trailingIcon
		{
			get => _trailingIcon != IntPtr.Zero ? (IntPtr)_trailingIcon : null;
			set { SetIntPtr(ref _trailingIcon, value); HastrailingIcon = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: useRootOverlay
/// {@macro flutter.widgets.RawMenuAnchor.useRootOverlay}
/// 
/// Defaults to false.
		public bool useRootOverlay { get; set; }

		// Simple field: menuChildren
/// The list of widgets that appear in the menu when it is opened.
/// 
/// These can be any widget, but are typically either [MenuItemButton] or
/// [SubmenuButton] widgets.
/// 
/// If [menuChildren] is empty, then the button for this menu item will be
/// disabled.
		public IntPtr menuChildren { get; set; }

	}
}
