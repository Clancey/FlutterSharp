// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A single-line [ListTile] with an expansion arrow icon that expands or collapses
/// the tile to reveal or hide the [children].
/// 
/// This widget is typically used with [ListView] to create an "expand /
/// collapse" list entry. When used with scrolling widgets like [ListView], a
/// unique [PageStorageKey] must be specified as the [key], to enable the
/// [ExpansionTile] to save and restore its expanded state when it is scrolled
/// in and out of view.
/// 
/// This class overrides the [ListTileThemeData.iconColor] and [ListTileThemeData.textColor]
/// theme properties for its [ListTile]. These colors animate between values when
/// the tile is expanded and collapsed: between [iconColor], [collapsedIconColor] and
/// between [textColor] and [collapsedTextColor].
/// 
/// The expansion arrow icon is shown on the right by default in left-to-right languages
/// (i.e. the trailing edge). This can be changed using [controlAffinity]. This maps
/// to the [leading] and [trailing] properties of [ExpansionTile].
/// 
/// {@tool dartpad}
/// This example demonstrates how the [ExpansionTile] icon's location and appearance
/// can be customized.
/// 
/// ** See code in examples/api/lib/material/expansion_tile/expansion_tile.0.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This example demonstrates how an [ExpansibleController] can be used to
/// programmatically expand or collapse an [ExpansionTile].
/// 
/// ** See code in examples/api/lib/material/expansion_tile/expansion_tile.1.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [ListTile], useful for creating expansion tile [children] when the
/// expansion tile represents a sublist.
/// * The "Expand and collapse" section of
/// <https://material.io/components/lists#types>
	[StructLayout(LayoutKind.Sequential)]
	internal class ExpansionTileStruct : MultiChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: leading
		public byte Hasleading { get; set; }

		// Widget field: leading
		private IntPtr _leading;

/// A widget to display before the title.
/// 
/// Typically a [CircleAvatar] widget.
/// 
/// Depending on the value of [controlAffinity], the [leading] widget
/// may replace the rotating expansion arrow icon.
		public IntPtr? leading
		{
			get => _leading != IntPtr.Zero ? (IntPtr)_leading : null;
			set { SetIntPtr(ref _leading, value); Hasleading = (byte)(value != null ? 1 : 0); }
		}

		// Widget field: title
		private IntPtr _title;

/// The primary content of the list item.
/// 
/// Typically a [Text] widget.
		public IntPtr title
		{
			get => (IntPtr)_title;
			set => SetIntPtr(ref _title, value);
		}

		// Has flag for nullable property: subtitle
		public byte Hassubtitle { get; set; }

		// Widget field: subtitle
		private IntPtr _subtitle;

/// Additional content displayed below the title.
/// 
/// Typically a [Text] widget.
		public IntPtr? subtitle
		{
			get => _subtitle != IntPtr.Zero ? (IntPtr)_subtitle : null;
			set { SetIntPtr(ref _subtitle, value); Hassubtitle = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onExpansionChanged
		public byte HasonExpansionChanged { get; set; }

		// Callback field: onExpansionChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onExpansionChanged;

/// Called when the tile expands or collapses.
/// 
/// When the tile starts expanding, this function is called with the value
/// true. When the tile starts collapsing, this function is called with
/// the value false.
/// 
/// Instead of providing this property, consider adding this callback as a
/// listener to a provided [controller].
		/// <summary>
		/// Action identifier for onExpansionChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onExpansionChangedAction
		{
			get => GetString(_onExpansionChanged);
			set { SetString(ref _onExpansionChanged, value); HasonExpansionChanged = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Nullable field: backgroundColor
		private IntPtr _backgroundColor;

/// The color to display behind the sublist when expanded.
/// 
/// If this property is null then [ExpansionTileThemeData.backgroundColor] is used. If that
/// is also null then Colors.transparent is used.
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr backgroundColor
		{
			get => _backgroundColor;
			set { _backgroundColor = value; HasbackgroundColor = 1; }
		}

		// Has flag for nullable property: collapsedBackgroundColor
		public byte HascollapsedBackgroundColor { get; set; }

		// Nullable field: collapsedBackgroundColor
		private IntPtr _collapsedBackgroundColor;

/// When not null, defines the background color of tile when the sublist is collapsed.
/// 
/// If this property is null then [ExpansionTileThemeData.collapsedBackgroundColor] is used.
/// If that is also null then Colors.transparent is used.
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr collapsedBackgroundColor
		{
			get => _collapsedBackgroundColor;
			set { _collapsedBackgroundColor = value; HascollapsedBackgroundColor = 1; }
		}

		// Has flag for nullable property: trailing
		public byte Hastrailing { get; set; }

		// Widget field: trailing
		private IntPtr _trailing;

/// A widget to display after the title.
/// 
/// Depending on the value of [controlAffinity], the [trailing] widget
/// may replace the rotating expansion arrow icon.
		public IntPtr? trailing
		{
			get => _trailing != IntPtr.Zero ? (IntPtr)_trailing : null;
			set { SetIntPtr(ref _trailing, value); Hastrailing = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: showTrailingIcon
/// Specifies if the [ExpansionTile] should build a default trailing icon if [trailing] is null.
		public bool showTrailingIcon { get; set; }

		// Simple field: initiallyExpanded
/// Specifies if the list tile is initially expanded (true) or collapsed (false).
/// 
/// Alternatively, a provided [controller] can be used to initially expand the
/// tile if [ExpansibleController.expand] is called before this widget is built.
/// 
/// Defaults to false.
		public bool initiallyExpanded { get; set; }

		// Simple field: maintainState
/// Specifies whether the state of the children is maintained when the tile expands and collapses.
/// 
/// When true, the children are kept in the tree while the tile is collapsed.
/// When false (default), the children are removed from the tree when the tile is
/// collapsed and recreated upon expansion.
		public bool maintainState { get; set; }

		// Has flag for nullable property: tilePadding
		public byte HastilePadding { get; set; }

		// Nullable field: tilePadding
		private IntPtr _tilePadding;

/// Specifies padding for the [ListTile].
/// 
/// Analogous to [ListTile.contentPadding], this property defines the insets for
/// the [leading], [title], [subtitle] and [trailing] widgets. It does not inset
/// the expanded [children] widgets.
/// 
/// If this property is null then [ExpansionTileThemeData.tilePadding] is used. If that
/// is also null then the tile's padding is `EdgeInsets.symmetric(horizontal: 16.0)`.
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr tilePadding
		{
			get => _tilePadding;
			set { _tilePadding = value; HastilePadding = 1; }
		}

		// Has flag for nullable property: expandedAlignment
		public byte HasexpandedAlignment { get; set; }

		// Nullable field: expandedAlignment
		private IntPtr _expandedAlignment;

/// Specifies the alignment of [children], which are arranged in a column when
/// the tile is expanded.
/// 
/// The internals of the expanded tile make use of a [Column] widget for
/// [children], and [Align] widget to align the column. The [expandedAlignment]
/// parameter is passed directly into the [Align].
/// 
/// Modifying this property controls the alignment of the column within the
/// expanded tile, not the alignment of [children] widgets within the column.
/// To align each child within [children], see [expandedCrossAxisAlignment].
/// 
/// The width of the column is the width of the widest child widget in [children].
/// 
/// If this property is null then [ExpansionTileThemeData.expandedAlignment]is used. If that
/// is also null then the value of [expandedAlignment] is [Alignment.center].
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr expandedAlignment
		{
			get => _expandedAlignment;
			set { _expandedAlignment = value; HasexpandedAlignment = 1; }
		}

		// Has flag for nullable property: expandedCrossAxisAlignment
		public byte HasexpandedCrossAxisAlignment { get; set; }

		// Nullable field: expandedCrossAxisAlignment
		private CrossAxisAlignment _expandedCrossAxisAlignment;

/// Specifies the alignment of each child within [children] when the tile is expanded.
/// 
/// The internals of the expanded tile make use of a [Column] widget for
/// [children], and the `crossAxisAlignment` parameter is passed directly into
/// the [Column].
/// 
/// Modifying this property controls the cross axis alignment of each child
/// within its [Column]. The width of the [Column] that houses [children] will
/// be the same as the widest child widget in [children]. The width of the
/// [Column] might not be equal to the width of the expanded tile.
/// 
/// To align the [Column] along the expanded tile, use the [expandedAlignment]
/// property instead.
/// 
/// When the value is null, the value of [expandedCrossAxisAlignment] is
/// [CrossAxisAlignment.center].
		public CrossAxisAlignment expandedCrossAxisAlignment
		{
			get => _expandedCrossAxisAlignment;
			set { _expandedCrossAxisAlignment = value; HasexpandedCrossAxisAlignment = 1; }
		}

		// Has flag for nullable property: childrenPadding
		public byte HaschildrenPadding { get; set; }

		// Nullable field: childrenPadding
		private IntPtr _childrenPadding;

/// Specifies padding for [children].
/// 
/// If this property is null then [ExpansionTileThemeData.childrenPadding] is used. If that
/// is also null then the value of [childrenPadding] is [EdgeInsets.zero].
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr childrenPadding
		{
			get => _childrenPadding;
			set { _childrenPadding = value; HaschildrenPadding = 1; }
		}

		// Has flag for nullable property: iconColor
		public byte HasiconColor { get; set; }

		// Nullable field: iconColor
		private IntPtr _iconColor;

/// The icon color of tile's expansion arrow icon when the sublist is expanded.
/// 
/// Used to override to the [ListTileThemeData.iconColor].
/// 
/// If this property is null then [ExpansionTileThemeData.iconColor] is used. If that
/// is also null then the value of [ColorScheme.primary] is used.
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr iconColor
		{
			get => _iconColor;
			set { _iconColor = value; HasiconColor = 1; }
		}

		// Has flag for nullable property: collapsedIconColor
		public byte HascollapsedIconColor { get; set; }

		// Nullable field: collapsedIconColor
		private IntPtr _collapsedIconColor;

/// The icon color of tile's expansion arrow icon when the sublist is collapsed.
/// 
/// Used to override to the [ListTileThemeData.iconColor].
/// 
/// If this property is null then [ExpansionTileThemeData.collapsedIconColor] is used. If that
/// is also null and [ThemeData.useMaterial3] is true, [ColorScheme.onSurface] is used. Otherwise,
/// defaults to [ThemeData.unselectedWidgetColor] color.
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr collapsedIconColor
		{
			get => _collapsedIconColor;
			set { _collapsedIconColor = value; HascollapsedIconColor = 1; }
		}

		// Has flag for nullable property: textColor
		public byte HastextColor { get; set; }

		// Nullable field: textColor
		private IntPtr _textColor;

/// The color of the tile's titles when the sublist is expanded.
/// 
/// Used to override to the [ListTileThemeData.textColor].
/// 
/// If this property is null then [ExpansionTileThemeData.textColor] is used. If that
/// is also null then and [ThemeData.useMaterial3] is true, color of the [TextTheme.bodyLarge]
/// will be used for the [title] and [subtitle]. Otherwise, defaults to [ColorScheme.primary] color.
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr textColor
		{
			get => _textColor;
			set { _textColor = value; HastextColor = 1; }
		}

		// Has flag for nullable property: collapsedTextColor
		public byte HascollapsedTextColor { get; set; }

		// Nullable field: collapsedTextColor
		private IntPtr _collapsedTextColor;

/// The color of the tile's titles when the sublist is collapsed.
/// 
/// Used to override to the [ListTileThemeData.textColor].
/// 
/// If this property is null then [ExpansionTileThemeData.collapsedTextColor] is used.
/// If that is also null and [ThemeData.useMaterial3] is true, color of the
/// [TextTheme.bodyLarge] will be used for the [title] and [subtitle]. Otherwise,
/// defaults to color of the [TextTheme.titleMedium].
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr collapsedTextColor
		{
			get => _collapsedTextColor;
			set { _collapsedTextColor = value; HascollapsedTextColor = 1; }
		}

		// Has flag for nullable property: shape
		public byte Hasshape { get; set; }

		// Nullable field: shape
		private IntPtr _shape;

/// The tile's border shape when the sublist is expanded.
/// 
/// If this property is null, the [ExpansionTileThemeData.shape] is used. If that
/// is also null, a [Border] with vertical sides default to [ThemeData.dividerColor] is used
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr shape
		{
			get => _shape;
			set { _shape = value; Hasshape = 1; }
		}

		// Has flag for nullable property: collapsedShape
		public byte HascollapsedShape { get; set; }

		// Nullable field: collapsedShape
		private IntPtr _collapsedShape;

/// The tile's border shape when the sublist is collapsed.
/// 
/// If this property is null, the [ExpansionTileThemeData.collapsedShape] is used. If that
/// is also null, a [Border] with vertical sides default to Color [Colors.transparent] is used
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public IntPtr collapsedShape
		{
			get => _collapsedShape;
			set { _collapsedShape = value; HascollapsedShape = 1; }
		}

		// Has flag for nullable property: clipBehavior
		public byte HasclipBehavior { get; set; }

		// Nullable field: clipBehavior
		private Clip _clipBehavior;

/// {@macro flutter.material.Material.clipBehavior}
/// 
/// If this is not null and a custom collapsed or expanded shape is provided,
/// the value of [clipBehavior] will be used to clip the expansion tile.
/// 
/// If this property is null, the [ExpansionTileThemeData.clipBehavior] is used. If that
/// is also null, defaults to [Clip.antiAlias].
/// 
/// See also:
/// 
/// * [ExpansionTileTheme.of], which returns the nearest [ExpansionTileTheme]'s
/// [ExpansionTileThemeData].
		public Clip clipBehavior
		{
			get => _clipBehavior;
			set { _clipBehavior = value; HasclipBehavior = 1; }
		}

		// Has flag for nullable property: controlAffinity
		public byte HascontrolAffinity { get; set; }

		// Nullable field: controlAffinity
		private IntPtr _controlAffinity;

/// Typically used to force the expansion arrow icon to the tile's leading or trailing edge.
/// 
/// By default, the value of [controlAffinity] is [ListTileControlAffinity.platform],
/// which means that the expansion arrow icon will appear on the tile's trailing edge.
		public IntPtr controlAffinity
		{
			get => _controlAffinity;
			set { _controlAffinity = value; HascontrolAffinity = 1; }
		}

		// Has flag for nullable property: controller
		public byte Hascontroller { get; set; }

		// Nullable field: controller
		private IntPtr _controller;

/// If provided, the controller can be used to expand and collapse tiles.
/// 
/// In cases where control over the tile's state is needed from a callback
/// triggered by a widget within the tile, [ExpansibleController.of] may be
/// more convenient than supplying a controller.
		public IntPtr controller
		{
			get => _controller;
			set { _controller = value; Hascontroller = 1; }
		}

		// Has flag for nullable property: dense
		public byte Hasdense { get; set; }

		// Nullable field: dense
		private bool _dense;

/// {@macro flutter.material.ListTile.dense}
		public bool dense
		{
			get => _dense;
			set { _dense = value; Hasdense = 1; }
		}

		// Has flag for nullable property: splashColor
		public byte HassplashColor { get; set; }

		// Nullable field: splashColor
		private IntPtr _splashColor;

/// The splash color of the ink response when the tile is tapped.
/// 
/// This color is passed directly to the underlying [ListTile]'s
/// `splashColor` property, which controls the ink ripple (splash)
/// animation when the tile is tapped. Internally, [ListTile] uses
/// an [InkWell] (which handles the actual splash effect), and so the
/// provided color will apply to that ripple.
/// 
/// If null, the splash color will default to the current theme’s
/// `ThemeData.splashColor`.
/// 
/// See also:
/// 
/// * [ListTile.splashColor], which sets the ink splash for the tile.
/// * [InkWell.splashColor], which determines the color of the ripple
/// effect in Material widgets.
/// * [ThemeData.splashColor], which provides a fallback color.
		public IntPtr splashColor
		{
			get => _splashColor;
			set { _splashColor = value; HassplashColor = 1; }
		}

		// Has flag for nullable property: visualDensity
		public byte HasvisualDensity { get; set; }

		// Nullable field: visualDensity
		private IntPtr _visualDensity;

/// Defines how compact the expansion tile's layout will be.
/// 
/// {@macro flutter.material.themedata.visualDensity}
		public IntPtr visualDensity
		{
			get => _visualDensity;
			set { _visualDensity = value; HasvisualDensity = 1; }
		}

		// Has flag for nullable property: minTileHeight
		public byte HasminTileHeight { get; set; }

		// Nullable field: minTileHeight
		private double _minTileHeight;

/// {@macro flutter.material.ListTile.minTileHeight}
		public double minTileHeight
		{
			get => _minTileHeight;
			set { _minTileHeight = value; HasminTileHeight = 1; }
		}

		// Has flag for nullable property: enableFeedback
		public byte HasenableFeedback { get; set; }

		// Nullable field: enableFeedback
		private bool _enableFeedback;

/// {@macro flutter.material.ListTile.enableFeedback}
		public bool enableFeedback
		{
			get => _enableFeedback;
			set { _enableFeedback = value; HasenableFeedback = 1; }
		}

		// Simple field: enabled
/// Whether this expansion tile is interactive.
/// 
/// If false, the internal [ListTile] will be disabled, changing its
/// appearance according to the theme and disabling user interaction.
/// 
/// Even if disabled, the expansion can still be toggled programmatically
/// through an [ExpansionTileController].
		public bool enabled { get; set; }

		// Has flag for nullable property: expansionAnimationStyle
		public byte HasexpansionAnimationStyle { get; set; }

		// Nullable field: expansionAnimationStyle
		private IntPtr _expansionAnimationStyle;

/// Used to override the expansion animation curve and duration.
/// 
/// If [AnimationStyle.duration] is provided, it will be used to override
/// the expansion animation duration. If it is null, then [AnimationStyle.duration]
/// from the [ExpansionTileThemeData.expansionAnimationStyle] will be used.
/// Otherwise, defaults to 200ms.
/// 
/// If [AnimationStyle.curve] is provided, it will be used to override
/// the expansion animation curve. If it is null, then [AnimationStyle.curve]
/// from the [ExpansionTileThemeData.expansionAnimationStyle] will be used.
/// Otherwise, defaults to [Curves.easeIn].
/// 
/// If [AnimationStyle.reverseCurve] is provided, it will be used to override
/// the collapse animation curve. If it is null, then [AnimationStyle.reverseCurve]
/// from the [ExpansionTileThemeData.expansionAnimationStyle] will be used.
/// Otherwise, the same curve will be used as for expansion.
/// 
/// To disable the theme animation, use [AnimationStyle.noAnimation].
/// 
/// {@tool dartpad}
/// This sample showcases how to override the [ExpansionTile] expansion
/// animation curve and duration using [AnimationStyle].
/// 
/// ** See code in examples/api/lib/material/expansion_tile/expansion_tile.2.dart **
/// {@end-tool}
		public IntPtr expansionAnimationStyle
		{
			get => _expansionAnimationStyle;
			set { _expansionAnimationStyle = value; HasexpansionAnimationStyle = 1; }
		}

		// Simple field: internalAddSemanticForOnTap
/// Whether to add button:true to the semantics if onTap is provided.
/// This is a temporary flag to help changing the behavior of ListTile onTap semantics.
		public bool internalAddSemanticForOnTap { get; set; }

	}
}
