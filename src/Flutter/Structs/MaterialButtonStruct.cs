// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A utility class for building Material buttons that depend on the
/// ambient [ButtonTheme] and [Theme].
/// 
/// This class is planned to be deprecated in a future release.
/// Please use one or more of these buttons and associated themes instead:
/// 
/// * [TextButton], [TextButtonTheme], [TextButtonThemeData],
/// * [ElevatedButton], [ElevatedButtonTheme], [ElevatedButtonThemeData],
/// * [OutlinedButton], [OutlinedButtonTheme], [OutlinedButtonThemeData],
/// * [FilledButton], [FilledButtonTheme], [FilledButtonThemeData]
/// 
/// The button's size will expand to fit the child widget, if necessary.
/// 
/// MaterialButtons whose [onPressed] and [onLongPress] callbacks are null will be disabled. To have
/// an enabled button, make sure to pass a non-null value for [onPressed] or [onLongPress].
/// 
/// To create a button directly, without inheriting theme defaults, use
/// [RawMaterialButton].
/// 
/// If you want an ink-splash effect for taps, but don't want to use a button,
/// consider using [InkWell] directly.
/// 
/// See also:
/// 
/// * [IconButton], to create buttons that contain icons rather than text.
	[StructLayout(LayoutKind.Sequential)]
	internal class MaterialButtonStruct : SingleChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: onPressed
		public byte HasonPressed { get; set; }

		// Callback field: onPressed
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onPressed;

/// The callback that is called when the button is tapped or otherwise activated.
/// 
/// If this callback and [onLongPress] are null, then the button will be disabled.
/// 
/// See also:
/// 
/// * [enabled], which is true if the button is enabled.
		/// <summary>
		/// Action identifier for onPressed callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onPressedAction
		{
			get => GetString(_onPressed);
			set { SetString(ref _onPressed, value); HasonPressed = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onLongPress
		public byte HasonLongPress { get; set; }

		// Callback field: onLongPress
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onLongPress;

/// The callback that is called when the button is long-pressed.
/// 
/// If this callback and [onPressed] are null, then the button will be disabled.
/// 
/// See also:
/// 
/// * [enabled], which is true if the button is enabled.
		/// <summary>
		/// Action identifier for onLongPress callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onLongPressAction
		{
			get => GetString(_onLongPress);
			set { SetString(ref _onLongPress, value); HasonLongPress = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onHighlightChanged
		public byte HasonHighlightChanged { get; set; }

		// Callback field: onHighlightChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onHighlightChanged;

/// Called by the underlying [InkWell] widget's [InkWell.onHighlightChanged]
/// callback.
/// 
/// If [onPressed] changes from null to non-null while a gesture is ongoing,
/// this can fire during the build phase (in which case calling
/// [State.setState] is not allowed).
		/// <summary>
		/// Action identifier for onHighlightChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onHighlightChangedAction
		{
			get => GetString(_onHighlightChanged);
			set { SetString(ref _onHighlightChanged, value); HasonHighlightChanged = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: mouseCursor
		public byte HasmouseCursor { get; set; }

		// Nullable field: mouseCursor
		private IntPtr _mouseCursor;

/// {@macro flutter.material.RawMaterialButton.mouseCursor}
/// 
/// If this property is null, [WidgetStateMouseCursor.clickable] will be used.
		public IntPtr mouseCursor
		{
			get => _mouseCursor;
			set { _mouseCursor = value; HasmouseCursor = 1; }
		}

		// Has flag for nullable property: textTheme
		public byte HastextTheme { get; set; }

		// Nullable field: textTheme
		private IntPtr _textTheme;

/// Defines the button's base colors, and the defaults for the button's minimum
/// size, internal padding, and shape.
/// 
/// Defaults to `ButtonTheme.of(context).textTheme`.
		public IntPtr textTheme
		{
			get => _textTheme;
			set { _textTheme = value; HastextTheme = 1; }
		}

		// Has flag for nullable property: textColor
		public byte HastextColor { get; set; }

		// Nullable field: textColor
		private IntPtr _textColor;

/// The color to use for this button's text.
/// 
/// The button's [Material.textStyle] will be the current theme's button text
/// style, [TextTheme.labelLarge] of [ThemeData.textTheme], configured with this
/// color.
/// 
/// The default text color depends on the button theme's text theme,
/// [ButtonThemeData.textTheme].
/// 
/// If [textColor] is a [WidgetStateProperty<Color>], [disabledTextColor]
/// will be ignored.
/// 
/// See also:
/// 
/// * [disabledTextColor], the text color to use when the button has been
/// disabled.
		public IntPtr textColor
		{
			get => _textColor;
			set { _textColor = value; HastextColor = 1; }
		}

		// Has flag for nullable property: disabledTextColor
		public byte HasdisabledTextColor { get; set; }

		// Nullable field: disabledTextColor
		private IntPtr _disabledTextColor;

/// The color to use for this button's text when the button is disabled.
/// 
/// The button's [Material.textStyle] will be the current theme's button text
/// style, [TextTheme.labelLarge] of [ThemeData.textTheme], configured with this
/// color.
/// 
/// The default value is the theme's disabled color,
/// [ThemeData.disabledColor].
/// 
/// If [textColor] is a [WidgetStateProperty<Color>], [disabledTextColor]
/// will be ignored.
/// 
/// See also:
/// 
/// * [textColor] - The color to use for this button's text when the button is [enabled].
		public IntPtr disabledTextColor
		{
			get => _disabledTextColor;
			set { _disabledTextColor = value; HasdisabledTextColor = 1; }
		}

		// Has flag for nullable property: color
		public byte Hascolor { get; set; }

		// Nullable field: color
		private IntPtr _color;

/// The button's fill color, displayed by its [Material], while it
/// is in its default (unpressed, [enabled]) state.
/// 
/// See also:
/// 
/// * [disabledColor] - the fill color of the button when the button is disabled.
		public IntPtr color
		{
			get => _color;
			set { _color = value; Hascolor = 1; }
		}

		// Has flag for nullable property: disabledColor
		public byte HasdisabledColor { get; set; }

		// Nullable field: disabledColor
		private IntPtr _disabledColor;

/// The fill color of the button when the button is disabled.
/// 
/// The default value of this color is the theme's disabled color,
/// [ThemeData.disabledColor].
/// 
/// See also:
/// 
/// * [color] - the fill color of the button when the button is [enabled].
		public IntPtr disabledColor
		{
			get => _disabledColor;
			set { _disabledColor = value; HasdisabledColor = 1; }
		}

		// Has flag for nullable property: splashColor
		public byte HassplashColor { get; set; }

		// Nullable field: splashColor
		private IntPtr _splashColor;

/// The splash color of the button's [InkWell].
/// 
/// The ink splash indicates that the button has been touched. It
/// appears on top of the button's child and spreads in an expanding
/// circle beginning where the touch occurred.
/// 
/// The default splash color is the current theme's splash color,
/// [ThemeData.splashColor].
/// 
/// The appearance of the splash can be configured with the theme's splash
/// factory, [ThemeData.splashFactory].
		public IntPtr splashColor
		{
			get => _splashColor;
			set { _splashColor = value; HassplashColor = 1; }
		}

		// Has flag for nullable property: focusColor
		public byte HasfocusColor { get; set; }

		// Nullable field: focusColor
		private IntPtr _focusColor;

/// The fill color of the button's [Material] when it has the input focus.
/// 
/// The button changed focus color when the button has the input focus. It
/// appears behind the button's child.
		public IntPtr focusColor
		{
			get => _focusColor;
			set { _focusColor = value; HasfocusColor = 1; }
		}

		// Has flag for nullable property: hoverColor
		public byte HashoverColor { get; set; }

		// Nullable field: hoverColor
		private IntPtr _hoverColor;

/// The fill color of the button's [Material] when a pointer is hovering over
/// it.
/// 
/// The button changes fill color when a pointer is hovering over the button.
/// It appears behind the button's child.
		public IntPtr hoverColor
		{
			get => _hoverColor;
			set { _hoverColor = value; HashoverColor = 1; }
		}

		// Has flag for nullable property: highlightColor
		public byte HashighlightColor { get; set; }

		// Nullable field: highlightColor
		private IntPtr _highlightColor;

/// The highlight color of the button's [InkWell].
/// 
/// The highlight indicates that the button is actively being pressed. It
/// appears on top of the button's child and quickly spreads to fill
/// the button, and then fades out.
/// 
/// If [textTheme] is [ButtonTextTheme.primary], the default highlight color is
/// transparent (in other words the highlight doesn't appear). Otherwise it's
/// the current theme's highlight color, [ThemeData.highlightColor].
		public IntPtr highlightColor
		{
			get => _highlightColor;
			set { _highlightColor = value; HashighlightColor = 1; }
		}

		// Has flag for nullable property: elevation
		public byte Haselevation { get; set; }

		// Nullable field: elevation
		private double _elevation;

/// The z-coordinate at which to place this button relative to its parent.
/// 
/// This controls the size of the shadow below the raised button.
/// 
/// Defaults to 2, the appropriate elevation for raised buttons. The value
/// is always non-negative.
/// 
/// See also:
/// 
/// * [TextButton], a button with no elevation or fill color.
/// * [focusElevation], the elevation when the button is focused.
/// * [hoverElevation], the elevation when a pointer is hovering over the
/// button.
/// * [disabledElevation], the elevation when the button is disabled.
/// * [highlightElevation], the elevation when the button is pressed.
		public double elevation
		{
			get => _elevation;
			set { _elevation = value; Haselevation = 1; }
		}

		// Has flag for nullable property: hoverElevation
		public byte HashoverElevation { get; set; }

		// Nullable field: hoverElevation
		private double _hoverElevation;

/// The elevation for the button's [Material] when the button
/// is [enabled] and a pointer is hovering over it.
/// 
/// Defaults to 4.0. The value is always non-negative.
/// 
/// See also:
/// 
/// * [elevation], the default elevation.
/// * [focusElevation], the elevation when the button is focused.
/// * [disabledElevation], the elevation when the button is disabled.
/// * [highlightElevation], the elevation when the button is pressed.
		public double hoverElevation
		{
			get => _hoverElevation;
			set { _hoverElevation = value; HashoverElevation = 1; }
		}

		// Has flag for nullable property: focusElevation
		public byte HasfocusElevation { get; set; }

		// Nullable field: focusElevation
		private double _focusElevation;

/// The elevation for the button's [Material] when the button
/// is [enabled] and has the input focus.
/// 
/// Defaults to 4.0. The value is always non-negative.
/// 
/// See also:
/// 
/// * [elevation], the default elevation.
/// * [hoverElevation], the elevation when a pointer is hovering over the
/// button.
/// * [disabledElevation], the elevation when the button is disabled.
/// * [highlightElevation], the elevation when the button is pressed.
		public double focusElevation
		{
			get => _focusElevation;
			set { _focusElevation = value; HasfocusElevation = 1; }
		}

		// Has flag for nullable property: highlightElevation
		public byte HashighlightElevation { get; set; }

		// Nullable field: highlightElevation
		private double _highlightElevation;

/// The elevation for the button's [Material] relative to its parent when the
/// button is [enabled] and pressed.
/// 
/// This controls the size of the shadow below the button. When a tap
/// down gesture occurs within the button, its [InkWell] displays a
/// [highlightColor] "highlight".
/// 
/// Defaults to 8.0. The value is always non-negative.
/// 
/// See also:
/// 
/// * [elevation], the default elevation.
/// * [focusElevation], the elevation when the button is focused.
/// * [hoverElevation], the elevation when a pointer is hovering over the
/// button.
/// * [disabledElevation], the elevation when the button is disabled.
		public double highlightElevation
		{
			get => _highlightElevation;
			set { _highlightElevation = value; HashighlightElevation = 1; }
		}

		// Has flag for nullable property: disabledElevation
		public byte HasdisabledElevation { get; set; }

		// Nullable field: disabledElevation
		private double _disabledElevation;

/// The elevation for the button's [Material] relative to its parent when the
/// button is not [enabled].
/// 
/// Defaults to 0.0. The value is always non-negative.
/// 
/// See also:
/// 
/// * [elevation], the default elevation.
/// * [highlightElevation], the elevation when the button is pressed.
		public double disabledElevation
		{
			get => _disabledElevation;
			set { _disabledElevation = value; HasdisabledElevation = 1; }
		}

		// Has flag for nullable property: colorBrightness
		public byte HascolorBrightness { get; set; }

		// Nullable field: colorBrightness
		private Brightness _colorBrightness;

/// The theme brightness to use for this button.
/// 
/// Defaults to the theme's brightness in [ThemeData.brightness]. Setting
/// this value determines the button text's colors based on
/// [ButtonThemeData.getTextColor].
/// 
/// See also:
/// 
/// * [ButtonTextTheme], uses [Brightness] to determine text color.
		public Brightness colorBrightness
		{
			get => _colorBrightness;
			set { _colorBrightness = value; HascolorBrightness = 1; }
		}

		// Has flag for nullable property: padding
		public byte Haspadding { get; set; }

		// Nullable field: padding
		private IntPtr _padding;

/// The internal padding for the button's [child].
/// 
/// Defaults to the value from the current [ButtonTheme],
/// [ButtonThemeData.padding].
		public IntPtr padding
		{
			get => _padding;
			set { _padding = value; Haspadding = 1; }
		}

		// Has flag for nullable property: visualDensity
		public byte HasvisualDensity { get; set; }

		// Nullable field: visualDensity
		private IntPtr _visualDensity;

/// Defines how compact the button's layout will be.
/// 
/// {@macro flutter.material.themedata.visualDensity}
/// 
/// See also:
/// 
/// * [ThemeData.visualDensity], which specifies the [visualDensity] for all
/// widgets within a [Theme].
		public IntPtr visualDensity
		{
			get => _visualDensity;
			set { _visualDensity = value; HasvisualDensity = 1; }
		}

		// Has flag for nullable property: shape
		public byte Hasshape { get; set; }

		// Nullable field: shape
		private IntPtr _shape;

/// The shape of the button's [Material].
/// 
/// The button's highlight and splash are clipped to this shape. If the
/// button has an elevation, then its drop shadow is defined by this
/// shape as well.
/// 
/// Defaults to the value from the current [ButtonTheme],
/// [ButtonThemeData.shape].
		public IntPtr shape
		{
			get => _shape;
			set { _shape = value; Hasshape = 1; }
		}

		// Simple field: clipBehavior
/// {@macro flutter.material.Material.clipBehavior}
/// 
/// Defaults to [Clip.none].
		public Clip clipBehavior { get; set; }

		// Has flag for nullable property: focusNode
		public byte HasfocusNode { get; set; }

		// Nullable field: focusNode
		private IntPtr _focusNode;

/// {@macro flutter.widgets.Focus.focusNode}
		public IntPtr focusNode
		{
			get => _focusNode;
			set { _focusNode = value; HasfocusNode = 1; }
		}

		// Simple field: autofocus
/// {@macro flutter.widgets.Focus.autofocus}
		public bool autofocus { get; set; }

		// Has flag for nullable property: animationDuration
		public byte HasanimationDuration { get; set; }

		// Nullable field: animationDuration
		private IntPtr _animationDuration;

/// Defines the duration of animated changes for [shape] and [elevation].
/// 
/// The default value is [kThemeChangeDuration].
		public IntPtr animationDuration
		{
			get => _animationDuration;
			set { _animationDuration = value; HasanimationDuration = 1; }
		}

		// Has flag for nullable property: materialTapTargetSize
		public byte HasmaterialTapTargetSize { get; set; }

		// Nullable field: materialTapTargetSize
		private MaterialTapTargetSize _materialTapTargetSize;

/// Configures the minimum size of the tap target.
/// 
/// Defaults to [ThemeData.materialTapTargetSize].
/// 
/// See also:
/// 
/// * [MaterialTapTargetSize], for a description of how this affects tap targets.
		public MaterialTapTargetSize materialTapTargetSize
		{
			get => _materialTapTargetSize;
			set { _materialTapTargetSize = value; HasmaterialTapTargetSize = 1; }
		}

		// Has flag for nullable property: minWidth
		public byte HasminWidth { get; set; }

		// Nullable field: minWidth
		private double _minWidth;

/// The smallest horizontal extent that the button will occupy.
/// 
/// Defaults to the value from the current [ButtonTheme].
		public double minWidth
		{
			get => _minWidth;
			set { _minWidth = value; HasminWidth = 1; }
		}

		// Has flag for nullable property: height
		public byte Hasheight { get; set; }

		// Nullable field: height
		private double _height;

/// The vertical extent of the button.
/// 
/// Defaults to the value from the current [ButtonTheme].
		public double height
		{
			get => _height;
			set { _height = value; Hasheight = 1; }
		}

		// Simple field: enableFeedback
/// Whether detected gestures should provide acoustic and/or haptic feedback.
/// 
/// For example, on Android a tap will produce a clicking sound and a
/// long-press will produce a short vibration, when feedback is enabled.
/// 
/// See also:
/// 
/// * [Feedback] for providing platform-specific feedback to certain actions.
		public bool enableFeedback { get; set; }

		// Simple field: enabled
		public bool enabled { get; set; }

	}
}
