// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// An iOS-styled bottom navigation tab bar.
/// 
/// Displays multiple tabs using [BottomNavigationBarItem] with one tab being
/// active, the first tab by default.
/// 
/// This [StatelessWidget] doesn't store the active tab itself. You must
/// listen to the [onTap] callbacks and call `setState` with a new [currentIndex]
/// for the new selection to reflect. This can also be done automatically
/// by wrapping this with a [CupertinoTabScaffold].
/// 
/// Tab changes typically trigger a switch between [Navigator]s, each with its
/// own navigation stack, per standard iOS design. This can be done by using
/// [CupertinoTabView]s inside each tab builder in [CupertinoTabScaffold].
/// 
/// If the given [backgroundColor]'s opacity is not 1.0 (which is the case by
/// default), it will produce a blurring effect to the content behind it.
/// 
/// When used as [CupertinoTabScaffold.tabBar], by default [CupertinoTabBar]
/// disables text scaling to match the native iOS behavior. To override
/// this behavior, wrap each of the `navigationBar`'s components inside a
/// [MediaQuery] with the desired [TextScaler].
/// 
/// {@tool dartpad}
/// This example shows a [CupertinoTabBar] placed in a [CupertinoTabScaffold].
/// 
/// ** See code in examples/api/lib/cupertino/bottom_tab_bar/cupertino_tab_bar.0.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [CupertinoTabScaffold], which hosts the [CupertinoTabBar] at the bottom.
/// * [BottomNavigationBarItem], an item in a [CupertinoTabBar].
/// * <https://developer.apple.com/design/human-interface-guidelines/ios/bars/tab-bars/>
	[StructLayout(LayoutKind.Sequential)]
	internal class CupertinoTabBarStruct : WidgetStruct
	{
		// Simple field: items
/// The interactive items laid out within the bottom navigation bar.
		public IntPtr items { get; set; }

		// Has flag for nullable property: onTap
		public byte HasonTap { get; set; }

		// Callback field: onTap
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onTap;

/// The callback that is called when a item is tapped.
/// 
/// The widget creating the bottom navigation bar needs to keep track of the
/// current index and call `setState` to rebuild it with the newly provided
/// index.
		/// <summary>
		/// Action identifier for onTap callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onTapAction
		{
			get => GetString(_onTap);
			set { SetString(ref _onTap, value); HasonTap = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: currentIndex
/// The index into [items] of the current active item.
/// 
/// Must be between 0 and the number of tabs minus 1, inclusive.
		public int currentIndex { get; set; }

		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Nullable field: backgroundColor
		private IntPtr _backgroundColor;

/// The background color of the tab bar. If it contains transparency, the
/// tab bar will automatically produce a blurring effect to the content
/// behind it.
/// 
/// Defaults to [CupertinoTheme]'s `barBackgroundColor` when null.
		public IntPtr backgroundColor
		{
			get => _backgroundColor;
			set { _backgroundColor = value; HasbackgroundColor = 1; }
		}

		// Has flag for nullable property: activeColor
		public byte HasactiveColor { get; set; }

		// Nullable field: activeColor
		private IntPtr _activeColor;

/// The foreground color of the icon and title for the [BottomNavigationBarItem]
/// of the selected tab.
/// 
/// Defaults to [CupertinoTheme]'s `primaryColor` if null.
		public IntPtr activeColor
		{
			get => _activeColor;
			set { _activeColor = value; HasactiveColor = 1; }
		}

		// Simple field: inactiveColor
/// The foreground color of the icon and title for the [BottomNavigationBarItem]s
/// in the unselected state.
/// 
/// Defaults to a [CupertinoDynamicColor] that matches the disabled foreground
/// color of the native `UITabBar` component.
		public IntPtr inactiveColor { get; set; }

		// Simple field: iconSize
/// The size of all of the [BottomNavigationBarItem] icons.
/// 
/// This value is used to configure the [IconTheme] for the navigation bar.
/// When a [BottomNavigationBarItem.icon] widget is not an [Icon] the widget
/// should configure itself to match the icon theme's size and color.
		public double iconSize { get; set; }

		// Simple field: height
/// The height of the [CupertinoTabBar].
/// 
/// Defaults to 50.
		public double height { get; set; }

		// Has flag for nullable property: border
		public byte Hasborder { get; set; }

		// Nullable field: border
		private IntPtr _border;

/// The border of the [CupertinoTabBar].
/// 
/// The default value is a one physical pixel top border with grey color.
		public IntPtr border
		{
			get => _border;
			set { _border = value; Hasborder = 1; }
		}

		// Simple field: preferredSize
		public IntPtr preferredSize { get; set; }

	}
}
