// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// Displays a menu when pressed and calls [onSelected] when the menu is dismissed
/// because an item was selected. The value passed to [onSelected] is the value of
/// the selected menu item.
/// 
/// One of [child] or [icon] may be provided, but not both. If [icon] is provided,
/// then [PopupMenuButton] behaves like an [IconButton].
/// 
/// If both are null, then a standard overflow icon is created (depending on the
/// platform).
/// 
/// ## Updating to [MenuAnchor]
/// 
/// There is a Material 3 component,
/// [MenuAnchor] that is preferred for applications that are configured
/// for Material 3 (see [ThemeData.useMaterial3]).
/// The [MenuAnchor] widget's visuals
/// are a little bit different, see the Material 3 spec at
/// <https://m3.material.io/components/menus/guidelines> for
/// more details.
/// 
/// The [MenuAnchor] widget's API is also slightly different.
/// [MenuAnchor]'s were built to be lower level interface for
/// creating menus that are displayed from an anchor.
/// 
/// There are a few steps you would take to migrate from
/// [PopupMenuButton] to [MenuAnchor]:
/// 
/// 1. Instead of using the [PopupMenuButton.itemBuilder] to build
/// a list of [PopupMenuEntry]s, you would use the [MenuAnchor.menuChildren]
/// which takes a list of [Widget]s. Usually, you would use a list of
/// [MenuItemButton]s as shown in the example below.
/// 
/// 2. Instead of using the [PopupMenuButton.onSelected] callback, you would
/// set individual callbacks for each of the [MenuItemButton]s using the
/// [MenuItemButton.onPressed] property.
/// 
/// 3. To anchor the [MenuAnchor] to a widget, you would use the [MenuAnchor.builder]
/// to return the widget of choice - usually a [TextButton] or an [IconButton].
/// 
/// 4. You may want to style the [MenuItemButton]s, see the [MenuItemButton]
/// documentation for details.
/// 
/// Use the sample below for an example of migrating from [PopupMenuButton] to
/// [MenuAnchor].
/// 
/// {@tool dartpad}
/// This example shows a menu with three items, selecting between an enum's
/// values and setting a `selectedMenu` field based on the selection.
/// 
/// ** See code in examples/api/lib/material/popup_menu/popup_menu.0.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This example shows how to migrate the above to a [MenuAnchor].
/// 
/// ** See code in examples/api/lib/material/menu_anchor/menu_anchor.2.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This sample shows the creation of a popup menu, as described in:
/// https://m3.material.io/components/menus/overview
/// 
/// ** See code in examples/api/lib/material/popup_menu/popup_menu.1.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This sample showcases how to override the [PopupMenuButton] animation
/// curves and duration using [AnimationStyle].
/// 
/// ** See code in examples/api/lib/material/popup_menu/popup_menu.2.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [PopupMenuItem], a popup menu entry for a single value.
/// * [PopupMenuDivider], a popup menu entry that is just a horizontal line.
/// * [CheckedPopupMenuItem], a popup menu item with a checkmark.
/// * [showMenu], a method to dynamically show a popup menu at a given location.
	[StructLayout(LayoutKind.Sequential)]
	internal class PopupMenuButtonStruct : SingleChildRenderObjectWidgetStruct
	{
		// Callback field: itemBuilder
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _itemBuilder;

/// Called when the button is pressed to create the items to show in the menu.
		/// <summary>
		/// Action identifier for itemBuilder callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? itemBuilderAction
		{
			get => GetString(_itemBuilder);
			set { SetString(ref _itemBuilder, value); }
		}

		// Has flag for nullable property: initialValue
		public byte HasinitialValue { get; set; }

		// Nullable field: initialValue
		private IntPtr _initialValue;

/// The value of the menu item, if any, that should be highlighted when the menu opens.
		public IntPtr initialValue
		{
			get => _initialValue;
			set { _initialValue = value; HasinitialValue = 1; }
		}

		// Has flag for nullable property: onOpened
		public byte HasonOpened { get; set; }

		// Callback field: onOpened
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onOpened;

/// Called when the popup menu is shown.
		/// <summary>
		/// Action identifier for onOpened callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onOpenedAction
		{
			get => GetString(_onOpened);
			set { SetString(ref _onOpened, value); HasonOpened = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onSelected
		public byte HasonSelected { get; set; }

		// Callback field: onSelected
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onSelected;

/// Called when the user selects a value from the popup menu created by this button.
/// 
/// If the popup menu is dismissed without selecting a value, [onCanceled] is
/// called instead.
		/// <summary>
		/// Action identifier for onSelected callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onSelectedAction
		{
			get => GetString(_onSelected);
			set { SetString(ref _onSelected, value); HasonSelected = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onCanceled
		public byte HasonCanceled { get; set; }

		// Callback field: onCanceled
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onCanceled;

/// Called when the user dismisses the popup menu without selecting an item.
/// 
/// If the user selects a value, [onSelected] is called instead.
		/// <summary>
		/// Action identifier for onCanceled callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onCanceledAction
		{
			get => GetString(_onCanceled);
			set { SetString(ref _onCanceled, value); HasonCanceled = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: tooltip
		public byte Hastooltip { get; set; }

		// String field: tooltip
		private IntPtr _tooltip;

/// Text that describes the action that will occur when the button is pressed.
/// 
/// This text is displayed when the user long-presses on the button and is
/// used for accessibility.
		public string? tooltip
		{
			get => GetString(_tooltip);
			set { SetString(ref _tooltip, value); Hastooltip = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: elevation
		public byte Haselevation { get; set; }

		// Nullable field: elevation
		private double _elevation;

/// The z-coordinate at which to place the menu when open. This controls the
/// size of the shadow below the menu.
/// 
/// Defaults to 8, the appropriate elevation for popup menus.
		public double elevation
		{
			get => _elevation;
			set { _elevation = value; Haselevation = 1; }
		}

		// Has flag for nullable property: shadowColor
		public byte HasshadowColor { get; set; }

		// Nullable field: shadowColor
		private IntPtr _shadowColor;

/// The color used to paint the shadow below the menu.
/// 
/// If null then the ambient [PopupMenuThemeData.shadowColor] is used.
/// If that is null too, then the overall theme's [ThemeData.shadowColor]
/// (default black) is used.
		public IntPtr shadowColor
		{
			get => _shadowColor;
			set { _shadowColor = value; HasshadowColor = 1; }
		}

		// Has flag for nullable property: surfaceTintColor
		public byte HassurfaceTintColor { get; set; }

		// Nullable field: surfaceTintColor
		private IntPtr _surfaceTintColor;

/// The color used as an overlay on [color] to indicate elevation.
/// 
/// This is not recommended for use. [Material 3 spec](https://m3.material.io/styles/color/the-color-system/color-roles)
/// introduced a set of tone-based surfaces and surface containers in its [ColorScheme],
/// which provide more flexibility. The intention is to eventually remove surface tint color from
/// the framework.
/// 
/// If null, [PopupMenuThemeData.surfaceTintColor] is used. If that
/// is also null, the default value is [Colors.transparent].
/// 
/// See [Material.surfaceTintColor] for more details on how this
/// overlay is applied.
		public IntPtr surfaceTintColor
		{
			get => _surfaceTintColor;
			set { _surfaceTintColor = value; HassurfaceTintColor = 1; }
		}

		// Simple field: padding
/// Matches IconButton's 8 dps padding by default. In some cases, notably where
/// this button appears as the trailing element of a list item, it's useful to be able
/// to set the padding to zero.
		public IntPtr padding { get; set; }

		// Has flag for nullable property: menuPadding
		public byte HasmenuPadding { get; set; }

		// Nullable field: menuPadding
		private IntPtr _menuPadding;

/// If provided, menu padding is used for empty space around the outside
/// of the popup menu.
/// 
/// If this property is null, then [PopupMenuThemeData.menuPadding] is used.
/// If [PopupMenuThemeData.menuPadding] is also null, then vertical padding
/// of 8 pixels is used.
		public IntPtr menuPadding
		{
			get => _menuPadding;
			set { _menuPadding = value; HasmenuPadding = 1; }
		}

		// Has flag for nullable property: splashRadius
		public byte HassplashRadius { get; set; }

		// Nullable field: splashRadius
		private double _splashRadius;

/// The splash radius.
/// 
/// If null, default splash radius of [InkWell] or [IconButton] is used.
		public double splashRadius
		{
			get => _splashRadius;
			set { _splashRadius = value; HassplashRadius = 1; }
		}

		// Has flag for nullable property: borderRadius
		public byte HasborderRadius { get; set; }

		// Nullable field: borderRadius
		private IntPtr _borderRadius;

/// The border radius for the [InkWell] that wraps the [child].
/// 
/// Defaults to null, which indicates no border radius should be applied.
		public IntPtr borderRadius
		{
			get => _borderRadius;
			set { _borderRadius = value; HasborderRadius = 1; }
		}

		// Has flag for nullable property: icon
		public byte Hasicon { get; set; }

		// Widget field: icon
		private IntPtr _icon;

/// If provided, the [icon] is used for this button
/// and the button will behave like an [IconButton].
		public IntPtr? icon
		{
			get => _icon != IntPtr.Zero ? (IntPtr)_icon : null;
			set { SetIntPtr(ref _icon, value); Hasicon = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: offset
/// The offset is applied relative to the initial position
/// set by the [position].
/// 
/// When not set, the offset defaults to [Offset.zero].
		public IntPtr offset { get; set; }

		// Simple field: enabled
/// Whether this popup menu button is interactive.
/// 
/// Defaults to true.
/// 
/// If true, the button will respond to presses by displaying the menu.
/// 
/// If false, the button is styled with the disabled color from the
/// current [Theme] and will not respond to presses or show the popup
/// menu and [onSelected], [onCanceled] and [itemBuilder] will not be called.
/// 
/// This can be useful in situations where the app needs to show the button,
/// but doesn't currently have anything to show in the menu.
		public bool enabled { get; set; }

		// Has flag for nullable property: shape
		public byte Hasshape { get; set; }

		// Nullable field: shape
		private IntPtr _shape;

/// If provided, the shape used for the menu.
/// 
/// If this property is null, then [PopupMenuThemeData.shape] is used.
/// If [PopupMenuThemeData.shape] is also null, then the default shape for
/// [MaterialType.card] is used. This default shape is a rectangle with
/// rounded edges of BorderRadius.circular(2.0).
		public IntPtr shape
		{
			get => _shape;
			set { _shape = value; Hasshape = 1; }
		}

		// Has flag for nullable property: color
		public byte Hascolor { get; set; }

		// Nullable field: color
		private IntPtr _color;

/// If provided, the background color used for the menu.
/// 
/// If this property is null, then [PopupMenuThemeData.color] is used.
/// If [PopupMenuThemeData.color] is also null, then
/// [ThemeData.cardColor] is used in Material 2. In Material3, defaults to
/// [ColorScheme.surfaceContainer].
		public IntPtr color
		{
			get => _color;
			set { _color = value; Hascolor = 1; }
		}

		// Has flag for nullable property: iconColor
		public byte HasiconColor { get; set; }

		// Nullable field: iconColor
		private IntPtr _iconColor;

/// If provided, this color is used for the button icon.
/// 
/// If this property is null, then [PopupMenuThemeData.iconColor] is used.
/// If [PopupMenuThemeData.iconColor] is also null then defaults to
/// [IconThemeData.color].
		public IntPtr iconColor
		{
			get => _iconColor;
			set { _iconColor = value; HasiconColor = 1; }
		}

		// Has flag for nullable property: enableFeedback
		public byte HasenableFeedback { get; set; }

		// Nullable field: enableFeedback
		private bool _enableFeedback;

/// Whether detected gestures should provide acoustic and/or haptic feedback.
/// 
/// For example, on Android a tap will produce a clicking sound and a
/// long-press will produce a short vibration, when feedback is enabled.
/// 
/// See also:
/// 
/// * [Feedback] for providing platform-specific feedback to certain actions.
		public bool enableFeedback
		{
			get => _enableFeedback;
			set { _enableFeedback = value; HasenableFeedback = 1; }
		}

		// Has flag for nullable property: iconSize
		public byte HasiconSize { get; set; }

		// Nullable field: iconSize
		private double _iconSize;

/// If provided, the size of the [Icon].
/// 
/// If this property is null, then [IconThemeData.size] is used.
/// If [IconThemeData.size] is also null, then
/// default size is 24.0 pixels.
		public double iconSize
		{
			get => _iconSize;
			set { _iconSize = value; HasiconSize = 1; }
		}

		// Has flag for nullable property: constraints
		public byte Hasconstraints { get; set; }

		// Nullable field: constraints
		private IntPtr _constraints;

/// Optional size constraints for the menu.
/// 
/// When unspecified, defaults to:
/// ```dart
/// const BoxConstraints(
/// minWidth: 2.0 * 56.0,
/// maxWidth: 5.0 * 56.0,
/// )
/// ```
/// 
/// The default constraints ensure that the menu width matches maximum width
/// recommended by the Material Design guidelines.
/// Specifying this parameter enables creation of menu wider than
/// the default maximum width.
		public IntPtr constraints
		{
			get => _constraints;
			set { _constraints = value; Hasconstraints = 1; }
		}

		// Has flag for nullable property: position
		public byte Hasposition { get; set; }

		// Nullable field: position
		private IntPtr _position;

/// Whether the popup menu is positioned over or under the popup menu button.
/// 
/// [offset] is used to change the position of the popup menu relative to the
/// position set by this parameter.
/// 
/// If this property is `null`, then [PopupMenuThemeData.position] is used. If
/// [PopupMenuThemeData.position] is also `null`, then the position defaults
/// to [PopupMenuPosition.over] which makes the popup menu appear directly
/// over the button that was used to create it.
		public IntPtr position
		{
			get => _position;
			set { _position = value; Hasposition = 1; }
		}

		// Simple field: clipBehavior
/// {@macro flutter.material.Material.clipBehavior}
/// 
/// The [clipBehavior] argument is used the clip shape of the menu.
/// 
/// Defaults to [Clip.none].
		public Clip clipBehavior { get; set; }

		// Simple field: useRootNavigator
/// Used to determine whether to push the menu to the [Navigator] furthest
/// from or nearest to the given `context`.
/// 
/// Defaults to false.
		public bool useRootNavigator { get; set; }

		// Has flag for nullable property: popUpAnimationStyle
		public byte HaspopUpAnimationStyle { get; set; }

		// Nullable field: popUpAnimationStyle
		private IntPtr _popUpAnimationStyle;

/// Used to override the default animation curves and durations of the popup
/// menu's open and close transitions.
/// 
/// If [AnimationStyle.curve] is provided, it will be used to override
/// the default popup animation curve. Otherwise, defaults to [Curves.linear].
/// 
/// If [AnimationStyle.reverseCurve] is provided, it will be used to
/// override the default popup animation reverse curve. Otherwise, defaults to
/// `Interval(0.0, 2.0 / 3.0)`.
/// 
/// If [AnimationStyle.duration] is provided, it will be used to override
/// the default popup animation duration. Otherwise, defaults to 300ms.
/// 
/// To disable the theme animation, use [AnimationStyle.noAnimation].
/// 
/// If this is null, then the default animation will be used.
		public IntPtr popUpAnimationStyle
		{
			get => _popUpAnimationStyle;
			set { _popUpAnimationStyle = value; HaspopUpAnimationStyle = 1; }
		}

		// Has flag for nullable property: routeSettings
		public byte HasrouteSettings { get; set; }

		// Nullable field: routeSettings
		private IntPtr _routeSettings;

/// Optional route settings for the menu.
/// 
/// See [RouteSettings] for details.
		public IntPtr routeSettings
		{
			get => _routeSettings;
			set { _routeSettings = value; HasrouteSettings = 1; }
		}

		// Has flag for nullable property: style
		public byte Hasstyle { get; set; }

		// Nullable field: style
		private IntPtr _style;

/// Customizes this icon button's appearance.
/// 
/// The [style] is only used for Material 3 [IconButton]s. If [ThemeData.useMaterial3]
/// is set to true, [style] is preferred for icon button customization, and any
/// parameters defined in [style] will override the same parameters in [IconButton].
/// 
/// Null by default.
		public IntPtr style
		{
			get => _style;
			set { _style = value; Hasstyle = 1; }
		}

		// Has flag for nullable property: requestFocus
		public byte HasrequestFocus { get; set; }

		// Nullable field: requestFocus
		private bool _requestFocus;

/// Whether to request focus when the menu appears.
/// 
/// If null, [Navigator.requestFocus] will be used instead.
		public bool requestFocus
		{
			get => _requestFocus;
			set { _requestFocus = value; HasrequestFocus = 1; }
		}

	}
}
