// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A simple Material Design dialog.
/// 
/// A simple dialog offers the user a choice between several options. A simple
/// dialog has an optional title that is displayed above the choices.
/// 
/// Choices are normally represented using [SimpleDialogOption] widgets. If
/// other widgets are used, see [contentPadding] for notes regarding the
/// conventions for obtaining the spacing expected by Material Design.
/// 
/// For dialogs that inform the user about a situation, consider using an
/// [AlertDialog].
/// 
/// Typically passed as the child widget to [showDialog], which displays the
/// dialog.
/// 
/// {@animation 350 622 https://flutter.github.io/assets-for-api-docs/assets/material/simple_dialog.mp4}
/// 
/// {@tool snippet}
/// 
/// In this example, the user is asked to select between two options. These
/// options are represented as an enum. The [showDialog] method here returns
/// a [Future] that completes to a value of that enum. If the user cancels
/// the dialog (e.g. by hitting the back button on Android, or tapping on the
/// mask behind the dialog) then the future completes with the null value.
/// 
/// The return value in this example is used as the index for a switch statement.
/// One advantage of using an enum as the return value and then using that to
/// drive a switch statement is that the analyzer will flag any switch statement
/// that doesn't mention every value in the enum.
/// 
/// ```dart
/// Future<void> _askedToLead() async {
/// switch (await showDialog<Department>(
/// context: context,
/// builder: (BuildContext context) {
/// return SimpleDialog(
/// title: const Text('Select assignment'),
/// children: <Widget>[
/// SimpleDialogOption(
/// onPressed: () { Navigator.pop(context, Department.treasury); },
/// child: const Text('Treasury department'),
/// ),
/// SimpleDialogOption(
/// onPressed: () { Navigator.pop(context, Department.state); },
/// child: const Text('State department'),
/// ),
/// ],
/// );
/// }
/// )) {
/// case Department.treasury:
/// Let's go.
/// ...
/// break;
/// case Department.state:
/// ...
/// break;
/// case null:
/// dialog dismissed
/// break;
/// }
/// }
/// ```
/// {@end-tool}
/// 
/// See also:
/// 
/// * [SimpleDialogOption], which are options used in this type of dialog.
/// * [AlertDialog], for dialogs that have a row of buttons below the body.
/// * [Dialog], on which [SimpleDialog] and [AlertDialog] are based.
/// * [showDialog], which actually displays the dialog and returns its result.
/// * <https://material.io/design/components/dialogs.html#simple-dialog>
	[StructLayout(LayoutKind.Sequential)]
	internal class SimpleDialogStruct : MultiChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: title
		public byte Hastitle { get; set; }

		// Widget field: title
		private IntPtr _title;

/// The (optional) title of the dialog is displayed in a large font at the top
/// of the dialog.
/// 
/// Typically a [Text] widget.
		public IntPtr? title
		{
			get => _title != IntPtr.Zero ? (IntPtr)_title : null;
			set { SetIntPtr(ref _title, value); Hastitle = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: titlePadding
/// Padding around the title.
/// 
/// If there is no title, no padding will be provided.
/// 
/// By default, this provides the recommend Material Design padding of 24
/// pixels around the left, top, and right edges of the title.
/// 
/// See [contentPadding] for the conventions regarding padding between the
/// [title] and the [children].
		public IntPtr titlePadding { get; set; }

		// Has flag for nullable property: titleTextStyle
		public byte HastitleTextStyle { get; set; }

		// Nullable field: titleTextStyle
		private IntPtr _titleTextStyle;

/// Style for the text in the [title] of this [SimpleDialog].
/// 
/// If null, [DialogThemeData.titleTextStyle] is used. If that's null, defaults to
/// [TextTheme.titleLarge] of [ThemeData.textTheme].
		public IntPtr titleTextStyle
		{
			get => _titleTextStyle;
			set { _titleTextStyle = value; HastitleTextStyle = 1; }
		}

		// Simple field: contentPadding
/// Padding around the content.
/// 
/// By default, this is 12 pixels on the top and 16 pixels on the bottom. This
/// is intended to be combined with children that have 24 pixels of padding on
/// the left and right, and 8 pixels of padding on the top and bottom, so that
/// the content ends up being indented 20 pixels from the title, 24 pixels
/// from the bottom, and 24 pixels from the sides.
/// 
/// The [SimpleDialogOption] widget uses such padding.
/// 
/// If there is no [title], the [contentPadding] should be adjusted so that
/// the top padding ends up being 24 pixels.
		public IntPtr contentPadding { get; set; }

		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Nullable field: backgroundColor
		private IntPtr _backgroundColor;

/// {@macro flutter.material.dialog.backgroundColor}
		public IntPtr backgroundColor
		{
			get => _backgroundColor;
			set { _backgroundColor = value; HasbackgroundColor = 1; }
		}

		// Has flag for nullable property: elevation
		public byte Haselevation { get; set; }

		// Nullable field: elevation
		private double _elevation;

/// {@macro flutter.material.dialog.elevation}
		public double elevation
		{
			get => _elevation;
			set { _elevation = value; Haselevation = 1; }
		}

		// Has flag for nullable property: shadowColor
		public byte HasshadowColor { get; set; }

		// Nullable field: shadowColor
		private IntPtr _shadowColor;

/// {@macro flutter.material.dialog.shadowColor}
		public IntPtr shadowColor
		{
			get => _shadowColor;
			set { _shadowColor = value; HasshadowColor = 1; }
		}

		// Has flag for nullable property: surfaceTintColor
		public byte HassurfaceTintColor { get; set; }

		// Nullable field: surfaceTintColor
		private IntPtr _surfaceTintColor;

/// {@macro flutter.material.dialog.surfaceTintColor}
		public IntPtr surfaceTintColor
		{
			get => _surfaceTintColor;
			set { _surfaceTintColor = value; HassurfaceTintColor = 1; }
		}

		// Has flag for nullable property: semanticLabel
		public byte HassemanticLabel { get; set; }

		// String field: semanticLabel
		private IntPtr _semanticLabel;

/// The semantic label of the dialog used by accessibility frameworks to
/// announce screen transitions when the dialog is opened and closed.
/// 
/// If this label is not provided, a semantic label will be inferred from the
/// [title] if it is not null. If there is no title, the label will be taken
/// from [MaterialLocalizations.dialogLabel].
/// 
/// See also:
/// 
/// * [SemanticsConfiguration.namesRoute], for a description of how this
/// value is used.
		public string? semanticLabel
		{
			get => GetString(_semanticLabel);
			set { SetString(ref _semanticLabel, value); HassemanticLabel = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: insetPadding
		public byte HasinsetPadding { get; set; }

		// Nullable field: insetPadding
		private IntPtr _insetPadding;

/// {@macro flutter.material.dialog.insetPadding}
		public IntPtr insetPadding
		{
			get => _insetPadding;
			set { _insetPadding = value; HasinsetPadding = 1; }
		}

		// Has flag for nullable property: clipBehavior
		public byte HasclipBehavior { get; set; }

		// Nullable field: clipBehavior
		private Clip _clipBehavior;

/// {@macro flutter.material.dialog.clipBehavior}
		public Clip clipBehavior
		{
			get => _clipBehavior;
			set { _clipBehavior = value; HasclipBehavior = 1; }
		}

		// Has flag for nullable property: shape
		public byte Hasshape { get; set; }

		// Nullable field: shape
		private IntPtr _shape;

/// {@macro flutter.material.dialog.shape}
		public IntPtr shape
		{
			get => _shape;
			set { _shape = value; Hasshape = 1; }
		}

		// Has flag for nullable property: alignment
		public byte Hasalignment { get; set; }

		// Nullable field: alignment
		private IntPtr _alignment;

/// {@macro flutter.material.dialog.shape}
		public IntPtr alignment
		{
			get => _alignment;
			set { _alignment = value; Hasalignment = 1; }
		}

		// Has flag for nullable property: constraints
		public byte Hasconstraints { get; set; }

		// Nullable field: constraints
		private IntPtr _constraints;

/// {@macro flutter.material.dialog.constraints}
		public IntPtr constraints
		{
			get => _constraints;
			set { _constraints = value; Hasconstraints = 1; }
		}

	}
}
