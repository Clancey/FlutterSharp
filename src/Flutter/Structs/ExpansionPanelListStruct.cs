// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A material expansion panel list that lays out its children and animates
/// expansions.
/// 
/// The [expansionCallback] is called when the expansion state changes. For
/// normal [ExpansionPanelList] widgets, it is the responsibility of the parent
/// widget to rebuild the [ExpansionPanelList] with updated values for
/// [ExpansionPanel.isExpanded]. For [ExpansionPanelList.radio] widgets, the
/// open state is tracked internally and the callback is invoked both for the
/// previously open panel, which is closing, and the previously closed panel,
/// which is opening.
/// 
/// {@tool dartpad}
/// Here is a simple example of how to use [ExpansionPanelList].
/// 
/// ** See code in examples/api/lib/material/expansion_panel/expansion_panel_list.0.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [ExpansionPanel], which is used in the [children] property.
/// * [ExpansionPanelList.radio], a variant of this widget where only one panel is open at a time.
/// * <https://material.io/design/components/lists.html#types>
	[StructLayout(LayoutKind.Sequential)]
	internal class ExpansionPanelListStruct : MultiChildRenderObjectWidgetStruct
	{
		// Has flag for nullable property: expansionCallback
		public byte HasexpansionCallback { get; set; }

		// Callback field: expansionCallback
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _expansionCallback;

/// The callback that gets called whenever one of the expand/collapse buttons
/// is pressed. The arguments passed to the callback are the index of the
/// pressed panel and whether the panel is currently expanded or not.
/// 
/// If [ExpansionPanelList.radio] is used, the callback may be called a
/// second time if a different panel was previously open. The arguments
/// passed to the second callback are the index of the panel that will close
/// and false, marking that it will be closed.
/// 
/// For [ExpansionPanelList], the callback should call [State.setState] when
/// it is notified about the closing/opening panel. On the other hand, the
/// callback for [ExpansionPanelList.radio] is intended to inform the parent
/// widget of changes, as the radio panels' open/close states are managed
/// internally.
/// 
/// This callback is useful in order to keep track of the expanded/collapsed
/// panels in a parent widget that may need to react to these changes.
		/// <summary>
		/// Action identifier for expansionCallback callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? expansionCallbackAction
		{
			get => GetString(_expansionCallback);
			set { SetString(ref _expansionCallback, value); HasexpansionCallback = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: animationDuration
/// The duration of the expansion animation.
		public IntPtr animationDuration { get; set; }

		// Has flag for nullable property: initialOpenPanelValue
		public byte HasinitialOpenPanelValue { get; set; }

		// Nullable field: initialOpenPanelValue
		private IntPtr _initialOpenPanelValue;

/// The value of the panel that initially begins open. (This value is
/// only used when initializing with the [ExpansionPanelList.radio]
/// constructor.)
		public IntPtr initialOpenPanelValue
		{
			get => _initialOpenPanelValue;
			set { _initialOpenPanelValue = value; HasinitialOpenPanelValue = 1; }
		}

		// Simple field: expandedHeaderPadding
/// The padding that surrounds the panel header when expanded.
/// 
/// By default, 16px of space is added to the header vertically (above and below)
/// during expansion.
		public IntPtr expandedHeaderPadding { get; set; }

		// Has flag for nullable property: dividerColor
		public byte HasdividerColor { get; set; }

		// Nullable field: dividerColor
		private IntPtr _dividerColor;

/// Defines color for the divider when [ExpansionPanel.isExpanded] is false.
/// 
/// If [dividerColor] is null, then [DividerThemeData.color] is used. If that
/// is null, then [ThemeData.dividerColor] is used.
		public IntPtr dividerColor
		{
			get => _dividerColor;
			set { _dividerColor = value; HasdividerColor = 1; }
		}

		// Simple field: elevation
/// Defines elevation for the [ExpansionPanel] while it's expanded.
/// 
/// By default, the value of elevation is 2.
		public double elevation { get; set; }

		// Has flag for nullable property: expandIconColor
		public byte HasexpandIconColor { get; set; }

		// Nullable field: expandIconColor
		private IntPtr _expandIconColor;

/// {@macro flutter.material.ExpandIcon.color}
		public IntPtr expandIconColor
		{
			get => _expandIconColor;
			set { _expandIconColor = value; HasexpandIconColor = 1; }
		}

		// Simple field: materialGapSize
/// Defines the [MaterialGap.size] of the [MaterialGap] which is placed
/// between the [ExpansionPanelList.children] when they're expanded.
/// 
/// Defaults to `16.0`.
		public double materialGapSize { get; set; }

	}
}
