// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// Manages a "search view" route that allows the user to select one of the
/// suggested completions for a search query.
/// 
/// The search view's route can either be shown by creating a [SearchController]
/// and then calling [SearchController.openView] or by tapping on an anchor.
/// When the anchor is tapped or [SearchController.openView] is called, the search view either
/// grows to a specific size, or grows to fill the entire screen. By default,
/// the search view only shows full screen on mobile platforms. Use [SearchAnchor.isFullScreen]
/// to override the default setting.
/// 
/// The search view is usually opened by a [SearchBar], an [IconButton] or an [Icon].
/// If [builder] returns an Icon, or any un-tappable widgets, we don't have
/// to explicitly call [SearchController.openView].
/// 
/// The search view route will be popped if the window size is changed and the
/// search view route is not in full-screen mode. However, if the search view route
/// is in full-screen mode, changing the window size, such as rotating a mobile
/// device from portrait mode to landscape mode, will not close the search view.
/// 
/// {@tool dartpad}
/// This example shows how to use an IconButton to open a search view in a [SearchAnchor].
/// It also shows how to use [SearchController] to open or close the search view route.
/// 
/// ** See code in examples/api/lib/material/search_anchor/search_anchor.2.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This example shows how to set up a floating (or pinned) AppBar with a
/// [SearchAnchor] for a title.
/// 
/// ** See code in examples/api/lib/material/search_anchor/search_anchor.1.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This example shows how to fetch the search suggestions from a remote API.
/// 
/// ** See code in examples/api/lib/material/search_anchor/search_anchor.3.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This example demonstrates fetching the search suggestions asynchronously and
/// debouncing network calls.
/// 
/// ** See code in examples/api/lib/material/search_anchor/search_anchor.4.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [SearchBar], a widget that defines a search bar.
/// * [SearchBarTheme], a widget that overrides the default configuration of a search bar.
/// * [SearchViewTheme], a widget that overrides the default configuration of a search view.
	[StructLayout(LayoutKind.Sequential)]
	internal class SearchAnchorStruct : WidgetStruct
	{
		// Has flag for nullable property: isFullScreen
		public byte HasisFullScreen { get; set; }

		// Nullable field: isFullScreen
		private bool _isFullScreen;

/// Whether the search view grows to fill the entire screen when the
/// [SearchAnchor] is tapped.
/// 
/// By default, the search view is full-screen on mobile devices. On other
/// platforms, the search view only grows to a specific size that is determined
/// by the anchor and the default size.
		public bool isFullScreen
		{
			get => _isFullScreen;
			set { _isFullScreen = value; HasisFullScreen = 1; }
		}

		// Has flag for nullable property: searchController
		public byte HassearchController { get; set; }

		// Nullable field: searchController
		private IntPtr _searchController;

/// An optional controller that allows opening and closing of the search view from
/// other widgets.
/// 
/// If this is null, one internal search controller is created automatically
/// and it is used to open the search view when the user taps on the anchor.
		public IntPtr searchController
		{
			get => _searchController;
			set { _searchController = value; HassearchController = 1; }
		}

		// Has flag for nullable property: viewBuilder
		public byte HasviewBuilder { get; set; }

		// Callback field: viewBuilder
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _viewBuilder;

/// Optional callback to obtain a widget to lay out the suggestion list of the
/// search view.
/// 
/// Default view uses a [ListView] with a vertical scroll direction.
		/// <summary>
		/// Action identifier for viewBuilder callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? viewBuilderAction
		{
			get => GetString(_viewBuilder);
			set { SetString(ref _viewBuilder, value); HasviewBuilder = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: viewLeading
		public byte HasviewLeading { get; set; }

		// Widget field: viewLeading
		private IntPtr _viewLeading;

/// An optional widget to display before the text input field when the search
/// view is open.
/// 
/// Typically the [viewLeading] widget is an [Icon] or an [IconButton].
/// 
/// Defaults to a back button which pops the view.
		public IntPtr? viewLeading
		{
			get => _viewLeading != IntPtr.Zero ? (IntPtr)_viewLeading : null;
			set { SetIntPtr(ref _viewLeading, value); HasviewLeading = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: viewTrailing
		public byte HasviewTrailing { get; set; }

		// Nullable field: viewTrailing
		private IntPtr _viewTrailing;

/// An optional widget list to display after the text input field when the search
/// view is open.
/// 
/// Typically the [viewTrailing] widget list only has one or two widgets.
/// 
/// Defaults to an icon button which clears the text in the input field.
		public IntPtr viewTrailing
		{
			get => _viewTrailing;
			set { _viewTrailing = value; HasviewTrailing = 1; }
		}

		// Has flag for nullable property: viewHintText
		public byte HasviewHintText { get; set; }

		// String field: viewHintText
		private IntPtr _viewHintText;

/// Text that is displayed when the search bar's input field is empty.
		public string? viewHintText
		{
			get => GetString(_viewHintText);
			set { SetString(ref _viewHintText, value); HasviewHintText = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: viewBackgroundColor
		public byte HasviewBackgroundColor { get; set; }

		// Nullable field: viewBackgroundColor
		private IntPtr _viewBackgroundColor;

/// The search view's background fill color.
/// 
/// If null, the value of [SearchViewThemeData.backgroundColor] will be used.
/// If this is also null, then the default value is [ColorScheme.surfaceContainerHigh].
		public IntPtr viewBackgroundColor
		{
			get => _viewBackgroundColor;
			set { _viewBackgroundColor = value; HasviewBackgroundColor = 1; }
		}

		// Has flag for nullable property: viewElevation
		public byte HasviewElevation { get; set; }

		// Nullable field: viewElevation
		private double _viewElevation;

/// The elevation of the search view's [Material].
/// 
/// If null, the value of [SearchViewThemeData.elevation] will be used. If this
/// is also null, then default value is 6.0.
		public double viewElevation
		{
			get => _viewElevation;
			set { _viewElevation = value; HasviewElevation = 1; }
		}

		// Has flag for nullable property: viewSurfaceTintColor
		public byte HasviewSurfaceTintColor { get; set; }

		// Nullable field: viewSurfaceTintColor
		private IntPtr _viewSurfaceTintColor;

/// The surface tint color of the search view's [Material].
/// 
/// This is not recommended for use. [Material 3 spec](https://m3.material.io/styles/color/the-color-system/color-roles)
/// introduced a set of tone-based surfaces and surface containers in its [ColorScheme],
/// which provide more flexibility. The intention is to eventually remove surface tint color from
/// the framework.
/// 
/// If null, the value of [SearchViewThemeData.surfaceTintColor] will be used.
/// If this is also null, then the default value is [ColorScheme.surfaceTint].
		public IntPtr viewSurfaceTintColor
		{
			get => _viewSurfaceTintColor;
			set { _viewSurfaceTintColor = value; HasviewSurfaceTintColor = 1; }
		}

		// Has flag for nullable property: viewSide
		public byte HasviewSide { get; set; }

		// Nullable field: viewSide
		private IntPtr _viewSide;

/// The color and weight of the search view's outline.
/// 
/// This value is combined with [viewShape] to create a shape decorated
/// with an outline. This will be ignored if the view is full-screen.
/// 
/// If null, the value of [SearchViewThemeData.side] will be used. If this is
/// also null, the search view doesn't have a side by default.
		public IntPtr viewSide
		{
			get => _viewSide;
			set { _viewSide = value; HasviewSide = 1; }
		}

		// Has flag for nullable property: viewShape
		public byte HasviewShape { get; set; }

		// Nullable field: viewShape
		private IntPtr _viewShape;

/// The shape of the search view's underlying [Material].
/// 
/// This shape is combined with [viewSide] to create a shape decorated
/// with an outline.
/// 
/// If null, the value of [SearchViewThemeData.shape] will be used.
/// If this is also null, then the default value is a rectangle shape for full-screen
/// mode and a [RoundedRectangleBorder] shape with a 28.0 radius otherwise.
		public IntPtr viewShape
		{
			get => _viewShape;
			set { _viewShape = value; HasviewShape = 1; }
		}

		// Has flag for nullable property: viewBarPadding
		public byte HasviewBarPadding { get; set; }

		// Nullable field: viewBarPadding
		private IntPtr _viewBarPadding;

/// The padding to use for the search view's search bar.
/// 
/// If null, then the default value is 8.0 horizontally.
		public IntPtr viewBarPadding
		{
			get => _viewBarPadding;
			set { _viewBarPadding = value; HasviewBarPadding = 1; }
		}

		// Has flag for nullable property: headerHeight
		public byte HasheaderHeight { get; set; }

		// Nullable field: headerHeight
		private double _headerHeight;

/// The height of the search field on the search view.
/// 
/// If null, the value of [SearchViewThemeData.headerHeight] will be used. If
/// this is also null, the default value is 56.0.
		public double headerHeight
		{
			get => _headerHeight;
			set { _headerHeight = value; HasheaderHeight = 1; }
		}

		// Has flag for nullable property: headerTextStyle
		public byte HasheaderTextStyle { get; set; }

		// Nullable field: headerTextStyle
		private IntPtr _headerTextStyle;

/// The style to use for the text being edited on the search view.
/// 
/// If null, defaults to the `bodyLarge` text style from the current [Theme].
/// The default text color is [ColorScheme.onSurface].
		public IntPtr headerTextStyle
		{
			get => _headerTextStyle;
			set { _headerTextStyle = value; HasheaderTextStyle = 1; }
		}

		// Has flag for nullable property: headerHintStyle
		public byte HasheaderHintStyle { get; set; }

		// Nullable field: headerHintStyle
		private IntPtr _headerHintStyle;

/// The style to use for the [viewHintText] on the search view.
/// 
/// If null, the value of [SearchViewThemeData.headerHintStyle] will be used.
/// If this is also null, the value of [headerTextStyle] will be used. If this is also null,
/// defaults to the `bodyLarge` text style from the current [Theme]. The default
/// text color is [ColorScheme.onSurfaceVariant].
		public IntPtr headerHintStyle
		{
			get => _headerHintStyle;
			set { _headerHintStyle = value; HasheaderHintStyle = 1; }
		}

		// Has flag for nullable property: dividerColor
		public byte HasdividerColor { get; set; }

		// Nullable field: dividerColor
		private IntPtr _dividerColor;

/// The color of the divider on the search view.
/// 
/// If this property is null, then [SearchViewThemeData.dividerColor] is used.
/// If that is also null, the default value is [ColorScheme.outline].
		public IntPtr dividerColor
		{
			get => _dividerColor;
			set { _dividerColor = value; HasdividerColor = 1; }
		}

		// Has flag for nullable property: viewConstraints
		public byte HasviewConstraints { get; set; }

		// Nullable field: viewConstraints
		private IntPtr _viewConstraints;

/// Optional size constraints for the search view.
/// 
/// By default, the search view has the same width as the anchor and is 2/3
/// the height of the screen. If the width and height of the view are within
/// the [viewConstraints], the view will show its default size. Otherwise,
/// the size of the view will be constrained by this property.
/// 
/// If null, the value of [SearchViewThemeData.constraints] will be used. If
/// this is also null, then the constraints defaults to:
/// ```dart
/// const BoxConstraints(minWidth: 360.0, minHeight: 240.0)
/// ```
		public IntPtr viewConstraints
		{
			get => _viewConstraints;
			set { _viewConstraints = value; HasviewConstraints = 1; }
		}

		// Has flag for nullable property: viewPadding
		public byte HasviewPadding { get; set; }

		// Nullable field: viewPadding
		private IntPtr _viewPadding;

/// The padding to use for the search view.
/// 
/// Has no effect if the search view is full-screen.
/// 
/// If null, the value of [SearchViewThemeData.padding] will be used.
		public IntPtr viewPadding
		{
			get => _viewPadding;
			set { _viewPadding = value; HasviewPadding = 1; }
		}

		// Has flag for nullable property: shrinkWrap
		public byte HasshrinkWrap { get; set; }

		// Nullable field: shrinkWrap
		private bool _shrinkWrap;

/// Whether the search view should shrink-wrap its contents.
/// 
/// Has no effect if the search view is full-screen.
/// 
/// If null, the value of [SearchViewThemeData.shrinkWrap] will be used. If
/// this is also null, then the default value is `false`.
		public bool shrinkWrap
		{
			get => _shrinkWrap;
			set { _shrinkWrap = value; HasshrinkWrap = 1; }
		}

		// Has flag for nullable property: textCapitalization
		public byte HastextCapitalization { get; set; }

		// Nullable field: textCapitalization
		private TextCapitalization _textCapitalization;

/// {@macro flutter.widgets.editableText.textCapitalization}
		public TextCapitalization textCapitalization
		{
			get => _textCapitalization;
			set { _textCapitalization = value; HastextCapitalization = 1; }
		}

		// Has flag for nullable property: viewOnChanged
		public byte HasviewOnChanged { get; set; }

		// Callback field: viewOnChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _viewOnChanged;

/// Called each time the user modifies the search view's text field.
/// 
/// See also:
/// 
/// * [viewOnSubmitted], which is called when the user indicates that they
/// are done editing the search view's text field.
		/// <summary>
		/// Action identifier for viewOnChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? viewOnChangedAction
		{
			get => GetString(_viewOnChanged);
			set { SetString(ref _viewOnChanged, value); HasviewOnChanged = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: viewOnSubmitted
		public byte HasviewOnSubmitted { get; set; }

		// Callback field: viewOnSubmitted
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _viewOnSubmitted;

/// Called when the user indicates that they are done editing the text in the
/// text field of a search view. Typically this is called when the user presses
/// the enter key.
/// 
/// See also:
/// 
/// * [viewOnChanged], which is called when the user modifies the text field
/// of the search view.
		/// <summary>
		/// Action identifier for viewOnSubmitted callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? viewOnSubmittedAction
		{
			get => GetString(_viewOnSubmitted);
			set { SetString(ref _viewOnSubmitted, value); HasviewOnSubmitted = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: viewOnClose
		public byte HasviewOnClose { get; set; }

		// Callback field: viewOnClose
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _viewOnClose;

/// Called when the search view is closed.
		/// <summary>
		/// Action identifier for viewOnClose callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? viewOnCloseAction
		{
			get => GetString(_viewOnClose);
			set { SetString(ref _viewOnClose, value); HasviewOnClose = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: viewOnOpen
		public byte HasviewOnOpen { get; set; }

		// Callback field: viewOnOpen
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _viewOnOpen;

/// Called when the search view is opened.
		/// <summary>
		/// Action identifier for viewOnOpen callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? viewOnOpenAction
		{
			get => GetString(_viewOnOpen);
			set { SetString(ref _viewOnOpen, value); HasviewOnOpen = (byte)(value != null ? 1 : 0); }
		}

		// Callback field: builder
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _builder;

/// Called to create a widget which can open a search view route when it is tapped.
/// 
/// The widget returned by this builder is faded out when it is tapped.
/// At the same time a search view route is faded in.
		/// <summary>
		/// Action identifier for builder callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? builderAction
		{
			get => GetString(_builder);
			set { SetString(ref _builder, value); }
		}

		// Callback field: suggestionsBuilder
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _suggestionsBuilder;

/// Called to get the suggestion list for the search view.
/// 
/// By default, the list returned by this builder is laid out in a [ListView].
/// To get a different layout, use [viewBuilder] to override.
		/// <summary>
		/// Action identifier for suggestionsBuilder callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? suggestionsBuilderAction
		{
			get => GetString(_suggestionsBuilder);
			set { SetString(ref _suggestionsBuilder, value); }
		}

		// Has flag for nullable property: textInputAction
		public byte HastextInputAction { get; set; }

		// Nullable field: textInputAction
		private TextInputAction _textInputAction;

/// {@macro flutter.widgets.TextField.textInputAction}
		public TextInputAction textInputAction
		{
			get => _textInputAction;
			set { _textInputAction = value; HastextInputAction = 1; }
		}

		// Has flag for nullable property: keyboardType
		public byte HaskeyboardType { get; set; }

		// Nullable field: keyboardType
		private IntPtr _keyboardType;

/// The type of action button to use for the keyboard.
/// 
/// Defaults to the default value specified in [TextField].
		public IntPtr keyboardType
		{
			get => _keyboardType;
			set { _keyboardType = value; HaskeyboardType = 1; }
		}

		// Simple field: enabled
/// Whether or not this widget is currently interactive.
/// 
/// When false, the widget will ignore taps and appear dimmed.
/// 
/// Defaults to true.
		public bool enabled { get; set; }

	}
}
