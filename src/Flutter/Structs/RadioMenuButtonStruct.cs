// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A menu item that combines a [Radio] widget with a [MenuItemButton].
/// 
/// To style the radio button separately from the overall button, add a
/// [RadioTheme] ancestor.
/// 
/// {@tool dartpad}
/// This example shows a menu with three radio buttons with shortcuts that
/// changes the background color of the body when the buttons are selected.
/// 
/// ** See code in examples/api/lib/material/menu_anchor/radio_menu_button.0.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// - [MenuBar], a widget that creates a menu bar of cascading menu items.
/// - [MenuAnchor], a widget that defines a region which can host a cascading
/// menu.
	[StructLayout(LayoutKind.Sequential)]
	internal class RadioMenuButtonStruct : SingleChildRenderObjectWidgetStruct
	{
		// Simple field: value
/// The value represented by this radio button.
/// 
/// This radio button is considered selected if its [value] matches the
/// [groupValue].
		public IntPtr value { get; set; }

		// Has flag for nullable property: groupValue
		public byte HasgroupValue { get; set; }

		// Nullable field: groupValue
		private IntPtr _groupValue;

/// The currently selected value for a group of radio buttons.
/// 
/// This radio button is considered selected if its [value] matches the
/// [groupValue].
		public IntPtr groupValue
		{
			get => _groupValue;
			set { _groupValue = value; HasgroupValue = 1; }
		}

		// Simple field: toggleable
/// Set to true if this radio button is allowed to be returned to an
/// indeterminate state by selecting it again when selected.
/// 
/// To indicate returning to an indeterminate state, [onChanged] will be
/// called with null.
/// 
/// If true, [onChanged] is called with [value] when selected while
/// [groupValue] != [value], and with null when selected again while
/// [groupValue] == [value].
/// 
/// If false, [onChanged] will be called with [value] when it is selected
/// while [groupValue] != [value], and only by selecting another radio button
/// in the group (i.e. changing the value of [groupValue]) can this radio
/// button be unselected.
/// 
/// The default is false.
		public bool toggleable { get; set; }

		// Has flag for nullable property: onChanged
		public byte HasonChanged { get; set; }

		// Callback field: onChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onChanged;

/// Called when the user selects this radio button.
/// 
/// The radio button passes [value] as a parameter to this callback. The radio
/// button does not actually change state until the parent widget rebuilds the
/// radio button with the new [groupValue].
/// 
/// If null, the radio button will be displayed as disabled.
/// 
/// The provided callback will not be invoked if this radio button is already
/// selected.
/// 
/// The callback provided to [onChanged] should update the state of the parent
/// [StatefulWidget] using the [State.setState] method, so that the parent
/// gets rebuilt; for example:
/// 
/// ```dart
/// RadioMenuButton<SingingCharacter>(
/// value: SingingCharacter.lafayette,
/// groupValue: _character,
/// onChanged: (SingingCharacter? newValue) {
/// setState(() {
/// _character = newValue;
/// });
/// },
/// child: const Text('Lafayette'),
/// )
/// ```
		/// <summary>
		/// Action identifier for onChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onChangedAction
		{
			get => GetString(_onChanged);
			set { SetString(ref _onChanged, value); HasonChanged = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onHover
		public byte HasonHover { get; set; }

		// Callback field: onHover
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onHover;

/// Called when a pointer enters or exits the button response area.
/// 
/// The value passed to the callback is true if a pointer has entered button
/// area and false if a pointer has exited.
		/// <summary>
		/// Action identifier for onHover callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onHoverAction
		{
			get => GetString(_onHover);
			set { SetString(ref _onHover, value); HasonHover = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onFocusChange
		public byte HasonFocusChange { get; set; }

		// Callback field: onFocusChange
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onFocusChange;

/// Handler called when the focus changes.
/// 
/// Called with true if this widget's node gains focus, and false if it loses
/// focus.
		/// <summary>
		/// Action identifier for onFocusChange callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onFocusChangeAction
		{
			get => GetString(_onFocusChange);
			set { SetString(ref _onFocusChange, value); HasonFocusChange = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: focusNode
		public byte HasfocusNode { get; set; }

		// Nullable field: focusNode
		private IntPtr _focusNode;

/// {@macro flutter.widgets.Focus.focusNode}
		public IntPtr focusNode
		{
			get => _focusNode;
			set { _focusNode = value; HasfocusNode = 1; }
		}

		// Has flag for nullable property: shortcut
		public byte Hasshortcut { get; set; }

		// Nullable field: shortcut
		private IntPtr _shortcut;

/// The optional shortcut that selects this [MenuItemButton].
/// 
/// {@macro flutter.material.MenuBar.shortcuts_note}
		public IntPtr shortcut
		{
			get => _shortcut;
			set { _shortcut = value; Hasshortcut = 1; }
		}

		// Has flag for nullable property: style
		public byte Hasstyle { get; set; }

		// Nullable field: style
		private IntPtr _style;

/// Customizes this button's appearance.
/// 
/// Non-null properties of this style override the corresponding properties in
/// [MenuItemButton.themeStyleOf] and [MenuItemButton.defaultStyleOf].
/// [WidgetStateProperty]s that resolve to non-null values will similarly
/// override the corresponding [WidgetStateProperty]s in
/// [MenuItemButton.themeStyleOf] and [MenuItemButton.defaultStyleOf].
/// 
/// Null by default.
		public IntPtr style
		{
			get => _style;
			set { _style = value; Hasstyle = 1; }
		}

		// Has flag for nullable property: statesController
		public byte HasstatesController { get; set; }

		// Nullable field: statesController
		private IntPtr _statesController;

/// {@macro flutter.material.inkwell.statesController}
		public IntPtr statesController
		{
			get => _statesController;
			set { _statesController = value; HasstatesController = 1; }
		}

		// Simple field: clipBehavior
/// {@macro flutter.material.Material.clipBehavior}
/// 
/// Defaults to [Clip.none].
		public Clip clipBehavior { get; set; }

		// Has flag for nullable property: trailingIcon
		public byte HastrailingIcon { get; set; }

		// Widget field: trailingIcon
		private IntPtr _trailingIcon;

/// An optional icon to display after the [child] label.
		public IntPtr? trailingIcon
		{
			get => _trailingIcon != IntPtr.Zero ? (IntPtr)_trailingIcon : null;
			set { SetIntPtr(ref _trailingIcon, value); HastrailingIcon = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: closeOnActivate
/// {@macro flutter.material.menu_anchor.closeOnActivate}
		public bool closeOnActivate { get; set; }

		// Simple field: enabled
		public bool enabled { get; set; }

	}
}
