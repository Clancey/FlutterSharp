// <auto-generated>
// This file was manually created for FlutterSharp Phase 4 - ListTile implementation
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;

namespace Flutter.Structs
{
/// A single fixed-height row that typically contains some text as well as
/// a leading or trailing icon.
///
/// A list tile contains one to three lines of text optionally flanked by icons or
/// other widgets, such as check boxes. The icons (or other widgets) for the
/// tile are defined with the [leading] and [trailing] parameters. The first line
/// of text is not optional and is specified with [title]. The value of [subtitle],
/// which is optional, will occupy the space allocated for an additional line of
/// text, or two lines if [isThreeLine] is true.
///
/// List tiles are typically used in [ListView]s, or arranged in [Column]s in
/// [Drawer]s and [Card]s.
///
/// See also:
///  * [ListTileTheme], which defines visual properties for [ListTile]s.
///  * [ListView], which can display an arbitrary number of [ListTile]s.
///  * [Card], which can be used to display a single [ListTile].
///  * [Drawer], which can be used to display a list of [ListTile]s.
	[StructLayout(LayoutKind.Sequential)]
	internal class ListTileStruct : WidgetStruct
	{
		// Leading widget - displayed before the title
		/// A widget to display before the title.
		///
		/// Typically an [Icon] or a [CircleAvatar] widget.
		public IntPtr leading { get; set; }

		// Title widget - the primary content of the list tile
		/// The primary content of the list tile.
		///
		/// Typically a [Text] widget.
		///
		/// This should not wrap. To enforce the single line limit, use
		/// [Text.maxLines].
		public IntPtr title { get; set; }

		// Subtitle widget - displayed below the title
		/// Additional content displayed below the title.
		///
		/// Typically a [Text] widget.
		///
		/// If [isThreeLine] is false, this should not wrap.
		/// If [isThreeLine] is true, this should be configured to take a maximum of
		/// two lines. For example, you can use [Text.maxLines] to enforce the maximum
		/// number of lines.
		public IntPtr subtitle { get; set; }

		// Trailing widget - displayed after the title
		/// A widget to display after the title.
		///
		/// Typically an [Icon] widget.
		///
		/// To show right-aligned metadata (assuming left-to-right reading order;
		/// left-aligned for right-to-left reading order), consider using a [Row] with
		/// [CrossAxisAlignment.baseline] alignment whose first child is [Expanded]
		/// and whose second child is the metadata text, instead of using the
		/// [trailing] property.
		public IntPtr trailing { get; set; }

		// Has flag for nullable property: isThreeLine
		public byte HasisThreeLine { get; set; }

		// Boolean: isThreeLine
		/// Whether this list tile is intended to display three lines of text.
		///
		/// If true, then [subtitle] must be non-null (since it is expected to give
		/// the second and third lines of text).
		///
		/// If false, the list tile is treated as having one line if the subtitle is
		/// null and treated as having two lines if the subtitle is non-null.
		public bool isThreeLine { get; set; }

		// Has flag for nullable property: dense
		public byte Hasdense { get; set; }

		// Boolean: dense
		/// Whether this list tile is part of a vertically dense list.
		///
		/// If this property is null then its value is based on [ListTileThemeData.dense].
		///
		/// Dense list tiles default to a smaller height.
		public bool dense { get; set; }

		// Has flag for nullable property: enabled
		public byte Hasenabled { get; set; }

		// Boolean: enabled
		/// Whether this list tile is interactive.
		///
		/// If false, this list tile is styled with the disabled color from the
		/// current [Theme] and the [onTap] and [onLongPress] callbacks are
		/// inoperative.
		public bool enabled { get; set; }

		// Has flag for nullable property: selected
		public byte Hasselected { get; set; }

		// Boolean: selected
		/// If this tile is also [enabled] then icons and text are rendered with the same color.
		///
		/// By default the selected color is the theme's primary color. The selected color
		/// can be overridden with a [ListTileThemeData.selectedColor].
		public bool selected { get; set; }

		// Callback: onTap
		IntPtr _onTapAction;
		/// Called when the user taps this list tile.
		/// Inoperative if [enabled] is false.
		public string? onTapAction
		{
			get => GetString(_onTapAction);
			set => SetString(ref _onTapAction, value);
		}

		// Callback: onLongPress
		IntPtr _onLongPressAction;
		/// Called when the user long-presses on this list tile.
		/// Inoperative if [enabled] is false.
		public string? onLongPressAction
		{
			get => GetString(_onLongPressAction);
			set => SetString(ref _onLongPressAction, value);
		}

		// Callback: onFocusChange
		IntPtr _onFocusChangeAction;
		/// Called when the focus changes.
		public string? onFocusChangeAction
		{
			get => GetString(_onFocusChangeAction);
			set => SetString(ref _onFocusChangeAction, value);
		}

		// Color properties - stored as ARGB uint
		// Has flag for nullable property: tileColor
		public byte HastileColor { get; set; }
		/// Defines the background color of `ListTile` when [selected] is false.
		public uint tileColor { get; set; }

		// Has flag for nullable property: selectedTileColor
		public byte HasselectedTileColor { get; set; }
		/// Defines the background color of `ListTile` when [selected] is true.
		public uint selectedTileColor { get; set; }

		// Has flag for nullable property: iconColor
		public byte HasiconColor { get; set; }
		/// Defines the default color for [leading] and [trailing] icons.
		public uint iconColor { get; set; }

		// Has flag for nullable property: textColor
		public byte HastextColor { get; set; }
		/// Defines the default color for the [title] and [subtitle].
		public uint textColor { get; set; }

		// Has flag for nullable property: focusColor
		public byte HasfocusColor { get; set; }
		/// The color for the tile's [Material] when it has the input focus.
		public uint focusColor { get; set; }

		// Has flag for nullable property: hoverColor
		public byte HashoverColor { get; set; }
		/// The color for the tile's [Material] when a pointer is hovering over it.
		public uint hoverColor { get; set; }

		// Has flag for nullable property: splashColor
		public byte HassplashColor { get; set; }
		/// The color of splash for the tile's [Material].
		public uint splashColor { get; set; }

		// ContentPadding is a complex type (EdgeInsetsGeometry)
		/// The tile's internal padding.
		public IntPtr contentPadding { get; set; }

		// Has flag for nullable property: autofocus
		public byte Hasautofocus { get; set; }
		/// True if this widget will be selected as the initial focus when no other
		/// node in its scope is currently focused.
		public bool autofocus { get; set; }

		// Has flag for nullable property: enableFeedback
		public byte HasenableFeedback { get; set; }
		/// Whether detected gestures should provide acoustic and/or haptic feedback.
		public bool enableFeedback { get; set; }

		// Has flag for nullable property: horizontalTitleGap
		public byte HashorizontalTitleGap { get; set; }
		/// The horizontal gap between the titles and the leading/trailing widgets.
		public double horizontalTitleGap { get; set; }

		// Has flag for nullable property: minVerticalPadding
		public byte HasminVerticalPadding { get; set; }
		/// The minimum padding on the top and bottom of the title and subtitle widgets.
		public double minVerticalPadding { get; set; }

		// Has flag for nullable property: minLeadingWidth
		public byte HasminLeadingWidth { get; set; }
		/// The minimum width allocated for the [leading] widget.
		public double minLeadingWidth { get; set; }

		// Has flag for nullable property: minTileHeight
		public byte HasminTileHeight { get; set; }
		/// The minimum height allocated for the [ListTile] widget.
		public double minTileHeight { get; set; }

		// titleAlignment - enum
		/// Defines how [leading] and [trailing] are vertically aligned
		/// relative to the [ListTile]'s titles.
		public int titleAlignment { get; set; }

		// style - enum
		/// Defines the font used for the [title].
		public int style { get; set; }
	}
}
