// <auto-generated>
// This file was manually created for FlutterSharp Phase 4
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter;
using Flutter.Enums;
using Flutter.Widgets;
using Flutter.Material;

namespace Flutter.Structs
{
/// A Material Design floating action button.
///
/// A floating action button is a circular icon button that hovers over content
/// to promote a primary action in the application. Floating action buttons are
/// most commonly used in the Scaffold.floatingActionButton field.
///
/// Use at most a single floating action button per screen. Floating action
/// buttons should be used for positive actions such as "create", "share", or
/// "navigate". (If more than one floating action button is used within a
/// Route, then make sure that each button has a unique heroTag, otherwise
/// an exception will be thrown.)
///
/// If the onPressed callback is null, then the button will be disabled and
/// will not react to touch. It is highly discouraged to disable a floating
/// action button as there is no indication to the user that the button is
/// disabled. Consider changing the backgroundColor if disabling the floating
/// action button.
	[StructLayout(LayoutKind.Sequential)]
	internal class FloatingActionButtonStruct : WidgetStruct
	{
		// Has flag for nullable property: onPressed
		public byte HasonPressed { get; set; }

		// Callback field: onPressed
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onPressed;

/// Called when the button is tapped or otherwise activated.
///
/// If this is set to null, the button will be disabled.
		/// <summary>
		/// Action identifier for onPressed callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// </summary>
		public string? onPressedAction
		{
			get => GetString(_onPressed);
			set { SetString(ref _onPressed, value); HasonPressed = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: tooltip
		public byte Hastooltip { get; set; }

		// String field: tooltip
		private IntPtr _tooltip;

/// Text that describes the action that will occur when the button is pressed.
///
/// This text is displayed when the user long-presses on the button and is
/// used for accessibility.
		public string? tooltip
		{
			get => GetString(_tooltip);
			set { SetString(ref _tooltip, value); Hastooltip = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: foregroundColor
		public byte HasforegroundColor { get; set; }

		// Color field: foregroundColor
/// The default foreground color for icons and text within the button.
		public uint foregroundColor { get; set; }

		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Color field: backgroundColor
/// The button's background color.
		public uint backgroundColor { get; set; }

		// Has flag for nullable property: focusColor
		public byte HasfocusColor { get; set; }

		// Color field: focusColor
/// The color to use for filling the button when the button has input focus.
		public uint focusColor { get; set; }

		// Has flag for nullable property: hoverColor
		public byte HashoverColor { get; set; }

		// Color field: hoverColor
/// The color to use for filling the button when the button has a pointer hovering over it.
		public uint hoverColor { get; set; }

		// Has flag for nullable property: splashColor
		public byte HassplashColor { get; set; }

		// Color field: splashColor
/// The splash color for this FloatingActionButton's InkWell.
		public uint splashColor { get; set; }

		// Has flag for nullable property: heroTag
		public byte HasheroTag { get; set; }

		// String field: heroTag (object type, but we use string for simple cases)
		private IntPtr _heroTag;

/// The tag to apply to the button's Hero widget.
///
/// Defaults to a tag that matches other floating action buttons.
		public string? heroTag
		{
			get => GetString(_heroTag);
			set { SetString(ref _heroTag, value); HasheroTag = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: elevation
		public byte Haselevation { get; set; }

		// Double field: elevation
/// The z-coordinate at which to place this button relative to its parent.
///
/// This controls the size of the shadow below the floating action button.
		public double elevation { get; set; }

		// Has flag for nullable property: focusElevation
		public byte HasfocusElevation { get; set; }

		// Double field: focusElevation
/// The z-coordinate at which to place this button relative to its parent when
/// the button has the input focus.
		public double focusElevation { get; set; }

		// Has flag for nullable property: hoverElevation
		public byte HashoverElevation { get; set; }

		// Double field: hoverElevation
/// The z-coordinate at which to place this button relative to its parent when
/// the button is hovered.
		public double hoverElevation { get; set; }

		// Has flag for nullable property: highlightElevation
		public byte HashighlightElevation { get; set; }

		// Double field: highlightElevation
/// The z-coordinate at which to place this button relative to its parent when
/// the button is pressed.
		public double highlightElevation { get; set; }

		// Has flag for nullable property: disabledElevation
		public byte HasdisabledElevation { get; set; }

		// Double field: disabledElevation
/// The z-coordinate at which to place this button when the button is disabled.
		public double disabledElevation { get; set; }

		// Simple field: mini
/// Controls the size of this button.
///
/// By default, floating action buttons are non-mini and have a height and
/// width of 56.0 logical pixels. Mini floating action buttons have a height
/// and width of 40.0 logical pixels with a layout width and height of 48.0
/// logical pixels.
		public bool mini { get; set; }

		// Simple field: clipBehavior
/// The content will be clipped (or not) according to this option.
///
/// Defaults to Clip.none.
		public Clip clipBehavior { get; set; }

		// Simple field: autofocus
/// True if this widget will be selected as the initial focus when no other
/// node in its scope is currently focused.
///
/// Defaults to false.
		public bool autofocus { get; set; }

		// Simple field: isExtended
/// True if this is an "extended" floating action button.
///
/// Defaults to false.
		public bool isExtended { get; set; }

		// Has flag for nullable property: enableFeedback
		public byte HasenableFeedback { get; set; }

		// Bool field: enableFeedback
/// Whether detected gestures should provide acoustic and/or haptic feedback.
		public bool enableFeedback { get; set; }

		// Simple field: focusNode
/// An optional focus node to use as the focus node for this widget.
		public IntPtr focusNode { get; set; }

		// Simple field: child
/// The widget below this widget in the tree.
///
/// Typically an Icon.
		public IntPtr child { get; set; }
	}
}
