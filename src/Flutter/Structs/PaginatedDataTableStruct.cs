// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A table that follows the
/// [Material 2](https://material.io/go/design-data-tables)
/// design specification, using multiple pages to display data.
/// 
/// A paginated data table shows [rowsPerPage] rows of data per page and
/// provides controls for showing other pages.
/// 
/// Data is read lazily from a [DataTableSource]. The widget is presented
/// as a [Card].
/// 
/// If the [key] is a [PageStorageKey], the [initialFirstRowIndex] is persisted
/// to [PageStorage].
/// 
/// {@tool dartpad}
/// 
/// This sample shows how to display a [DataTable] with three columns: name,
/// age, and role. The columns are defined by three [DataColumn] objects. The
/// table contains three rows of data for three example users, the data for
/// which is defined by three [DataRow] objects.
/// 
/// ** See code in examples/api/lib/material/paginated_data_table/paginated_data_table.0.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// 
/// This example shows how paginated data tables can supported sorted data.
/// 
/// ** See code in examples/api/lib/material/paginated_data_table/paginated_data_table.1.dart **
/// {@end-tool}
/// 
/// See also:
/// 
/// * [DataTable], which is not paginated.
/// * `TableView` from the
/// [two_dimensional_scrollables](https://pub.dev/packages/two_dimensional_scrollables)
/// package, for displaying large amounts of data without pagination.
/// * <https://material.io/go/design-data-tables>
	[StructLayout(LayoutKind.Sequential)]
	internal class PaginatedDataTableStruct : WidgetStruct
	{
		// Has flag for nullable property: header
		public byte Hasheader { get; set; }

		// Widget field: header
		private IntPtr _header;

/// The table card's optional header.
/// 
/// This is typically a [Text] widget, but can also be a [Row] of
/// [TextButton]s. To show icon buttons at the top end side of the table with
/// a header, set the [actions] property.
/// 
/// If items in the table are selectable, then, when the selection is not
/// empty, the header is replaced by a count of the selected items. The
/// [actions] are still visible when items are selected.
		public IntPtr? header
		{
			get => _header != IntPtr.Zero ? (IntPtr)_header : null;
			set { SetIntPtr(ref _header, value); Hasheader = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: actions
		public byte Hasactions { get; set; }

		// Nullable field: actions
		private IntPtr _actions;

/// Icon buttons to show at the top end side of the table. The [header] must
/// not be null to show the actions.
/// 
/// Typically, the exact actions included in this list will vary based on
/// whether any rows are selected or not.
/// 
/// These should be size 24.0 with default padding (8.0).
		public IntPtr actions
		{
			get => _actions;
			set { _actions = value; Hasactions = 1; }
		}

		// Simple field: columns
/// The configuration and labels for the columns in the table.
		public IntPtr columns { get; set; }

		// Has flag for nullable property: sortColumnIndex
		public byte HassortColumnIndex { get; set; }

		// Nullable field: sortColumnIndex
		private int _sortColumnIndex;

/// The current primary sort key's column.
/// 
/// See [DataTable.sortColumnIndex] for details.
/// 
/// The direction of the sort is specified using [sortAscending].
		public int sortColumnIndex
		{
			get => _sortColumnIndex;
			set { _sortColumnIndex = value; HassortColumnIndex = 1; }
		}

		// Simple field: sortAscending
/// Whether the column mentioned in [sortColumnIndex], if any, is sorted
/// in ascending order.
/// 
/// See [DataTable.sortAscending] for details.
		public bool sortAscending { get; set; }

		// Has flag for nullable property: onSelectAll
		public byte HasonSelectAll { get; set; }

		// Callback field: onSelectAll
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onSelectAll;

/// Invoked when the user selects or unselects every row, using the
/// checkbox in the heading row.
/// 
/// See [DataTable.onSelectAll].
		/// <summary>
		/// Action identifier for onSelectAll callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onSelectAllAction
		{
			get => GetString(_onSelectAll);
			set { SetString(ref _onSelectAll, value); HasonSelectAll = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: dataRowMinHeight
		public byte HasdataRowMinHeight { get; set; }

		// Nullable field: dataRowMinHeight
		private double _dataRowMinHeight;

/// The minimum height of each row (excluding the row that contains column headings).
/// 
/// This value is optional and defaults to [kMinInteractiveDimension] if not
/// specified.
		public double dataRowMinHeight
		{
			get => _dataRowMinHeight;
			set { _dataRowMinHeight = value; HasdataRowMinHeight = 1; }
		}

		// Has flag for nullable property: dataRowMaxHeight
		public byte HasdataRowMaxHeight { get; set; }

		// Nullable field: dataRowMaxHeight
		private double _dataRowMaxHeight;

/// The maximum height of each row (excluding the row that contains column headings).
/// 
/// This value is optional and defaults to [kMinInteractiveDimension] if not
/// specified.
		public double dataRowMaxHeight
		{
			get => _dataRowMaxHeight;
			set { _dataRowMaxHeight = value; HasdataRowMaxHeight = 1; }
		}

		// Simple field: headingRowHeight
/// The height of the heading row.
/// 
/// This value is optional and defaults to 56.0 if not specified.
		public double headingRowHeight { get; set; }

		// Simple field: horizontalMargin
/// The horizontal margin between the edges of the table and the content
/// in the first and last cells of each row.
/// 
/// When a checkbox is displayed, it is also the margin between the checkbox
/// the content in the first data column.
/// 
/// This value defaults to 24.0 to adhere to the Material Design specifications.
/// 
/// If [checkboxHorizontalMargin] is null, then [horizontalMargin] is also the
/// margin between the edge of the table and the checkbox, as well as the
/// margin between the checkbox and the content in the first data column.
		public double horizontalMargin { get; set; }

		// Simple field: columnSpacing
/// The horizontal margin between the contents of each data column.
/// 
/// This value defaults to 56.0 to adhere to the Material Design specifications.
		public double columnSpacing { get; set; }

		// Simple field: showCheckboxColumn
/// {@macro flutter.material.dataTable.showCheckboxColumn}
		public bool showCheckboxColumn { get; set; }

		// Simple field: showFirstLastButtons
/// Flag to display the pagination buttons to go to the first and last pages.
		public bool showFirstLastButtons { get; set; }

		// Has flag for nullable property: initialFirstRowIndex
		public byte HasinitialFirstRowIndex { get; set; }

		// Nullable field: initialFirstRowIndex
		private int _initialFirstRowIndex;

/// The index of the first row to display when the widget is first created.
		public int initialFirstRowIndex
		{
			get => _initialFirstRowIndex;
			set { _initialFirstRowIndex = value; HasinitialFirstRowIndex = 1; }
		}

		// Has flag for nullable property: dividerThickness
		public byte HasdividerThickness { get; set; }

		// Nullable field: dividerThickness
		private double _dividerThickness;

/// {@macro flutter.material.dataTable.dividerThickness}
/// 
/// If null, [DataTableThemeData.dividerThickness] is used. This value
/// defaults to 1.0.
		public double dividerThickness
		{
			get => _dividerThickness;
			set { _dividerThickness = value; HasdividerThickness = 1; }
		}

		// Has flag for nullable property: onPageChanged
		public byte HasonPageChanged { get; set; }

		// Callback field: onPageChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onPageChanged;

/// Invoked when the user switches to another page.
/// 
/// The value is the index of the first row on the currently displayed page.
		/// <summary>
		/// Action identifier for onPageChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onPageChangedAction
		{
			get => GetString(_onPageChanged);
			set { SetString(ref _onPageChanged, value); HasonPageChanged = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: rowsPerPage
/// The number of rows to show on each page.
/// 
/// See also:
/// 
/// * [onRowsPerPageChanged]
/// * [defaultRowsPerPage]
		public int rowsPerPage { get; set; }

		// Simple field: availableRowsPerPage
/// The options to offer for the rowsPerPage.
/// 
/// The current [rowsPerPage] must be a value in this list.
/// 
/// The values in this list should be sorted in ascending order.
		public IntPtr availableRowsPerPage { get; set; }

		// Has flag for nullable property: onRowsPerPageChanged
		public byte HasonRowsPerPageChanged { get; set; }

		// Callback field: onRowsPerPageChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onRowsPerPageChanged;

/// Invoked when the user selects a different number of rows per page.
/// 
/// If this is null, then the value given by [rowsPerPage] will be used
/// and no affordance will be provided to change the value.
		/// <summary>
		/// Action identifier for onRowsPerPageChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onRowsPerPageChangedAction
		{
			get => GetString(_onRowsPerPageChanged);
			set { SetString(ref _onRowsPerPageChanged, value); HasonRowsPerPageChanged = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: source
/// The data source which provides data to show in each row.
/// 
/// This object should generally have a lifetime longer than the
/// [PaginatedDataTable] widget itself; it should be reused each time the
/// [PaginatedDataTable] constructor is called.
		public IntPtr source { get; set; }

		// Simple field: dragStartBehavior
/// {@macro flutter.widgets.scrollable.dragStartBehavior}
		public DragStartBehavior dragStartBehavior { get; set; }

		// Has flag for nullable property: checkboxHorizontalMargin
		public byte HascheckboxHorizontalMargin { get; set; }

		// Nullable field: checkboxHorizontalMargin
		private double _checkboxHorizontalMargin;

/// Horizontal margin around the checkbox, if it is displayed.
/// 
/// If null, then [horizontalMargin] is used as the margin between the edge
/// of the table and the checkbox, as well as the margin between the checkbox
/// and the content in the first data column. This value defaults to 24.0.
		public double checkboxHorizontalMargin
		{
			get => _checkboxHorizontalMargin;
			set { _checkboxHorizontalMargin = value; HascheckboxHorizontalMargin = 1; }
		}

		// Has flag for nullable property: arrowHeadColor
		public byte HasarrowHeadColor { get; set; }

		// Nullable field: arrowHeadColor
		private IntPtr _arrowHeadColor;

/// Defines the color of the arrow heads in the footer.
		public IntPtr arrowHeadColor
		{
			get => _arrowHeadColor;
			set { _arrowHeadColor = value; HasarrowHeadColor = 1; }
		}

		// Has flag for nullable property: controller
		public byte Hascontroller { get; set; }

		// Nullable field: controller
		private IntPtr _controller;

/// {@macro flutter.widgets.scroll_view.controller}
		public IntPtr controller
		{
			get => _controller;
			set { _controller = value; Hascontroller = 1; }
		}

		// Has flag for nullable property: primary
		public byte Hasprimary { get; set; }

		// Nullable field: primary
		private bool _primary;

/// {@macro flutter.widgets.scroll_view.primary}
		public bool primary
		{
			get => _primary;
			set { _primary = value; Hasprimary = 1; }
		}

		// Has flag for nullable property: headingRowColor
		public byte HasheadingRowColor { get; set; }

		// Nullable field: headingRowColor
		private IntPtr _headingRowColor;

/// {@macro flutter.material.dataTable.headingRowColor}
		public IntPtr headingRowColor
		{
			get => _headingRowColor;
			set { _headingRowColor = value; HasheadingRowColor = 1; }
		}

		// Simple field: showEmptyRows
/// Controls the visibility of empty rows on the last page of a
/// [PaginatedDataTable].
/// 
/// Defaults to `true`, which means empty rows will be populated on the
/// last page of the table if there is not enough content.
/// When set to `false`, empty rows will not be created.
		public bool showEmptyRows { get; set; }

		// Has flag for nullable property: dataRowHeight
		public byte HasdataRowHeight { get; set; }

		// Nullable field: dataRowHeight
		private double _dataRowHeight;

		public double dataRowHeight
		{
			get => _dataRowHeight;
			set { _dataRowHeight = value; HasdataRowHeight = 1; }
		}

	}
}
