// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A widget that annotates the widget tree with a description of the meaning of
/// the widgets.
/// 
/// {@youtube 560 315 https://www.youtube.com/watch?v=NvtMt_DtFrQ}
/// 
/// {@macro flutter.widgets.SemanticsBase}
/// * [SliverSemantics], the sliver variant of this widget.
	[StructLayout(LayoutKind.Sequential)]
	internal class SemanticsStruct : SingleChildRenderObjectWidgetStruct
	{
		// Simple field: container
		public bool container { get; set; }

		// Simple field: explicitChildNodes
		public bool explicitChildNodes { get; set; }

		// Simple field: excludeSemantics
		public bool excludeSemantics { get; set; }

		// Simple field: blockUserActions
		public bool blockUserActions { get; set; }

		// Has flag for nullable property: enabled
		public byte Hasenabled { get; set; }

		// Nullable field: enabled
		private bool _enabled;

		public bool enabled
		{
			get => _enabled;
			set { _enabled = value; Hasenabled = 1; }
		}

		// Has flag for nullable property: @checked
		public byte Haschecked { get; set; }

		// Nullable field: @checked
		private bool _checked;

		public bool @checked
		{
			get => _checked;
			set { _checked = value; Haschecked = 1; }
		}

		// Has flag for nullable property: mixed
		public byte Hasmixed { get; set; }

		// Nullable field: mixed
		private bool _mixed;

		public bool mixed
		{
			get => _mixed;
			set { _mixed = value; Hasmixed = 1; }
		}

		// Has flag for nullable property: selected
		public byte Hasselected { get; set; }

		// Nullable field: selected
		private bool _selected;

		public bool selected
		{
			get => _selected;
			set { _selected = value; Hasselected = 1; }
		}

		// Has flag for nullable property: toggled
		public byte Hastoggled { get; set; }

		// Nullable field: toggled
		private bool _toggled;

		public bool toggled
		{
			get => _toggled;
			set { _toggled = value; Hastoggled = 1; }
		}

		// Has flag for nullable property: button
		public byte Hasbutton { get; set; }

		// Nullable field: button
		private bool _button;

		public bool button
		{
			get => _button;
			set { _button = value; Hasbutton = 1; }
		}

		// Has flag for nullable property: slider
		public byte Hasslider { get; set; }

		// Nullable field: slider
		private bool _slider;

		public bool slider
		{
			get => _slider;
			set { _slider = value; Hasslider = 1; }
		}

		// Has flag for nullable property: keyboardKey
		public byte HaskeyboardKey { get; set; }

		// Nullable field: keyboardKey
		private bool _keyboardKey;

		public bool keyboardKey
		{
			get => _keyboardKey;
			set { _keyboardKey = value; HaskeyboardKey = 1; }
		}

		// Has flag for nullable property: link
		public byte Haslink { get; set; }

		// Nullable field: link
		private bool _link;

		public bool link
		{
			get => _link;
			set { _link = value; Haslink = 1; }
		}

		// Has flag for nullable property: linkUrl
		public byte HaslinkUrl { get; set; }

		// Nullable field: linkUrl
		private IntPtr _linkUrl;

		public IntPtr linkUrl
		{
			get => _linkUrl;
			set { _linkUrl = value; HaslinkUrl = 1; }
		}

		// Has flag for nullable property: header
		public byte Hasheader { get; set; }

		// Nullable field: header
		private bool _header;

		public bool header
		{
			get => _header;
			set { _header = value; Hasheader = 1; }
		}

		// Has flag for nullable property: headingLevel
		public byte HasheadingLevel { get; set; }

		// Nullable field: headingLevel
		private int _headingLevel;

		public int headingLevel
		{
			get => _headingLevel;
			set { _headingLevel = value; HasheadingLevel = 1; }
		}

		// Has flag for nullable property: textField
		public byte HastextField { get; set; }

		// Nullable field: textField
		private bool _textField;

		public bool textField
		{
			get => _textField;
			set { _textField = value; HastextField = 1; }
		}

		// Has flag for nullable property: @readOnly
		public byte HasreadOnly { get; set; }

		// Nullable field: @readOnly
		private bool _readOnly;

		public bool @readOnly
		{
			get => _readOnly;
			set { _readOnly = value; HasreadOnly = 1; }
		}

		// Has flag for nullable property: focusable
		public byte Hasfocusable { get; set; }

		// Nullable field: focusable
		private bool _focusable;

		public bool focusable
		{
			get => _focusable;
			set { _focusable = value; Hasfocusable = 1; }
		}

		// Has flag for nullable property: focused
		public byte Hasfocused { get; set; }

		// Nullable field: focused
		private bool _focused;

		public bool focused
		{
			get => _focused;
			set { _focused = value; Hasfocused = 1; }
		}

		// Has flag for nullable property: inMutuallyExclusiveGroup
		public byte HasinMutuallyExclusiveGroup { get; set; }

		// Nullable field: inMutuallyExclusiveGroup
		private bool _inMutuallyExclusiveGroup;

		public bool inMutuallyExclusiveGroup
		{
			get => _inMutuallyExclusiveGroup;
			set { _inMutuallyExclusiveGroup = value; HasinMutuallyExclusiveGroup = 1; }
		}

		// Has flag for nullable property: obscured
		public byte Hasobscured { get; set; }

		// Nullable field: obscured
		private bool _obscured;

		public bool obscured
		{
			get => _obscured;
			set { _obscured = value; Hasobscured = 1; }
		}

		// Has flag for nullable property: multiline
		public byte Hasmultiline { get; set; }

		// Nullable field: multiline
		private bool _multiline;

		public bool multiline
		{
			get => _multiline;
			set { _multiline = value; Hasmultiline = 1; }
		}

		// Has flag for nullable property: scopesRoute
		public byte HasscopesRoute { get; set; }

		// Nullable field: scopesRoute
		private bool _scopesRoute;

		public bool scopesRoute
		{
			get => _scopesRoute;
			set { _scopesRoute = value; HasscopesRoute = 1; }
		}

		// Has flag for nullable property: namesRoute
		public byte HasnamesRoute { get; set; }

		// Nullable field: namesRoute
		private bool _namesRoute;

		public bool namesRoute
		{
			get => _namesRoute;
			set { _namesRoute = value; HasnamesRoute = 1; }
		}

		// Has flag for nullable property: hidden
		public byte Hashidden { get; set; }

		// Nullable field: hidden
		private bool _hidden;

		public bool hidden
		{
			get => _hidden;
			set { _hidden = value; Hashidden = 1; }
		}

		// Has flag for nullable property: image
		public byte Hasimage { get; set; }

		// Nullable field: image
		private bool _image;

		public bool image
		{
			get => _image;
			set { _image = value; Hasimage = 1; }
		}

		// Has flag for nullable property: liveRegion
		public byte HasliveRegion { get; set; }

		// Nullable field: liveRegion
		private bool _liveRegion;

		public bool liveRegion
		{
			get => _liveRegion;
			set { _liveRegion = value; HasliveRegion = 1; }
		}

		// Has flag for nullable property: expanded
		public byte Hasexpanded { get; set; }

		// Nullable field: expanded
		private bool _expanded;

		public bool expanded
		{
			get => _expanded;
			set { _expanded = value; Hasexpanded = 1; }
		}

		// Has flag for nullable property: isRequired
		public byte HasisRequired { get; set; }

		// Nullable field: isRequired
		private bool _isRequired;

		public bool isRequired
		{
			get => _isRequired;
			set { _isRequired = value; HasisRequired = 1; }
		}

		// Has flag for nullable property: maxValueLength
		public byte HasmaxValueLength { get; set; }

		// Nullable field: maxValueLength
		private int _maxValueLength;

		public int maxValueLength
		{
			get => _maxValueLength;
			set { _maxValueLength = value; HasmaxValueLength = 1; }
		}

		// Has flag for nullable property: currentValueLength
		public byte HascurrentValueLength { get; set; }

		// Nullable field: currentValueLength
		private int _currentValueLength;

		public int currentValueLength
		{
			get => _currentValueLength;
			set { _currentValueLength = value; HascurrentValueLength = 1; }
		}

		// Has flag for nullable property: identifier
		public byte Hasidentifier { get; set; }

		// String field: identifier
		private IntPtr _identifier;

		public string? identifier
		{
			get => GetString(_identifier);
			set { SetString(ref _identifier, value); Hasidentifier = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: label
		public byte Haslabel { get; set; }

		// String field: label
		private IntPtr _label;

		public string? label
		{
			get => GetString(_label);
			set { SetString(ref _label, value); Haslabel = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: attributedLabel
		public byte HasattributedLabel { get; set; }

		// Nullable field: attributedLabel
		private IntPtr _attributedLabel;

		public IntPtr attributedLabel
		{
			get => _attributedLabel;
			set { _attributedLabel = value; HasattributedLabel = 1; }
		}

		// Has flag for nullable property: value
		public byte Hasvalue { get; set; }

		// String field: value
		private IntPtr _value;

		public string? value
		{
			get => GetString(_value);
			set { SetString(ref _value, value); Hasvalue = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: attributedValue
		public byte HasattributedValue { get; set; }

		// Nullable field: attributedValue
		private IntPtr _attributedValue;

		public IntPtr attributedValue
		{
			get => _attributedValue;
			set { _attributedValue = value; HasattributedValue = 1; }
		}

		// Has flag for nullable property: increasedValue
		public byte HasincreasedValue { get; set; }

		// String field: increasedValue
		private IntPtr _increasedValue;

		public string? increasedValue
		{
			get => GetString(_increasedValue);
			set { SetString(ref _increasedValue, value); HasincreasedValue = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: attributedIncreasedValue
		public byte HasattributedIncreasedValue { get; set; }

		// Nullable field: attributedIncreasedValue
		private IntPtr _attributedIncreasedValue;

		public IntPtr attributedIncreasedValue
		{
			get => _attributedIncreasedValue;
			set { _attributedIncreasedValue = value; HasattributedIncreasedValue = 1; }
		}

		// Has flag for nullable property: decreasedValue
		public byte HasdecreasedValue { get; set; }

		// String field: decreasedValue
		private IntPtr _decreasedValue;

		public string? decreasedValue
		{
			get => GetString(_decreasedValue);
			set { SetString(ref _decreasedValue, value); HasdecreasedValue = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: attributedDecreasedValue
		public byte HasattributedDecreasedValue { get; set; }

		// Nullable field: attributedDecreasedValue
		private IntPtr _attributedDecreasedValue;

		public IntPtr attributedDecreasedValue
		{
			get => _attributedDecreasedValue;
			set { _attributedDecreasedValue = value; HasattributedDecreasedValue = 1; }
		}

		// Has flag for nullable property: hint
		public byte Hashint { get; set; }

		// String field: hint
		private IntPtr _hint;

		public string? hint
		{
			get => GetString(_hint);
			set { SetString(ref _hint, value); Hashint = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: attributedHint
		public byte HasattributedHint { get; set; }

		// Nullable field: attributedHint
		private IntPtr _attributedHint;

		public IntPtr attributedHint
		{
			get => _attributedHint;
			set { _attributedHint = value; HasattributedHint = 1; }
		}

		// Has flag for nullable property: tooltip
		public byte Hastooltip { get; set; }

		// String field: tooltip
		private IntPtr _tooltip;

		public string? tooltip
		{
			get => GetString(_tooltip);
			set { SetString(ref _tooltip, value); Hastooltip = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onTapHint
		public byte HasonTapHint { get; set; }

		// String field: onTapHint
		private IntPtr _onTapHint;

		public string? onTapHint
		{
			get => GetString(_onTapHint);
			set { SetString(ref _onTapHint, value); HasonTapHint = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onLongPressHint
		public byte HasonLongPressHint { get; set; }

		// String field: onLongPressHint
		private IntPtr _onLongPressHint;

		public string? onLongPressHint
		{
			get => GetString(_onLongPressHint);
			set { SetString(ref _onLongPressHint, value); HasonLongPressHint = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: textDirection
		public byte HastextDirection { get; set; }

		// Nullable field: textDirection
		private TextDirection _textDirection;

		public TextDirection textDirection
		{
			get => _textDirection;
			set { _textDirection = value; HastextDirection = 1; }
		}

		// Has flag for nullable property: sortKey
		public byte HassortKey { get; set; }

		// Nullable field: sortKey
		private IntPtr _sortKey;

		public IntPtr sortKey
		{
			get => _sortKey;
			set { _sortKey = value; HassortKey = 1; }
		}

		// Has flag for nullable property: tagForChildren
		public byte HastagForChildren { get; set; }

		// Nullable field: tagForChildren
		private IntPtr _tagForChildren;

		public IntPtr tagForChildren
		{
			get => _tagForChildren;
			set { _tagForChildren = value; HastagForChildren = 1; }
		}

		// Has flag for nullable property: onTap
		public byte HasonTap { get; set; }

		// Callback field: onTap
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onTap;

		/// <summary>
		/// Action identifier for onTap callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onTapAction
		{
			get => GetString(_onTap);
			set { SetString(ref _onTap, value); HasonTap = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onLongPress
		public byte HasonLongPress { get; set; }

		// Callback field: onLongPress
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onLongPress;

		/// <summary>
		/// Action identifier for onLongPress callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onLongPressAction
		{
			get => GetString(_onLongPress);
			set { SetString(ref _onLongPress, value); HasonLongPress = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onScrollLeft
		public byte HasonScrollLeft { get; set; }

		// Callback field: onScrollLeft
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onScrollLeft;

		/// <summary>
		/// Action identifier for onScrollLeft callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onScrollLeftAction
		{
			get => GetString(_onScrollLeft);
			set { SetString(ref _onScrollLeft, value); HasonScrollLeft = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onScrollRight
		public byte HasonScrollRight { get; set; }

		// Callback field: onScrollRight
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onScrollRight;

		/// <summary>
		/// Action identifier for onScrollRight callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onScrollRightAction
		{
			get => GetString(_onScrollRight);
			set { SetString(ref _onScrollRight, value); HasonScrollRight = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onScrollUp
		public byte HasonScrollUp { get; set; }

		// Callback field: onScrollUp
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onScrollUp;

		/// <summary>
		/// Action identifier for onScrollUp callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onScrollUpAction
		{
			get => GetString(_onScrollUp);
			set { SetString(ref _onScrollUp, value); HasonScrollUp = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onScrollDown
		public byte HasonScrollDown { get; set; }

		// Callback field: onScrollDown
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onScrollDown;

		/// <summary>
		/// Action identifier for onScrollDown callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onScrollDownAction
		{
			get => GetString(_onScrollDown);
			set { SetString(ref _onScrollDown, value); HasonScrollDown = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onIncrease
		public byte HasonIncrease { get; set; }

		// Callback field: onIncrease
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onIncrease;

		/// <summary>
		/// Action identifier for onIncrease callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onIncreaseAction
		{
			get => GetString(_onIncrease);
			set { SetString(ref _onIncrease, value); HasonIncrease = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onDecrease
		public byte HasonDecrease { get; set; }

		// Callback field: onDecrease
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onDecrease;

		/// <summary>
		/// Action identifier for onDecrease callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onDecreaseAction
		{
			get => GetString(_onDecrease);
			set { SetString(ref _onDecrease, value); HasonDecrease = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onCopy
		public byte HasonCopy { get; set; }

		// Callback field: onCopy
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onCopy;

		/// <summary>
		/// Action identifier for onCopy callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onCopyAction
		{
			get => GetString(_onCopy);
			set { SetString(ref _onCopy, value); HasonCopy = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onCut
		public byte HasonCut { get; set; }

		// Callback field: onCut
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onCut;

		/// <summary>
		/// Action identifier for onCut callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onCutAction
		{
			get => GetString(_onCut);
			set { SetString(ref _onCut, value); HasonCut = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onPaste
		public byte HasonPaste { get; set; }

		// Callback field: onPaste
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onPaste;

		/// <summary>
		/// Action identifier for onPaste callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onPasteAction
		{
			get => GetString(_onPaste);
			set { SetString(ref _onPaste, value); HasonPaste = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onDismiss
		public byte HasonDismiss { get; set; }

		// Callback field: onDismiss
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onDismiss;

		/// <summary>
		/// Action identifier for onDismiss callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onDismissAction
		{
			get => GetString(_onDismiss);
			set { SetString(ref _onDismiss, value); HasonDismiss = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onMoveCursorForwardByCharacter
		public byte HasonMoveCursorForwardByCharacter { get; set; }

		// Callback field: onMoveCursorForwardByCharacter
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onMoveCursorForwardByCharacter;

		/// <summary>
		/// Action identifier for onMoveCursorForwardByCharacter callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onMoveCursorForwardByCharacterAction
		{
			get => GetString(_onMoveCursorForwardByCharacter);
			set { SetString(ref _onMoveCursorForwardByCharacter, value); HasonMoveCursorForwardByCharacter = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onMoveCursorBackwardByCharacter
		public byte HasonMoveCursorBackwardByCharacter { get; set; }

		// Callback field: onMoveCursorBackwardByCharacter
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onMoveCursorBackwardByCharacter;

		/// <summary>
		/// Action identifier for onMoveCursorBackwardByCharacter callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onMoveCursorBackwardByCharacterAction
		{
			get => GetString(_onMoveCursorBackwardByCharacter);
			set { SetString(ref _onMoveCursorBackwardByCharacter, value); HasonMoveCursorBackwardByCharacter = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onSetSelection
		public byte HasonSetSelection { get; set; }

		// Callback field: onSetSelection
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onSetSelection;

		/// <summary>
		/// Action identifier for onSetSelection callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onSetSelectionAction
		{
			get => GetString(_onSetSelection);
			set { SetString(ref _onSetSelection, value); HasonSetSelection = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onSetText
		public byte HasonSetText { get; set; }

		// Callback field: onSetText
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onSetText;

		/// <summary>
		/// Action identifier for onSetText callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onSetTextAction
		{
			get => GetString(_onSetText);
			set { SetString(ref _onSetText, value); HasonSetText = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onDidGainAccessibilityFocus
		public byte HasonDidGainAccessibilityFocus { get; set; }

		// Callback field: onDidGainAccessibilityFocus
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onDidGainAccessibilityFocus;

		/// <summary>
		/// Action identifier for onDidGainAccessibilityFocus callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onDidGainAccessibilityFocusAction
		{
			get => GetString(_onDidGainAccessibilityFocus);
			set { SetString(ref _onDidGainAccessibilityFocus, value); HasonDidGainAccessibilityFocus = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onDidLoseAccessibilityFocus
		public byte HasonDidLoseAccessibilityFocus { get; set; }

		// Callback field: onDidLoseAccessibilityFocus
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onDidLoseAccessibilityFocus;

		/// <summary>
		/// Action identifier for onDidLoseAccessibilityFocus callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onDidLoseAccessibilityFocusAction
		{
			get => GetString(_onDidLoseAccessibilityFocus);
			set { SetString(ref _onDidLoseAccessibilityFocus, value); HasonDidLoseAccessibilityFocus = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onFocus
		public byte HasonFocus { get; set; }

		// Callback field: onFocus
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onFocus;

		/// <summary>
		/// Action identifier for onFocus callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onFocusAction
		{
			get => GetString(_onFocus);
			set { SetString(ref _onFocus, value); HasonFocus = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onExpand
		public byte HasonExpand { get; set; }

		// Callback field: onExpand
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onExpand;

		/// <summary>
		/// Action identifier for onExpand callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onExpandAction
		{
			get => GetString(_onExpand);
			set { SetString(ref _onExpand, value); HasonExpand = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onCollapse
		public byte HasonCollapse { get; set; }

		// Callback field: onCollapse
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onCollapse;

		/// <summary>
		/// Action identifier for onCollapse callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onCollapseAction
		{
			get => GetString(_onCollapse);
			set { SetString(ref _onCollapse, value); HasonCollapse = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: customSemanticsActions
		public byte HascustomSemanticsActions { get; set; }

		// Nullable field: customSemanticsActions
		private IntPtr _customSemanticsActions;

		public IntPtr customSemanticsActions
		{
			get => _customSemanticsActions;
			set { _customSemanticsActions = value; HascustomSemanticsActions = 1; }
		}

		// Has flag for nullable property: role
		public byte Hasrole { get; set; }

		// Nullable field: role
		private IntPtr _role;

		public IntPtr role
		{
			get => _role;
			set { _role = value; Hasrole = 1; }
		}

		// Has flag for nullable property: controlsNodes
		public byte HascontrolsNodes { get; set; }

		// Nullable field: controlsNodes
		private IntPtr _controlsNodes;

		public IntPtr controlsNodes
		{
			get => _controlsNodes;
			set { _controlsNodes = value; HascontrolsNodes = 1; }
		}

		// Simple field: validationResult
		public IntPtr validationResult { get; set; }

		// Has flag for nullable property: inputType
		public byte HasinputType { get; set; }

		// Nullable field: inputType
		private IntPtr _inputType;

		public IntPtr inputType
		{
			get => _inputType;
			set { _inputType = value; HasinputType = 1; }
		}

		// Has flag for nullable property: localeForSubtree
		public byte HaslocaleForSubtree { get; set; }

		// Nullable field: localeForSubtree
		private IntPtr _localeForSubtree;

		public IntPtr localeForSubtree
		{
			get => _localeForSubtree;
			set { _localeForSubtree = value; HaslocaleForSubtree = 1; }
		}

	}
}
