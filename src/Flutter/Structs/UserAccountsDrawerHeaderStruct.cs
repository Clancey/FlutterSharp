// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A Material Design [Drawer] header that identifies the app's user.
/// 
/// Requires one of its ancestors to be a [Material] widget.
/// 
/// See also:
/// 
/// * [DrawerHeader], for a drawer header that doesn't show user accounts.
/// * <https://material.io/design/components/navigation-drawer.html#anatomy>
	[StructLayout(LayoutKind.Sequential)]
	internal class UserAccountsDrawerHeaderStruct : WidgetStruct
	{
		// Has flag for nullable property: decoration
		public byte Hasdecoration { get; set; }

		// Nullable field: decoration
		private IntPtr _decoration;

/// The header's background. If decoration is null then a [BoxDecoration]
/// with its background color set to the current theme's primaryColor is used.
		public IntPtr decoration
		{
			get => _decoration;
			set { _decoration = value; Hasdecoration = 1; }
		}

		// Has flag for nullable property: margin
		public byte Hasmargin { get; set; }

		// Nullable field: margin
		private IntPtr _margin;

/// The margin around the drawer header.
		public IntPtr margin
		{
			get => _margin;
			set { _margin = value; Hasmargin = 1; }
		}

		// Has flag for nullable property: currentAccountPicture
		public byte HascurrentAccountPicture { get; set; }

		// Widget field: currentAccountPicture
		private IntPtr _currentAccountPicture;

/// A widget placed in the upper-left corner that represents the current
/// user's account. Normally a [CircleAvatar].
		public IntPtr? currentAccountPicture
		{
			get => _currentAccountPicture != IntPtr.Zero ? (IntPtr)_currentAccountPicture : null;
			set { SetIntPtr(ref _currentAccountPicture, value); HascurrentAccountPicture = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: otherAccountsPictures
		public byte HasotherAccountsPictures { get; set; }

		// Nullable field: otherAccountsPictures
		private IntPtr _otherAccountsPictures;

/// A list of widgets that represent the current user's other accounts.
/// Up to three of these widgets will be arranged in a row in the header's
/// upper-right corner. Normally a list of [CircleAvatar] widgets.
		public IntPtr otherAccountsPictures
		{
			get => _otherAccountsPictures;
			set { _otherAccountsPictures = value; HasotherAccountsPictures = 1; }
		}

		// Simple field: currentAccountPictureSize
/// The size of the [currentAccountPicture].
		public IntPtr currentAccountPictureSize { get; set; }

		// Simple field: otherAccountsPicturesSize
/// The size of each widget in [otherAccountsPicturesSize].
		public IntPtr otherAccountsPicturesSize { get; set; }

		// Has flag for nullable property: accountName
		public byte HasaccountName { get; set; }

		// Widget field: accountName
		private IntPtr _accountName;

/// A widget that represents the user's current account name. It is
/// displayed on the left, below the [currentAccountPicture].
		public IntPtr? accountName
		{
			get => _accountName != IntPtr.Zero ? (IntPtr)_accountName : null;
			set { SetIntPtr(ref _accountName, value); HasaccountName = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: accountEmail
		public byte HasaccountEmail { get; set; }

		// Widget field: accountEmail
		private IntPtr _accountEmail;

/// A widget that represents the email address of the user's current account.
/// It is displayed on the left, below the [accountName].
		public IntPtr? accountEmail
		{
			get => _accountEmail != IntPtr.Zero ? (IntPtr)_accountEmail : null;
			set { SetIntPtr(ref _accountEmail, value); HasaccountEmail = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onDetailsPressed
		public byte HasonDetailsPressed { get; set; }

		// Callback field: onDetailsPressed
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onDetailsPressed;

/// A callback that is called when the horizontal area which contains the
/// [accountName] and [accountEmail] is tapped.
		/// <summary>
		/// Action identifier for onDetailsPressed callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onDetailsPressedAction
		{
			get => GetString(_onDetailsPressed);
			set { SetString(ref _onDetailsPressed, value); HasonDetailsPressed = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: arrowColor
/// The [Color] of the arrow icon.
		public IntPtr arrowColor { get; set; }

	}
}
