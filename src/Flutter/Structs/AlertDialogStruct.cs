// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// A Material Design alert dialog.
/// 
/// An alert dialog (also known as a basic dialog) informs the user about
/// situations that require acknowledgment. An alert dialog has an optional
/// title and an optional list of actions. The title is displayed above the
/// content and the actions are displayed below the content.
/// 
/// {@youtube 560 315 https://www.youtube.com/watch?v=75CsnyRXf5I}
/// 
/// For dialogs that offer the user a choice between several options, consider
/// using a [SimpleDialog].
/// 
/// Typically passed as the child widget to [showDialog], which displays the
/// dialog.
/// 
/// {@animation 350 622 https://flutter.github.io/assets-for-api-docs/assets/material/alert_dialog.mp4}
/// 
/// {@tool snippet}
/// 
/// This snippet shows a method in a [State] which, when called, displays a dialog box
/// and returns a [Future] that completes when the dialog is dismissed.
/// 
/// ```dart
/// Future<void> _showMyDialog() async {
/// return showDialog<void>(
/// context: context,
/// barrierDismissible: false, // user must tap button!
/// builder: (BuildContext context) {
/// return AlertDialog(
/// title: const Text('AlertDialog Title'),
/// content: const SingleChildScrollView(
/// child: ListBody(
/// children: <Widget>[
/// Text('This is a demo alert dialog.'),
/// Text('Would you like to approve of this message?'),
/// ],
/// ),
/// ),
/// actions: <Widget>[
/// TextButton(
/// child: const Text('Approve'),
/// onPressed: () {
/// Navigator.of(context).pop();
/// },
/// ),
/// ],
/// );
/// },
/// );
/// }
/// ```
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This demo shows a [TextButton] which when pressed, calls [showDialog]. When called, this method
/// displays a Material dialog above the current contents of the app and returns
/// a [Future] that completes when the dialog is dismissed.
/// 
/// ** See code in examples/api/lib/material/dialog/alert_dialog.0.dart **
/// {@end-tool}
/// 
/// {@tool dartpad}
/// This sample shows the creation of [AlertDialog], as described in:
/// https://m3.material.io/components/dialogs/overview
/// 
/// ** See code in examples/api/lib/material/dialog/alert_dialog.1.dart **
/// {@end-tool}
/// 
/// ## Alert dialogs and scrolling
/// 
/// By default, alert dialogs size themselves to contain their children.
/// 
/// If the content is too large to fit on the screen vertically, the dialog will
/// display the title and actions, and let the _[content]_ overflow. This is
/// rarely desired. Consider using a scrolling widget for [content], such as
/// [SingleChildScrollView], to avoid overflow.
/// 
/// Because the dialog attempts to size itself to the contents, the [content]
/// must support reporting its intrinsic dimensions. In particular, this means
/// that lazily-rendered widgets such as [ListView], [GridView], and
/// [CustomScrollView], will not work in an [AlertDialog] unless they are
/// wrapped in a widget that forces a particular size (e.g. a [SizedBox]).
/// 
/// For finer-grained control over the sizing of a dialog, consider using
/// [Dialog] directly.
/// 
/// See also:
/// 
/// * [SimpleDialog], which handles the scrolling of the contents but has no [actions].
/// * [Dialog], on which [AlertDialog] and [SimpleDialog] are based.
/// * [CupertinoAlertDialog], an iOS-styled alert dialog.
/// * [showDialog], which actually displays the dialog and returns its result.
/// * <https://material.io/design/components/dialogs.html#alert-dialog>
/// * <https://m3.material.io/components/dialogs>
	[StructLayout(LayoutKind.Sequential)]
	internal class AlertDialogStruct : WidgetStruct
	{
		// Has flag for nullable property: icon
		public byte Hasicon { get; set; }

		// Widget field: icon
		private IntPtr _icon;

/// An optional icon to display at the top of the dialog.
/// 
/// Typically, an [Icon] widget. Providing an icon centers the [title]'s text.
		public IntPtr? icon
		{
			get => _icon != IntPtr.Zero ? (IntPtr)_icon : null;
			set { SetIntPtr(ref _icon, value); Hasicon = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: iconColor
		public byte HasiconColor { get; set; }

		// Nullable field: iconColor
		private IntPtr _iconColor;

/// Color for the [Icon] in the [icon] of this [AlertDialog].
/// 
/// If null, [DialogThemeData.iconColor] is used. If that is null, defaults to
/// color scheme's [ColorScheme.secondary] if [ThemeData.useMaterial3] is
/// true, black otherwise.
		public IntPtr iconColor
		{
			get => _iconColor;
			set { _iconColor = value; HasiconColor = 1; }
		}

		// Has flag for nullable property: iconPadding
		public byte HasiconPadding { get; set; }

		// Nullable field: iconPadding
		private IntPtr _iconPadding;

/// Padding around the [icon].
/// 
/// If there is no [icon], no padding will be provided. Otherwise, this
/// padding is used.
/// 
/// This property defaults to providing 24 pixels on the top, left, and right
/// of the [icon]. If [title] is _not_ null, 16 pixels of bottom padding is
/// added to separate the [icon] from the [title]. If the [title] is null and
/// [content] is _not_ null, then no bottom padding is provided (but see
/// [contentPadding]). In any other case 24 pixels of bottom padding is
/// added.
		public IntPtr iconPadding
		{
			get => _iconPadding;
			set { _iconPadding = value; HasiconPadding = 1; }
		}

		// Has flag for nullable property: title
		public byte Hastitle { get; set; }

		// Widget field: title
		private IntPtr _title;

/// The (optional) title of the dialog is displayed in a large font at the top
/// of the dialog, below the (optional) [icon].
/// 
/// Typically a [Text] widget.
		public IntPtr? title
		{
			get => _title != IntPtr.Zero ? (IntPtr)_title : null;
			set { SetIntPtr(ref _title, value); Hastitle = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: titlePadding
		public byte HastitlePadding { get; set; }

		// Nullable field: titlePadding
		private IntPtr _titlePadding;

/// Padding around the title.
/// 
/// If there is no title, no padding will be provided. Otherwise, this padding
/// is used.
/// 
/// This property defaults to providing 24 pixels on the top, left, and right
/// of the title. If the [content] is not null, then no bottom padding is
/// provided (but see [contentPadding]). If it _is_ null, then an extra 20
/// pixels of bottom padding is added to separate the [title] from the
/// [actions].
		public IntPtr titlePadding
		{
			get => _titlePadding;
			set { _titlePadding = value; HastitlePadding = 1; }
		}

		// Has flag for nullable property: titleTextStyle
		public byte HastitleTextStyle { get; set; }

		// Nullable field: titleTextStyle
		private IntPtr _titleTextStyle;

/// Style for the text in the [title] of this [AlertDialog].
/// 
/// If null, [DialogThemeData.titleTextStyle] is used. If that's null, defaults to
/// [TextTheme.headlineSmall] of [ThemeData.textTheme] if
/// [ThemeData.useMaterial3] is true, [TextTheme.titleLarge] otherwise.
		public IntPtr titleTextStyle
		{
			get => _titleTextStyle;
			set { _titleTextStyle = value; HastitleTextStyle = 1; }
		}

		// Has flag for nullable property: content
		public byte Hascontent { get; set; }

		// Widget field: content
		private IntPtr _content;

/// The (optional) content of the dialog is displayed in the center of the
/// dialog in a lighter font.
/// 
/// Typically this is a [SingleChildScrollView] that contains the dialog's
/// message. As noted in the [AlertDialog] documentation, it's important
/// to use a [SingleChildScrollView] if there's any risk that the content
/// will not fit, as the contents will otherwise overflow the dialog.
/// 
/// The [content] must support reporting its intrinsic dimensions. In
/// particular, [ListView], [GridView], and [CustomScrollView] cannot be used
/// here unless they are first wrapped in a widget that itself can report
/// intrinsic dimensions, such as a [SizedBox].
		public IntPtr? content
		{
			get => _content != IntPtr.Zero ? (IntPtr)_content : null;
			set { SetIntPtr(ref _content, value); Hascontent = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: contentPadding
		public byte HascontentPadding { get; set; }

		// Nullable field: contentPadding
		private IntPtr _contentPadding;

/// Padding around the content.
/// 
/// If there is no [content], no padding will be provided. Otherwise, this
/// padding is used.
/// 
/// This property defaults to providing a padding of 20 pixels above the
/// [content] to separate the [content] from the [title], and 24 pixels on the
/// left, right, and bottom to separate the [content] from the other edges of
/// the dialog.
/// 
/// If [ThemeData.useMaterial3] is true, the top padding separating the
/// content from the title defaults to 16 pixels instead of 20 pixels.
		public IntPtr contentPadding
		{
			get => _contentPadding;
			set { _contentPadding = value; HascontentPadding = 1; }
		}

		// Has flag for nullable property: contentTextStyle
		public byte HascontentTextStyle { get; set; }

		// Nullable field: contentTextStyle
		private IntPtr _contentTextStyle;

/// Style for the text in the [content] of this [AlertDialog].
/// 
/// If null, [DialogThemeData.contentTextStyle] is used. If that's null, defaults
/// to [TextTheme.bodyMedium] of [ThemeData.textTheme] if
/// [ThemeData.useMaterial3] is true, [TextTheme.titleMedium] otherwise.
		public IntPtr contentTextStyle
		{
			get => _contentTextStyle;
			set { _contentTextStyle = value; HascontentTextStyle = 1; }
		}

		// Has flag for nullable property: actions
		public byte Hasactions { get; set; }

		// Nullable field: actions
		private IntPtr _actions;

/// The (optional) set of actions that are displayed at the bottom of the
/// dialog with an [OverflowBar].
/// 
/// Typically this is a list of [TextButton] widgets. It is recommended to
/// set the [Text.textAlign] to [TextAlign.end] for the [Text] within the
/// [TextButton], so that buttons whose labels wrap to an extra line align
/// with the overall [OverflowBar]'s alignment within the dialog.
/// 
/// If the [title] is not null but the [content] _is_ null, then an extra 20
/// pixels of padding is added above the [OverflowBar] to separate the [title]
/// from the [actions].
		public IntPtr actions
		{
			get => _actions;
			set { _actions = value; Hasactions = 1; }
		}

		// Has flag for nullable property: actionsPadding
		public byte HasactionsPadding { get; set; }

		// Nullable field: actionsPadding
		private IntPtr _actionsPadding;

/// Padding around the set of [actions] at the bottom of the dialog.
/// 
/// Typically used to provide padding to the button bar between the button bar
/// and the edges of the dialog.
/// 
/// The [buttonPadding] may contribute to the padding on the edges of
/// [actions] as well.
/// 
/// If there are no [actions], then no padding will be included.
/// 
/// {@tool snippet}
/// This is an example of a set of actions aligned with the content widget.
/// ```dart
/// AlertDialog(
/// title: const Text('Title'),
/// content: Container(width: 200, height: 200, color: Colors.green),
/// actions: <Widget>[
/// ElevatedButton(onPressed: () {}, child: const Text('Button 1')),
/// ElevatedButton(onPressed: () {}, child: const Text('Button 2')),
/// ],
/// actionsPadding: const EdgeInsets.symmetric(horizontal: 8.0),
/// )
/// ```
/// {@end-tool}
/// 
/// See also:
/// 
/// * [OverflowBar], which [actions] configures to lay itself out.
		public IntPtr actionsPadding
		{
			get => _actionsPadding;
			set { _actionsPadding = value; HasactionsPadding = 1; }
		}

		// Has flag for nullable property: actionsAlignment
		public byte HasactionsAlignment { get; set; }

		// Nullable field: actionsAlignment
		private MainAxisAlignment _actionsAlignment;

/// Defines the horizontal layout of the [actions] according to the same
/// rules as for [Row.mainAxisAlignment].
/// 
/// This parameter is passed along to the dialog's [OverflowBar].
/// 
/// If this parameter is null (the default) then [MainAxisAlignment.end]
/// is used.
		public MainAxisAlignment actionsAlignment
		{
			get => _actionsAlignment;
			set { _actionsAlignment = value; HasactionsAlignment = 1; }
		}

		// Has flag for nullable property: actionsOverflowAlignment
		public byte HasactionsOverflowAlignment { get; set; }

		// Nullable field: actionsOverflowAlignment
		private IntPtr _actionsOverflowAlignment;

/// The horizontal alignment of [actions] within the vertical
/// "overflow" layout.
/// 
/// If the dialog's [actions] do not fit into a single row, then they
/// are arranged in a column. This parameter controls the horizontal
/// alignment of widgets in the case of an overflow.
/// 
/// If this parameter is null (the default) then [OverflowBarAlignment.end]
/// is used.
/// 
/// See also:
/// 
/// * [OverflowBar], which [actions] configures to lay itself out.
		public IntPtr actionsOverflowAlignment
		{
			get => _actionsOverflowAlignment;
			set { _actionsOverflowAlignment = value; HasactionsOverflowAlignment = 1; }
		}

		// Has flag for nullable property: actionsOverflowDirection
		public byte HasactionsOverflowDirection { get; set; }

		// Nullable field: actionsOverflowDirection
		private VerticalDirection _actionsOverflowDirection;

/// The vertical direction of [actions] if the children overflow
/// horizontally.
/// 
/// If the dialog's [actions] do not fit into a single row, then they
/// are arranged in a column. The first action is at the top of the
/// column if this property is set to [VerticalDirection.down], since it
/// "starts" at the top and "ends" at the bottom. On the other hand,
/// the first action will be at the bottom of the column if this
/// property is set to [VerticalDirection.up], since it "starts" at the
/// bottom and "ends" at the top.
/// 
/// See also:
/// 
/// * [OverflowBar], which [actions] configures to lay itself out.
		public VerticalDirection actionsOverflowDirection
		{
			get => _actionsOverflowDirection;
			set { _actionsOverflowDirection = value; HasactionsOverflowDirection = 1; }
		}

		// Has flag for nullable property: actionsOverflowButtonSpacing
		public byte HasactionsOverflowButtonSpacing { get; set; }

		// Nullable field: actionsOverflowButtonSpacing
		private double _actionsOverflowButtonSpacing;

/// The spacing between [actions] when the [OverflowBar] switches to a column
/// layout because the actions don't fit horizontally.
/// 
/// If the widgets in [actions] do not fit into a single row, they are
/// arranged into a column. This parameter provides additional vertical space
/// between buttons when it does overflow.
/// 
/// The button spacing may appear to be more than the value provided. This is
/// because most buttons adhere to the [MaterialTapTargetSize] of 48px. So,
/// even though a button might visually be 36px in height, it might still take
/// up to 48px vertically.
/// 
/// If null then no spacing will be added in between buttons in an overflow
/// state.
		public double actionsOverflowButtonSpacing
		{
			get => _actionsOverflowButtonSpacing;
			set { _actionsOverflowButtonSpacing = value; HasactionsOverflowButtonSpacing = 1; }
		}

		// Has flag for nullable property: buttonPadding
		public byte HasbuttonPadding { get; set; }

		// Nullable field: buttonPadding
		private IntPtr _buttonPadding;

/// The padding that surrounds each button in [actions].
/// 
/// This is different from [actionsPadding], which defines the padding
/// between the entire button bar and the edges of the dialog.
/// 
/// If this property is null, then it will default to
/// 8.0 logical pixels on the left and right.
		public IntPtr buttonPadding
		{
			get => _buttonPadding;
			set { _buttonPadding = value; HasbuttonPadding = 1; }
		}

		// Has flag for nullable property: backgroundColor
		public byte HasbackgroundColor { get; set; }

		// Nullable field: backgroundColor
		private IntPtr _backgroundColor;

/// {@macro flutter.material.dialog.backgroundColor}
		public IntPtr backgroundColor
		{
			get => _backgroundColor;
			set { _backgroundColor = value; HasbackgroundColor = 1; }
		}

		// Has flag for nullable property: elevation
		public byte Haselevation { get; set; }

		// Nullable field: elevation
		private double _elevation;

/// {@macro flutter.material.dialog.elevation}
		public double elevation
		{
			get => _elevation;
			set { _elevation = value; Haselevation = 1; }
		}

		// Has flag for nullable property: shadowColor
		public byte HasshadowColor { get; set; }

		// Nullable field: shadowColor
		private IntPtr _shadowColor;

/// {@macro flutter.material.dialog.shadowColor}
		public IntPtr shadowColor
		{
			get => _shadowColor;
			set { _shadowColor = value; HasshadowColor = 1; }
		}

		// Has flag for nullable property: surfaceTintColor
		public byte HassurfaceTintColor { get; set; }

		// Nullable field: surfaceTintColor
		private IntPtr _surfaceTintColor;

/// {@macro flutter.material.dialog.surfaceTintColor}
		public IntPtr surfaceTintColor
		{
			get => _surfaceTintColor;
			set { _surfaceTintColor = value; HassurfaceTintColor = 1; }
		}

		// Has flag for nullable property: semanticLabel
		public byte HassemanticLabel { get; set; }

		// String field: semanticLabel
		private IntPtr _semanticLabel;

/// The semantic label of the dialog used by accessibility frameworks to
/// announce screen transitions when the dialog is opened and closed.
/// 
/// In iOS, if this label is not provided, a semantic label will be inferred
/// from the [title] if it is not null.
/// 
/// In Android, if this label is not provided, the dialog will use the
/// [MaterialLocalizations.alertDialogLabel] as its label.
/// 
/// See also:
/// 
/// * [SemanticsConfiguration.namesRoute], for a description of how this
/// value is used.
		public string? semanticLabel
		{
			get => GetString(_semanticLabel);
			set { SetString(ref _semanticLabel, value); HassemanticLabel = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: insetPadding
		public byte HasinsetPadding { get; set; }

		// Nullable field: insetPadding
		private IntPtr _insetPadding;

/// {@macro flutter.material.dialog.insetPadding}
		public IntPtr insetPadding
		{
			get => _insetPadding;
			set { _insetPadding = value; HasinsetPadding = 1; }
		}

		// Has flag for nullable property: clipBehavior
		public byte HasclipBehavior { get; set; }

		// Nullable field: clipBehavior
		private Clip _clipBehavior;

/// {@macro flutter.material.dialog.clipBehavior}
		public Clip clipBehavior
		{
			get => _clipBehavior;
			set { _clipBehavior = value; HasclipBehavior = 1; }
		}

		// Has flag for nullable property: shape
		public byte Hasshape { get; set; }

		// Nullable field: shape
		private IntPtr _shape;

/// {@macro flutter.material.dialog.shape}
		public IntPtr shape
		{
			get => _shape;
			set { _shape = value; Hasshape = 1; }
		}

		// Has flag for nullable property: alignment
		public byte Hasalignment { get; set; }

		// Nullable field: alignment
		private IntPtr _alignment;

/// {@macro flutter.material.dialog.alignment}
		public IntPtr alignment
		{
			get => _alignment;
			set { _alignment = value; Hasalignment = 1; }
		}

		// Has flag for nullable property: constraints
		public byte Hasconstraints { get; set; }

		// Nullable field: constraints
		private IntPtr _constraints;

/// {@macro flutter.material.dialog.constraints}
		public IntPtr constraints
		{
			get => _constraints;
			set { _constraints = value; Hasconstraints = 1; }
		}

		// Simple field: scrollable
/// Determines whether the [title] and [content] widgets are wrapped in a
/// scrollable.
/// 
/// This configuration is used when the [title] and [content] are expected
/// to overflow. Both [title] and [content] are wrapped in a scroll view,
/// allowing all overflowed content to be visible while still showing the
/// button bar.
		public bool scrollable { get; set; }

	}
}
