// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter;
using Flutter.Enums;
using Flutter.Widgets;
using Flutter.Material;

namespace Flutter.Structs
{
/// A Material Design icon button.
///
/// An icon button is a picture printed on a Material widget that reacts to touches
/// by filling with color (ink).
///
/// Icon buttons are commonly used in the [AppBar.actions] field, but they can be used
/// in many other places as well.
///
/// If the [onPressed] callback is null, then the button will be disabled and will not
/// react to touch.
///
/// Requires one of its ancestors to be a [Material] widget. This is typically
/// provided by the app's [Scaffold].
///
/// The hit region of an icon button is larger than the actual [iconSize], to make
/// it easier to tap. If you are trying to size the widget to a particular extent,
/// use the [iconSize] property instead of, for example, a [SizedBox] or [Container].
///
/// Icon buttons can be given a [tooltip] to show when the user long-presses the button.
/// The tooltip string is used for accessibility.
///
/// {@tool dartpad}
/// This sample shows an [IconButton] that uses the Material icon "volume_up" to
/// increase the volume.
///
/// ** See code in examples/api/lib/material/icon_button/icon_button.0.dart **
/// {@end-tool}
///
/// See also:
///
/// * [Icons], the library of Material icons.
/// * [BackButton], an icon button for a "back" affordance which adapts to the
///   current platform's conventions.
/// * [CloseButton], an icon button for closing pages.
/// * [AppBar], to show a toolbar at the top of an application.
/// * [TextButton], [ElevatedButton], [OutlinedButton], for buttons with text labels and an optional icon.
/// * <https://material.io/design/components/buttons.html#toggle-button>
	[StructLayout(LayoutKind.Sequential)]
	internal class IconButtonStruct : WidgetStruct
	{
		// Has flag for nullable property: onPressed
		public byte HasonPressed { get; set; }

		// Callback field: onPressed
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onPressed;

/// The callback that is called when the button is tapped or otherwise activated.
///
/// If this is null, the button will be disabled.
		/// <summary>
		/// Action identifier for onPressed callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "icon_button_pressed") that your C# action handler will recognize.
		/// </summary>
		public string? onPressedAction
		{
			get => GetString(_onPressed);
			set { SetString(ref _onPressed, value); HasonPressed = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: onLongPress
		public byte HasonLongPress { get; set; }

		// Callback field: onLongPress
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onLongPress;

/// Called when the button is long-pressed.
///
/// If this callback is null, the button will still respond to tap events.
		/// <summary>
		/// Action identifier for onLongPress callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "icon_button_long_press") that your C# action handler will recognize.
		/// </summary>
		public string? onLongPressAction
		{
			get => GetString(_onLongPress);
			set { SetString(ref _onLongPress, value); HasonLongPress = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: icon
/// The icon to display inside the button.
///
/// The [Icon.size] and [Icon.color] of the icon are configured automatically
/// based on the [iconSize] and [color] parameters.
///
/// Typically an [Icon] widget.
		public IntPtr icon { get; set; }

		// Has flag for nullable property: tooltip
		public byte Hastooltip { get; set; }

		// String field: tooltip
		private IntPtr _tooltip;

/// Text that describes the action that will occur when the button is pressed.
///
/// This text is displayed when the user long-presses on the button and is
/// used for accessibility.
		public string? tooltip
		{
			get => GetString(_tooltip);
			set { SetString(ref _tooltip, value); Hastooltip = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: iconSize
		public byte HasiconSize { get; set; }

		// Nullable field: iconSize
/// The size of the icon inside the button.
///
/// This property must be null or a positive number. Defaults to 24.0.
///
/// If this property is null, then [IconButtonTheme.of(context).style?.iconSize]
/// is used. If that is also null, then [24.0] is used.
///
/// The icon is centered within the button, and the button itself has a
/// larger hit region than the icon (see [constraints]).
		public double iconSize { get; set; }

		// Simple field: autofocus
/// True if this widget will be selected as the initial focus when no other
/// node in its scope is currently focused.
///
/// Ideally, there is only one widget with autofocus set in each
/// [FocusScope]. If there is more than one widget with autofocus set, then
/// the first one added to the tree will get focus.
///
/// Defaults to false.
		public bool autofocus { get; set; }

		// Simple field: padding
/// The padding of the button.
///
/// The padding is defined in terms of the [EdgeInsets] class.
///
/// If this property is null, then [IconButtonTheme.of(context).style?.padding]
/// is used. If that is also null, then a default of 8.0 logical pixels on all
/// sides is used.
		public IntPtr padding { get; set; }

		// Simple field: color
/// The color to use for the icon inside the button, if the icon is enabled.
///
/// Defaults to leaving this up to the [icon] widget.
///
/// The icon is enabled if [onPressed] is not null.
///
/// See also:
///
/// * [disabledColor], which is used when [onPressed] is null.
		public IntPtr color { get; set; }

		// Simple field: alignment
/// Defines how the icon is positioned within the IconButton.
///
/// This property must not be null. It defaults to [Alignment.center].
///
/// See also:
///
/// * [Alignment], a class for aligning a child within a parent.
		public IntPtr alignment { get; set; }

	}
}
