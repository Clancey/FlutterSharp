// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Runtime.InteropServices;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;
using Flutter.Services;
using Flutter.Navigation;

namespace Flutter.Structs
{
/// An iOS-style switch.
/// 
/// Used to toggle the on/off state of a single setting.
/// 
/// {@youtube 560 315 https://www.youtube.com/watch?v=24tg_N4sdMQ}
/// 
/// The switch itself does not maintain its toggle state. Instead, when the
/// toggle state of the switch changes, the widget calls the [onChanged]
/// callback. Most widgets that use a switch will listen for the [onChanged]
/// callback and rebuild the switch with a new [value] to update the visual
/// appearance of the switch.
/// 
/// {@tool dartpad}
/// This example shows a toggleable [CupertinoSwitch]. When the thumb slides to
/// the other side of the track, the switch is toggled between on/off.
/// 
/// ** See code in examples/api/lib/cupertino/switch/cupertino_switch.0.dart **
/// {@end-tool}
/// 
/// {@tool snippet}
/// 
/// This sample shows how to use a [CupertinoSwitch] in a [CupertinoListTile]. The
/// [MergeSemantics] is used to turn the entire [CupertinoListTile] into a single item
/// for accessibility tools.
/// 
/// ```dart
/// MergeSemantics(
/// child: CupertinoListTile(
/// title: const Text('Lights'),
/// trailing: CupertinoSwitch(
/// value: _lights,
/// onChanged: (bool value) { setState(() { _lights = value; }); },
/// ),
/// onTap: () { setState(() { _lights = !_lights; }); },
/// ),
/// )
/// ```
/// {@end-tool}
/// 
/// See also:
/// 
/// * [Switch], the Material Design equivalent.
/// * <https://developer.apple.com/design/human-interface-guidelines/toggles/>
	[StructLayout(LayoutKind.Sequential)]
	internal class CupertinoSwitchStruct : WidgetStruct
	{
		// Simple field: value
/// Whether this switch is on or off.
		public bool value { get; set; }

		// Has flag for nullable property: onChanged
		public byte HasonChanged { get; set; }

		// Callback field: onChanged
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onChanged;

/// Called when the user toggles the switch on or off.
/// 
/// The switch passes the new value to the callback but does not actually
/// change state until the parent widget rebuilds the switch with the new
/// value.
/// 
/// If null, the switch will be displayed as disabled, which has a reduced opacity.
/// 
/// The callback provided to [onChanged] should update the state of the parent
/// [StatefulWidget] using the [State.setState] method, so that the parent
/// gets rebuilt; for example:
/// 
/// ```dart
/// CupertinoSwitch(
/// value: _giveVerse,
/// onChanged: (bool newValue) {
/// setState(() {
/// _giveVerse = newValue;
/// });
/// },
/// )
/// ```
		/// <summary>
		/// Action identifier for onChanged callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onChangedAction
		{
			get => GetString(_onChanged);
			set { SetString(ref _onChanged, value); HasonChanged = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: activeTrackColor
		public byte HasactiveTrackColor { get; set; }

		// Nullable field: activeTrackColor
		private IntPtr _activeTrackColor;

/// The color to use for the track when the switch is on.
/// 
/// If null and [applyTheme] is false, defaults to [CupertinoColors.systemGreen]
/// in accordance to native iOS behavior. Otherwise, defaults to
/// [CupertinoThemeData.primaryColor].
/// 
/// See also:
/// 
/// * [inactiveTrackColor], the color to use for the track when the switch is off.
		public IntPtr activeTrackColor
		{
			get => _activeTrackColor;
			set { _activeTrackColor = value; HasactiveTrackColor = 1; }
		}

		// Has flag for nullable property: inactiveTrackColor
		public byte HasinactiveTrackColor { get; set; }

		// Nullable field: inactiveTrackColor
		private IntPtr _inactiveTrackColor;

/// The color to use for the track when the switch is off.
/// 
/// Defaults to [CupertinoColors.secondarySystemFill] when null.
/// 
/// See also:
/// 
/// * [activeTrackColor], the color to use for the track when the switch is on.
		public IntPtr inactiveTrackColor
		{
			get => _inactiveTrackColor;
			set { _inactiveTrackColor = value; HasinactiveTrackColor = 1; }
		}

		// Has flag for nullable property: thumbColor
		public byte HasthumbColor { get; set; }

		// Nullable field: thumbColor
		private IntPtr _thumbColor;

/// The color to use for the thumb when the switch is on.
/// 
/// If this color is not opaque, it is blended against
/// [CupertinoThemeData.scaffoldBackgroundColor], so as not to see through the
/// thumb to the track underneath.
/// 
/// Defaults to [CupertinoColors.white] when null.
/// 
/// See also:
/// 
/// * [inactiveThumbColor], the color to use for the thumb when the switch is off.
		public IntPtr thumbColor
		{
			get => _thumbColor;
			set { _thumbColor = value; HasthumbColor = 1; }
		}

		// Has flag for nullable property: inactiveThumbColor
		public byte HasinactiveThumbColor { get; set; }

		// Nullable field: inactiveThumbColor
		private IntPtr _inactiveThumbColor;

/// The color to use on the thumb when the switch is off.
/// 
/// If this color is not opaque, it is blended against
/// [CupertinoThemeData.scaffoldBackgroundColor], so as not to see through the
/// thumb to the track underneath.
/// 
/// If null, defaults to [thumbColor]. If that is also null,
/// [CupertinoColors.white] is used.
/// 
/// See also:
/// 
/// * [thumbColor], the color to use for the thumb when the switch is on.
		public IntPtr inactiveThumbColor
		{
			get => _inactiveThumbColor;
			set { _inactiveThumbColor = value; HasinactiveThumbColor = 1; }
		}

		// Has flag for nullable property: focusColor
		public byte HasfocusColor { get; set; }

		// Nullable field: focusColor
		private IntPtr _focusColor;

/// The color to use for the focus highlight for keyboard interactions.
/// 
/// Defaults to [activeColor] with an opacity of 0.80, a brightness of 0.69
/// and a saturation of 0.835.
		public IntPtr focusColor
		{
			get => _focusColor;
			set { _focusColor = value; HasfocusColor = 1; }
		}

		// Has flag for nullable property: onLabelColor
		public byte HasonLabelColor { get; set; }

		// Nullable field: onLabelColor
		private IntPtr _onLabelColor;

/// The color to use for the accessibility label when the switch is on.
/// 
/// Defaults to [CupertinoColors.white] when null.
		public IntPtr onLabelColor
		{
			get => _onLabelColor;
			set { _onLabelColor = value; HasonLabelColor = 1; }
		}

		// Has flag for nullable property: offLabelColor
		public byte HasoffLabelColor { get; set; }

		// Nullable field: offLabelColor
		private IntPtr _offLabelColor;

/// The color to use for the accessibility label when the switch is off.
/// 
/// Defaults to [Color.fromARGB(255, 179, 179, 179)]
/// (or [Color.fromARGB(255, 255, 255, 255)] in high contrast) when null.
		public IntPtr offLabelColor
		{
			get => _offLabelColor;
			set { _offLabelColor = value; HasoffLabelColor = 1; }
		}

		// Has flag for nullable property: activeThumbImage
		public byte HasactiveThumbImage { get; set; }

		// Nullable field: activeThumbImage
		private IntPtr _activeThumbImage;

/// {@macro flutter.material.switch.activeThumbImage}
		public IntPtr activeThumbImage
		{
			get => _activeThumbImage;
			set { _activeThumbImage = value; HasactiveThumbImage = 1; }
		}

		// Has flag for nullable property: onActiveThumbImageError
		public byte HasonActiveThumbImageError { get; set; }

		// Callback field: onActiveThumbImageError
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onActiveThumbImageError;

/// {@macro flutter.material.switch.onActiveThumbImageError}
		/// <summary>
		/// Action identifier for onActiveThumbImageError callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onActiveThumbImageErrorAction
		{
			get => GetString(_onActiveThumbImageError);
			set { SetString(ref _onActiveThumbImageError, value); HasonActiveThumbImageError = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: inactiveThumbImage
		public byte HasinactiveThumbImage { get; set; }

		// Nullable field: inactiveThumbImage
		private IntPtr _inactiveThumbImage;

/// {@macro flutter.material.switch.inactiveThumbImage}
		public IntPtr inactiveThumbImage
		{
			get => _inactiveThumbImage;
			set { _inactiveThumbImage = value; HasinactiveThumbImage = 1; }
		}

		// Has flag for nullable property: onInactiveThumbImageError
		public byte HasonInactiveThumbImageError { get; set; }

		// Callback field: onInactiveThumbImageError
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onInactiveThumbImageError;

/// {@macro flutter.material.switch.onInactiveThumbImageError}
		/// <summary>
		/// Action identifier for onInactiveThumbImageError callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onInactiveThumbImageErrorAction
		{
			get => GetString(_onInactiveThumbImageError);
			set { SetString(ref _onInactiveThumbImageError, value); HasonInactiveThumbImageError = (byte)(value != null ? 1 : 0); }
		}

		// Has flag for nullable property: trackOutlineColor
		public byte HastrackOutlineColor { get; set; }

		// Nullable field: trackOutlineColor
		private IntPtr _trackOutlineColor;

/// The outline color of this [CupertinoSwitch]'s track.
/// 
/// Resolved in the following states:
/// * [WidgetState.selected].
/// * [WidgetState.hovered].
/// * [WidgetState.focused].
/// * [WidgetState.disabled].
/// 
/// {@tool snippet}
/// This example resolves the [trackOutlineColor] based on the current
/// [WidgetState] of the [CupertinoSwitch], providing a different [Color] when it is
/// [WidgetState.disabled].
/// 
/// ```dart
/// CupertinoSwitch(
/// value: true,
/// onChanged: (bool value) { },
/// trackOutlineColor: WidgetStateProperty.resolveWith<Color?>((Set<WidgetState> states) {
/// if (states.contains(WidgetState.disabled)) {
/// return CupertinoColors.activeOrange.withOpacity(.48);
/// }
/// return null; // Use the default color.
/// }),
/// )
/// ```
/// {@end-tool}
/// 
/// The [CupertinoSwitch] track has no outline by default.
		public IntPtr trackOutlineColor
		{
			get => _trackOutlineColor;
			set { _trackOutlineColor = value; HastrackOutlineColor = 1; }
		}

		// Has flag for nullable property: trackOutlineWidth
		public byte HastrackOutlineWidth { get; set; }

		// Nullable field: trackOutlineWidth
		private IntPtr _trackOutlineWidth;

/// The outline width of this [CupertinoSwitch]'s track.
/// 
/// Resolved in the following states:
/// * [WidgetState.selected].
/// * [WidgetState.hovered].
/// * [WidgetState.focused].
/// * [WidgetState.disabled].
/// 
/// {@tool snippet}
/// This example resolves the [trackOutlineWidth] based on the current
/// [WidgetState] of the [CupertinoSwitch], providing a different outline width when it is
/// [WidgetState.disabled].
/// 
/// ```dart
/// CupertinoSwitch(
/// value: true,
/// onChanged: (bool value) { },
/// trackOutlineWidth: WidgetStateProperty.resolveWith<double?>((Set<WidgetState> states) {
/// if (states.contains(WidgetState.disabled)) {
/// return 5.0;
/// }
/// return null; // Use the default width.
/// }),
/// )
/// ```
/// {@end-tool}
/// 
/// Since a [CupertinoSwitch] has no track outline by default, this parameter
/// is set only if [trackOutlineColor] is provided.
/// 
/// Defaults to 2.0 if a [trackOutlineColor] is provided.
		public IntPtr trackOutlineWidth
		{
			get => _trackOutlineWidth;
			set { _trackOutlineWidth = value; HastrackOutlineWidth = 1; }
		}

		// Has flag for nullable property: thumbIcon
		public byte HasthumbIcon { get; set; }

		// Nullable field: thumbIcon
		private IntPtr _thumbIcon;

/// The icon to use on the thumb of this switch.
/// 
/// Resolved in the following states:
/// * [WidgetState.selected].
/// * [WidgetState.hovered].
/// * [WidgetState.focused].
/// * [WidgetState.disabled].
/// 
/// {@tool snippet}
/// This example resolves the [thumbIcon] based on the current
/// [WidgetState] of the [CupertinoSwitch], providing a different [Icon] when it is
/// [WidgetState.disabled].
/// 
/// ```dart
/// CupertinoSwitch(
/// value: true,
/// onChanged: (bool value) { },
/// thumbIcon: WidgetStateProperty.resolveWith<Icon?>((Set<WidgetState> states) {
/// if (states.contains(WidgetState.disabled)) {
/// return const Icon(Icons.close);
/// }
/// return null; // All other states will use the default thumbIcon.
/// }),
/// )
/// ```
/// {@end-tool}
/// 
/// If null, then the [CupertinoSwitch] does not have any icons on the thumb.
		public IntPtr thumbIcon
		{
			get => _thumbIcon;
			set { _thumbIcon = value; HasthumbIcon = 1; }
		}

		// Has flag for nullable property: mouseCursor
		public byte HasmouseCursor { get; set; }

		// Nullable field: mouseCursor
		private IntPtr _mouseCursor;

/// The cursor for a mouse pointer when it enters or is hovering over the
/// widget.
/// 
/// Resolved in the following states:
/// * [WidgetState.selected].
/// * [WidgetState.hovered].
/// * [WidgetState.focused].
/// * [WidgetState.disabled].
/// 
/// {@tool snippet}
/// This example resolves the [mouseCursor] based on the current
/// [WidgetState] of the [CupertinoSwitch], providing a different [mouseCursor] when it is
/// [WidgetState.disabled].
/// 
/// ```dart
/// CupertinoSwitch(
/// value: true,
/// onChanged: (bool value) { },
/// mouseCursor: WidgetStateProperty.resolveWith<MouseCursor>((Set<WidgetState> states) {
/// if (states.contains(WidgetState.disabled)) {
/// return SystemMouseCursors.click;
/// }
/// return SystemMouseCursors.basic; // All other states will use the default mouseCursor.
/// }),
/// )
/// ```
/// {@end-tool}
/// 
/// If null, then [MouseCursor.defer] is used when the switch is disabled.
/// When the switch is enabled, [SystemMouseCursors.click] is used on Web, and
/// [MouseCursor.defer] is used on other platforms.
/// 
/// See also:
/// 
/// * [WidgetStateMouseCursor], a [MouseCursor] that implements
/// `WidgetStateProperty` which is used in APIs that need to accept
/// either a [MouseCursor] or a [WidgetStateProperty].
		public IntPtr mouseCursor
		{
			get => _mouseCursor;
			set { _mouseCursor = value; HasmouseCursor = 1; }
		}

		// Has flag for nullable property: focusNode
		public byte HasfocusNode { get; set; }

		// Nullable field: focusNode
		private IntPtr _focusNode;

/// {@macro flutter.widgets.Focus.focusNode}
		public IntPtr focusNode
		{
			get => _focusNode;
			set { _focusNode = value; HasfocusNode = 1; }
		}

		// Has flag for nullable property: onFocusChange
		public byte HasonFocusChange { get; set; }

		// Callback field: onFocusChange
		// Using action string pattern - Dart will dispatch action to C# via method channel
		IntPtr _onFocusChange;

/// Handler called when the focus changes.
/// 
/// Called with true if this widget's node gains focus, and false if it loses
/// focus.
		/// <summary>
		/// Action identifier for onFocusChange callback.
		/// When this action is triggered in Dart, it will be dispatched to C# via method channel.
		/// Set to a string identifier (e.g., "button_pressed_main") that your C# action handler will recognize.
		/// </summary>
		public string? onFocusChangeAction
		{
			get => GetString(_onFocusChange);
			set { SetString(ref _onFocusChange, value); HasonFocusChange = (byte)(value != null ? 1 : 0); }
		}

		// Simple field: autofocus
/// {@macro flutter.widgets.Focus.autofocus}
		public bool autofocus { get; set; }

		// Has flag for nullable property: applyTheme
		public byte HasapplyTheme { get; set; }

		// Nullable field: applyTheme
		private bool _applyTheme;

/// {@template flutter.cupertino.CupertinoSwitch.applyTheme}
/// Whether to apply the ambient [CupertinoThemeData].
/// 
/// If true, the track uses [CupertinoThemeData.primaryColor] for the track
/// when the switch is on.
/// 
/// Defaults to [CupertinoThemeData.applyThemeToAll].
/// {@endtemplate}
		public bool applyTheme
		{
			get => _applyTheme;
			set { _applyTheme = value; HasapplyTheme = 1; }
		}

		// Simple field: dragStartBehavior
/// {@template flutter.cupertino.CupertinoSwitch.dragStartBehavior}
/// Determines the way that drag start behavior is handled.
/// 
/// If set to [DragStartBehavior.start], the drag behavior used to move the
/// switch from on to off will begin at the position where the drag gesture won
/// the arena. If set to [DragStartBehavior.down] it will begin at the position
/// where a down event was first detected.
/// 
/// In general, setting this to [DragStartBehavior.start] will make drag
/// animation smoother and setting it to [DragStartBehavior.down] will make
/// drag behavior feel slightly more reactive.
/// 
/// By default, the drag start behavior is [DragStartBehavior.start].
/// 
/// See also:
/// 
/// * [DragGestureRecognizer.dragStartBehavior], which gives an example for
/// the different behaviors.
/// 
/// {@endtemplate}
		public DragStartBehavior dragStartBehavior { get; set; }

		// Has flag for nullable property: activeColor
		public byte HasactiveColor { get; set; }

		// Nullable field: activeColor
		private IntPtr _activeColor;

		public IntPtr activeColor
		{
			get => _activeColor;
			set { _activeColor = value; HasactiveColor = 1; }
		}

		// Has flag for nullable property: trackColor
		public byte HastrackColor { get; set; }

		// Nullable field: trackColor
		private IntPtr _trackColor;

		public IntPtr trackColor
		{
			get => _trackColor;
			set { _trackColor = value; HastrackColor = 1; }
		}

	}
}
