namespace Flutter.Enums
{
	/// <summary>
	/// Defines the way to treat the selection's rect width.
	/// </summary>
	public enum BoxWidthStyle
	{
		/// Provide tight bounding boxes that fit widths per run.
		Tight = 0,
		/// Extends the width to the maximum width of the box.
		Max = 1
	}
}
