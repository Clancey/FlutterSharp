// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Collections;
using System.Collections.Generic;
using Flutter;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Structs;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;

namespace Flutter.Material
{
    /// <summary>
    /// A Material Design primary tab bar.
    ///
    /// A tab bar widget with tabs that represents the primary content destinations.
    /// Each tab corresponds to a view that is displayed when the tab is selected.
    ///
    /// Typically created as the AppBar.bottom part of an AppBar and in conjunction
    /// with a TabBarView.
    ///
    /// If a TabController is not provided, then a DefaultTabController ancestor
    /// must be provided instead. The tab controller's TabController.length must
    /// equal the length of the tabs list.
    ///
    /// Example:
    /// ```csharp
    /// new TabBar {
    ///     new Tab(text: "Tab 1"),
    ///     new Tab(text: "Tab 2"),
    ///     new Tab(text: "Tab 3")
    /// }
    /// ```
    ///
    /// See also:
    ///  * Tab, which represents a single tab.
    ///  * TabBarView, which displays widgets for the currently selected tab.
    ///  * TabController, which coordinates tab selection between a TabBar and a TabBarView.
    ///  * DefaultTabController, which is used when sharing a TabController isn't convenient.
    /// </summary>
    public class TabBar : MultiChildRenderObjectWidget, IEnumerable<Widget>
    {
        /// <summary>
        /// Internal list for collection initializer support
        /// </summary>
        private List<Widget> _tabsList = new List<Widget>();

        /// <summary>
        /// Adds a tab widget. Supports collection initializer syntax.
        /// </summary>
        public void Add(Widget tab)
        {
            _tabsList.Add(tab);
        }

        /// <summary>
        /// Adds multiple tab widgets.
        /// </summary>
        public void AddRange(IEnumerable<Widget> tabs)
        {
            _tabsList.AddRange(tabs);
        }

        /// <summary>
        /// Gets the enumerator for tab widgets.
        /// </summary>
        public IEnumerator<Widget> GetEnumerator() => _tabsList.GetEnumerator();

        /// <summary>
        /// Gets the enumerator for tab widgets.
        /// </summary>
        IEnumerator IEnumerable.GetEnumerator() => GetEnumerator();

        /// <summary>
        /// Initializes a new instance of the <see cref="TabBar"/> class.
        /// Use collection initializer syntax: new TabBar { tab1, tab2, tab3 }
        /// </summary>
        public TabBar()
        {
        }

        /// <summary>
        /// Initializes a new instance of the <see cref="TabBar"/> class.
        /// </summary>
        /// <param name="tabs">Typically a list of two or more Tab widgets.</param>
        /// <param name="isScrollable">Whether this tab bar can be scrolled horizontally.</param>
        /// <param name="indicatorWeight">The thickness of the line drawn below the selected tab.</param>
        public TabBar(
            List<Widget>? tabs = null,
            bool isScrollable = false,
            double indicatorWeight = 2.0
        )
        {
            if (tabs != null)
                _tabsList.AddRange(tabs);

            if (isScrollable)
            {
                throw new NotSupportedException($"{nameof(TabBar)}.{nameof(isScrollable)} is not supported yet.");
            }

            if (indicatorWeight != 2.0)
            {
                throw new NotSupportedException($"{nameof(TabBar)}.{nameof(indicatorWeight)} is not supported yet.");
            }

            // Children are set in PrepareForSending to support collection initializers
        }

        /// <summary>
        /// Prepares the widget for sending to Flutter, including setting children.
        /// </summary>
        internal override void PrepareForSending()
        {
            var s = GetBackingStruct<TabBarStruct>();
            s.children = SetChildrenAndGetPointer(_tabsList);
            base.PrepareForSending();
        }

        protected override FlutterObjectStruct CreateBackingStruct() => new TabBarStruct();
    }
}
