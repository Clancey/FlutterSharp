// <auto-generated>
// This file was automatically generated by FlutterSharp.CodeGen
// Do not modify this file directly
// </auto-generated>

using System;
using System.Collections;
using System.Collections.Generic;
using Flutter;
using Flutter.Enums;
using Flutter.Gestures;
using Flutter.UI;
using Flutter.Structs;
using Flutter.Widgets;
using Flutter.Material;
using Flutter.Cupertino;

namespace Flutter.Widgets
{
/// A Material Design icon button.
///
/// An icon button is a picture printed on a Material widget that reacts to touches
/// by filling with color (ink).
///
/// Icon buttons are commonly used in the [AppBar.actions] field, but they can be used
/// in many other places as well.
///
/// If the [onPressed] callback is null, then the button will be disabled and will not
/// react to touch.
///
/// Requires one of its ancestors to be a [Material] widget. This is typically
/// provided by the app's [Scaffold].
///
/// The hit region of an icon button is larger than the actual [iconSize], to make
/// it easier to tap. If you are trying to size the widget to a particular extent,
/// use the [iconSize] property instead of, for example, a [SizedBox] or [Container].
///
/// Icon buttons can be given a [tooltip] to show when the user long-presses the button.
/// The tooltip string is used for accessibility.
///
/// {@tool dartpad}
/// This sample shows an [IconButton] that uses the Material icon "volume_up" to
/// increase the volume.
///
/// ** See code in examples/api/lib/material/icon_button/icon_button.0.dart **
/// {@end-tool}
///
/// See also:
///
/// * [Icons], the library of Material icons.
/// * [BackButton], an icon button for a "back" affordance which adapts to the
///   current platform's conventions.
/// * [CloseButton], an icon button for closing pages.
/// * [AppBar], to show a toolbar at the top of an application.
/// * [TextButton], [ElevatedButton], [OutlinedButton], for buttons with text labels and an optional icon.
/// * <https://material.io/design/components/buttons.html#toggle-button>
	public class IconButton : StatelessWidget
	{
		/// <summary>
		/// Initializes a new instance of the <see cref="IconButton"/> class.
		/// </summary>
		/// <param name="onPressed">The callback that is called when the button is tapped or otherwise activated. If null, the button will be disabled.</param>
		/// <param name="onLongPress">Called when the button is long-pressed. If null, the button will still respond to tap events.</param>
		/// <param name="icon">The icon to display inside the button. Typically an Icon widget. Required.</param>
		/// <param name="tooltip">Text that describes the action that will occur when the button is pressed. Displayed on long-press and used for accessibility.</param>
		/// <param name="iconSize">The size of the icon inside the button. Defaults to 24.0 if not specified.</param>
		/// <param name="autofocus">True if this widget will be selected as the initial focus. Defaults to false.</param>
		/// <param name="padding">The padding of the button. If null, defaults to 8.0 logical pixels on all sides.</param>
		/// <param name="color">The color to use for the icon inside the button when enabled. If null, uses the icon's default color.</param>
		/// <param name="alignment">Defines how the icon is positioned within the IconButton. Defaults to Alignment.center.</param>
		public IconButton(
			Action? onPressed = null,
			Action? onLongPress = null,
			Widget? icon = null,
			string? tooltip = null,
			double? iconSize = null,
			bool autofocus = false,
			IntPtr? padding = null,
			IntPtr? color = null,
			IntPtr? alignment = null
		)
		{
			var s = GetBackingStruct<IconButtonStruct>();

			// Register callbacks and assign action IDs to struct
			s.onPressedAction = RegisterCallback(onPressed);
			s.onLongPressAction = RegisterCallback(onLongPress);

			// Assign icon widget (required)
			s.icon = GetWidgetHandle(icon);

			// Assign simple properties
			s.tooltip = tooltip;

			if (iconSize.HasValue)
			{
				s.iconSize = iconSize.Value;
				s.HasiconSize = 1;
			}

			s.autofocus = autofocus;

			// Assign IntPtr properties (padding, color, alignment)
			if (padding.HasValue)
				s.padding = padding.Value;

			if (color.HasValue)
				s.color = color.Value;

			if (alignment.HasValue)
				s.alignment = alignment.Value;
		}

		protected override FlutterObjectStruct CreateBackingStruct() => new IconButtonStruct();
	}
}
