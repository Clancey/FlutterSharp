// <auto-generated>
// This file was automatically generated on 2025-11-20 02:52:32 UTC
// DO NOT EDIT - Changes will be overwritten
// </auto-generated>

import 'dart:ffi';
import 'package:ffi/ffi.dart';
import 'package:flutter/material.dart';

/// A graphical icon widget drawn with a glyph from a font described in
an [IconData] such as material's predefined [IconData]s in [Icons].

Icons are not interactive. For an interactive icon, consider material's
[IconButton].

There must be an ambient [Directionality] widget when using [Icon].
Typically this is introduced automatically by the [WidgetsApp] or
[MaterialApp].

This widget assumes that the rendered icon is squared. Non-squared icons may
render incorrectly.

{@tool snippet}

This example shows how to create a [Row] of [Icon]s in different colors and
sizes. The first [Icon] uses a [semanticLabel] to announce in accessibility
modes like TalkBack and VoiceOver.

![The following code snippet would generate a row of icons consisting of a pink heart, a green musical note, and a blue umbrella, each progressively bigger than the last.](https://flutter.github.io/assets-for-api-docs/assets/widgets/icon.png)

```dart
const Row(
  mainAxisAlignment: MainAxisAlignment.spaceAround,
  children: <Widget>[
    Icon(
      Icons.favorite,
      color: Colors.pink,
      size: 24.0,
      semanticLabel: 'Text to announce in accessibility modes',
    ),
    Icon(
      Icons.audiotrack,
      color: Colors.green,
      size: 30.0,
    ),
    Icon(
      Icons.beach_access,
      color: Colors.blue,
      size: 36.0,
    ),
  ],
)
```
{@end-tool}

See also:

 * [IconButton], for interactive icons.
 * [Icons], for the list of available Material Icons for use with this class.
 * [IconTheme], which provides ambient configuration for icons.
 * [ImageIcon], for showing icons from [AssetImage]s or other [ImageProvider]s.
/// Parser that converts IconStruct to Icon widget.
class IconParser implements WidgetParser {
  @override
  Widget parse(Pointer<Void> structPtr) {
    if (structPtr.address == 0) {
      throw ArgumentError('Null pointer passed to IconParser');
    }

    final struct = structPtr.cast<IconStruct>().ref;

    return Icon(
      icon: struct.icon.address != 0
        ? parseIconData(struct.icon)
        : null,      size: struct.size.address != 0
        ? parseNum(struct.size)
        : null,      fill: struct.fill.address != 0
        ? parseNum(struct.fill)
        : null,      weight: struct.weight.address != 0
        ? parseNum(struct.weight)
        : null,      grade: struct.grade.address != 0
        ? parseNum(struct.grade)
        : null,      opticalSize: struct.opticalSize.address != 0
        ? parseNum(struct.opticalSize)
        : null,      color: parseObject(struct.color),      shadows: struct.shadows.address != 0
        ? parseList(struct.shadows)
        : null,      semanticLabel: struct.semanticLabel.address != 0
        ? parseObject(struct.semanticLabel)
        : null,      textDirection: parseObject(struct.textDirection),      applyTextScaling: struct.applyTextScaling.address != 0
        ? parseBool(struct.applyTextScaling)
        : null,      blendMode: parseObject(struct.blendMode)    );
  }

  /// Helper function to parse a string from a Pointer<Utf8>.
  String? parseString(Pointer<Utf8> ptr) {
    if (ptr.address == 0) return null;
    return ptr.toDartString();
  }

  /// Helper function to parse a nullable string from a Pointer<Utf8>.
  String? parseNullableString(Pointer<Utf8> ptr) {
    if (ptr.address == 0) return null;
    return ptr.toDartString();
  }

  /// Helper function to parse an integer.
  int parseInt(int value) => value;

  /// Helper function to parse a nullable integer.
  int? parseNullableInt(int value) => value == 0 ? null : value;

  /// Helper function to parse a double.
  double parseDouble(double value) => value;

  /// Helper function to parse a nullable double.
  double? parseNullableDouble(double value) {
    if (value.isNaN || value.isInfinite) return null;
    return value;
  }

  /// Helper function to parse a boolean.
  bool parseBool(int value) => value != 0;

  /// Helper function to parse a nullable boolean.
  bool? parseNullableBool(int value) => value == 0 ? null : value != 0;

  /// Helper function to parse a child widget.
  Widget? parseChild(Pointer<Void> ptr) {
    if (ptr.address == 0) return null;
    // TODO: Implement child widget parsing
    return null;
  }

  /// Helper function to parse multiple child widgets.
  List<Widget>? parseChildren(Pointer<Void> ptr) {
    if (ptr.address == 0) return null;
    // TODO: Implement children parsing
    return null;
  }

  /// Helper function to parse generic objects.
  dynamic parseObject(Pointer<Void> ptr) {
    if (ptr.address == 0) return null;
    // TODO: Implement object parsing
    return null;
  }

  /// Helper function to parse callbacks.
  Function? parseCallback(Pointer<Void> ptr) {
    if (ptr.address == 0) return null;
    // TODO: Implement callback parsing
    return null;
  }

  /// Helper function to parse lists.
  List<T>? parseList<T>(Pointer<Void> ptr) {
    if (ptr.address == 0) return null;
    // TODO: Implement list parsing
    return null;
  }

  /// Helper function to parse maps.
  Map<K, V>? parseMap<K, V>(Pointer<Void> ptr) {
    if (ptr.address == 0) return null;
    // TODO: Implement map parsing
    return null;
  }
}
